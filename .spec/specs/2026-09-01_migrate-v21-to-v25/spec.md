# Musefold v2.1 到 v2.5 全功能迁移 - 项目范围

> 2026-09-22 最新授权：用户明确要求先 Git 提交当前开发版本，再部署独立云端 2.5 并保留旧版。`dedace4` 已保存开发快照，不代表整个迁移验收完成。用户随后拒绝 `www` 根入口独占方案，改为共享域名下 `/Musefold/v25/` 应用命名空间；路径适配与独立资源部署继续，公网尚未切换。见 [独立部署记录](../../../docs/v2.5/V25-CLOUD-DEPLOYMENT.md)。此前“生产未授权”的历史描述不再限制本次明确授权范围，Windows 等未验项仍保留。

> 2026-09-21当前：按已批准的非Windows范围，真实跨端同步和三端账号已通过；账号收费方案从普通图到移动新版转正/实际格式2包下载已有逐段真实证据，实际费用616,178点含失败文本，未充值。发现并修复限流错误映射、Agent提示词字段约束和方案轮询节拍；最终候选1851项ee593e6d…的完整check38/38通过，API/565项三端/自然小时及当前包最终验证仍进行中。详细首败、接续与账单见[09-21真实验收](../../../docs/v2.5/V25-REAL-ACCEPTANCE-2026-09-21.md)。旧552P/旧包只属原输入，不替代本次验收；Windows、正式签名公证及完整产品上线边界不变，未提交。

> 2026-09-20最新：会话机制开发、本地联合验收与New API账号扩展生产部署完成。按用户授权先备份并验证恢复，只切换账号服务，7.016秒健康；旧会话保留，新协议/释放实测通过，测试会话归零。事后唯一用户字段变化为验证登录的last_login_at，积分/计费/其他用户不变。源码1850项bb580e98…及原本地check38/38、完整三端552P/0F/13S/0flaky、最终macOS包7P/真实备份3P证据继续有效。任务91/96；Musefold v1.1公共站点未切换，真实同步/收费方案与完整非Windows迁移仍未闭合，未提前提交。见[会话机制§8.3–§10](../../../docs/v2.5/V25-LOGIN-SESSIONS.md)。下方旧授权/阻塞描述为历史阶段。

> 2026-09-19 LIVE25 阶段目标：推进至可做真实账号与中转站验证并交付报告。隔离本地栈与严格入口已准备，TvT 真实单张及重启保留已验；账号服务拒绝所提供凭据，正向账号/云链路待核对。准备阶段不替代完整 v2.5、平台安装包或生产发布验收；[结果与边界](../../../docs/v2.5/V25-LIVE-ACCEPTANCE-2026-09-19.md)。

> 2026-09-07 文档核对：后续执行入口见 [迁移路线图](../../../docs/v2.5/V25-MIGRATION-ROADMAP.md)、[goal 任务卡](../../../docs/v2.5/V25-MIGRATION-GOALS.md)与[测试手册](../../../docs/v2.5/V25-MIGRATION-TESTING.md)。B8 后用户已启动剩余开发 goal，B9 实施与实际门禁见 [开发执行记录](../../../docs/v2.5/V25-DEVELOPMENT-LOG.md)；费用裁决见 [费用边界](../../../docs/v2.5/V25-SPEND-BOUNDARIES.md)。代码仍含未提交增量，Cloud MCP 保持只读；管理员已获后续 goal 授权，仅在整包验收后启动。

## 1. 问题定义
- **项目目标**：把 v2.1 的可用产品能力迁入 v2.5 的四端复用架构，并完成可验收的行为与视觉对齐。
- **目标用户**：使用 Musefold 进行 AI 生图、提示词管理、历史管理、账号/连接管理和云同步的个人创作者。
- **核心价值**：用户在 Windows/macOS 桌面、PC Web 和移动 Web 上获得同一套主要工作流，迁移后既保留 v2.1 的功能语义，也获得 v2.5 的共享 features、契约和数据可靠性。

## 2. 假设与待确认

### 2.1 已确认事实
- 2026-09-20 用户在知悉需修改zhaozhaoyue.top的New API账号服务源码、保留扩展维护及先备份后升级方式后，明确回复“好的，拿去修改吧”。此前部署授权前置已满足：只升级账号服务，不切换Musefold v1.1业务站点，不改积分/模型价格/作品，保留SESSION_SECRET和有效旧会话；按本地已验镜像、兼容增量迁移及备份检查执行，部署与实网成功须另有证据。
- 2026-09-20 本轮用户明确要求现在实现登录会话机制，并更新目标为完成2.5开发、全量非Windows测试后Git提交。推翻“只完成设计”的阶段边界。登录满额须弹出固定选择清理确认，成功后继续同一次登录；容量竞争仍不足则重新读取并再次提示，不静默追加撤销对象。实现沿现有账号契约、共享UI与双Gateway，并维护固定上游版本的可重现会话协议补丁；隔离测试后才部署目标账号服务，其他生产服务/费用/账号保持不变。Windows真机仍延期。
- 2026-09-20 12:36上海接续结果：目标账号50条旧会话已撤销，新登录/本人会话列表/主动退出/旧双凭据401实测通过，两条验证会话均已释放；其他418条会话摘要及目标财务字段不变。原AUTH_SESSION_LIMIT外部阻塞已解除。自动释放、满额安全挑战与设备管理的设计见docs/v2.5/V25-LOGIN-SESSIONS.md，尚未实现或部署；原真实同步/收费方案未重跑，完整迁移保持未完成。下方外部条件段落记录释放前状态，不覆盖本条最新事实。
- 2026-09-20追加会话治理：用户授权SSH进入账号服务主机，强制释放xiaomiao阻塞登录的会话，并要求自动释放、登录容量处理、已登录设备展示和用户手动选择释放。公开DNS核实`.com`不存在，实际权威`.top`为45.207.211.136，已有musefold-cloud SSH已连接；仅处理目标账号，不改积分、生成/费用记录或其他用户。推翻此前“只能等待用户手动释放”的外部阻塞假设；最小路径先核对线上原生会话实现并有界撤销，再沿现有契约/Gateway补会话生命周期，禁止暴露Bearer/refresh或静默撤销活跃设备。Windows延期与其他验收边界不变。
- 2026-09-20追加收费授权：用户允许使用xiaomiao账号现有全部积分完成全面、真实、完整的验收，未设置额外总费用上限；不自动充值，不把授权当作必须消耗全部余额。账号模式生图、方案文本/图片调用可按必要覆盖执行，记录真实余额、价格、调用与结果；外部BYOK余额不由该账号积分授权自动扩大。
- 2026-09-20 用户调整顺序：Windows App 真机开发/测试验收延期，本轮先完成其余 v2.5 开发和测试。Windows 原生控件、原生模块、安装/升级、签名及产物验收单独保留，不能记为通过，也不再作为其他平台本轮推进的前置阻塞；共享代码仍须保持 Windows 边界。macOS、PC Web、Mobile Web、API/Worker、数据迁移与账号/同步/设计方案验收条件不降低。生产发布及新增真实收费不由此获得额外授权。
- 当前分支已有连续的 v2.5 迁移提交，以及一批未提交的 API、Worker、SQLite、IPC、共享 features、双端宿主、文档和 E2E 改动；这些改动由前一轮代理完成，必须纳入本包审计，不得覆盖。
- v2.5 的源码、自动化测试和 `docs/v2.5/V25-UI-SPEC.md` 是当前权威；v2.1 文档只用于识别旧版行为。
- 主要迁移面包括应用壳、工作台/生图、提示词库、生成历史、设计方案、设置、账号/AI 连接、云同步、桌面本地数据库和 Web/桌面双宿主。
- 桌宠是冻结面；豆包网页模式的主进程语义保留、渲染层不迁移，但产品界面必须保留豆包入口。
- 设计方案是 v2.1 用户功能，必须完成共享 UI、桌面和 Web 数据链路迁移；v2.5 文档中的“暂缓”记录由本包更新。
- 2026-09-20 用户追加：账号模式已新增云端模型；登录后用户可以切换模型，价格以云端提供的数据为准。该要求取代账号模式只暴露固定 `musefold-image-pro` 的旧假设；既有身份、费用和幂等安全条件不降低。

### 2.2 关键假设
- v2.1 的核心用户行为由当前仓库历史代码、`v2.5-baseline` 和现有测试足以判定；无法从历史实现证明的细节以当前 v2.5 契约和 UI 规范为准。
- 现有未提交改动属于同一迁移目标；恢复后如发现明显无关主题，按独立边界分流，不混入本包提交。
- 四端共享 `packages/features`，宿主只负责路由、壳和 gateway 注入；不为单端复制产品 UI。
- “基本保持一致”优先保持信息架构、文案、动作和状态，允许只改善可访问性、响应式布局和交互可发现性，并将有意差异记录在 v2.5 UI 规范。

### 2.3 待确认问题
- 无（已确认；产品范围和设计选择无需新增澄清）。桌宠和豆包内部渲染是非目标，设计方案必须迁移。

外部验收条件（不是需求待确认）：目标账号此前50条阻塞会话已按授权释放，新增账号协议也已按明确授权上线并通过实网协议验证，原“上游协议未部署”前置已解除。真实双客户端同步/方案付费链路尚须继续，不用合成账号或协议烟测代替；Musefold v1.1公共站点未切换。收费授权已获得，本次账号部署新增收费调用0，不充值。Windows App已明确延期，不再重试SSH或阻挡其余平台验证；正式签名安装、远端CI/TLS/两个独立远程MCP客户端及完整产品发布条件另外保留，不能由本机受控服务和ad-hoc包替代。最新证据见[会话部署记录](../../../docs/v2.5/V25-LOGIN-SESSIONS.md#10-账号服务生产部署与实网验收2026-09-20)。

### 2.4 可选解释与取舍
- 当前选择：采用一个整包承接当前连续迁移改动，而不是拆成多个历史补录包 -> 所有改动围绕 v2.1 到 v2.5 的同一交付目标，拆分会割裂契约、宿主和 E2E 的联合验收。
- 当前选择：先恢复上一轮代理成果再做增量修复 -> 保留已有实现和测试证据，避免重复开发或误删用户工作。

## 3. 功能范围

### 3.1 核心功能（MVP）
- [ ] 四端应用壳与导航：桌面常驻侧栏、Web 桌面/移动响应式壳、会话列表、账号 footer、设置入口和豆包入口。
- [ ] 工作台与生图：会话创建/切换/重命名/置顶/归档/删除、提示词 Composer、比例/质量/负面词/Provider、参考素材、生成状态时间线、重试/取消/删除。
- [ ] 提示词库与历史：列表、搜索/标签/收藏/评分/编辑/复制/回收站，以及生成历史筛选、详情、删除和状态展示。
- [ ] 账号与连接：登录/登出、账号额度、AI 连接列表/切换/配置入口、云同步状态和归档会话恢复闭环。
- [x] 账号模式云端模型与价格：读取当前账号可用模型，选择模型贯穿预估/确认/实际请求；价格及适用账号分组倍率由云端提供，不用本地硬编码价格或把缺失定价当免费。 09-20实现及API/Worker/Web/Electron本机联合验收通过；真实账号和全平台发布仍属于账号与完整迁移验收未闭合项。
- [ ] 数据与服务链路：contracts 单一事实源、PostgreSQL/SQLite 增量迁移、API/Worker 生图状态机、桌面 v25 单通道 IPC、Web API gateway。
- [ ] 迁移验收：关键单测、集成测试、Web/Electron E2E、移动/桌面视觉基线和仓库门禁全部达到可判定状态。

### 3.2 扩展功能
- 允许在不改变功能语义的前提下改善响应式布局、键盘可访问性、错误/加载态和动作可发现性，并同步记录有意差异。

### 3.3 不在范围内
- 桌宠 renderer、桌宠主进程域和专用 preload 的迁移、重构或视觉改造。
- 豆包网页模式内部渲染、登录管理重构和专属限制提示迁移；只保留现有主进程能力和 v2.5 界面入口。
- Skill runtime 对话、独立 GitHub Skill 导入 UI（设计方案创建中的来源确认属于本轮）、通用分享/导入、命令面板完整实现、微调链和热更新控制面；Design Schemes 专属 `.musefold.design` import/export 明确属于本轮范围。
- 不为单端复制 `packages/features`，不做与迁移无关的目录重排、依赖升级或产品新功能。

## 4. 最小实现路径
- 恢复前一轮代理改动，在同一 integration branch 上以源码、测试和 v2.5 文档进行差距审计。
- 先修 contracts/schema/gateway 和服务端主链路，再修桌面 IPC 与共享 features，最后验证 Web/Electron 宿主和 E2E。
- 让已有测试成为每个 ownership 的完成证据；只补缺失测试或修复真实失败，不以删测试换取通过。
- 暂不引入新的状态管理、跨端抽象、服务端接口版本或配置化框架；沿用现有 TanStack Query、MusefoldGateway、zod、Drizzle 和 v25 单通道桥。
- 不迁移冻结/暂缓域的渲染实现，避免把一次版本迁移扩展成产品重做。

## 5. 技术决策
- 技术栈：沿用 Next.js 16、Electron 43、Hono、PostgreSQL/Drizzle、SQLite/Drizzle、TanStack Query、zod、Playwright 和 Vitest。
- 本轮允许改动：当前迁移涉及的 contracts、platform、db/desktop-db/core、api/worker、desktop v25 IPC/preload/host、web-next、features/ui、相关测试与 v2.5 证据文档。
- 本轮不应触碰：`apps/desktop/src/pet`、桌宠主进程域、桌宠 preload，以及与本包无关的旧版本冻结代码。
- Git integration branch：`spec/2026-09-01_migrate-v21-to-v25`，从 `main` 创建；上一轮工作已先保存到 `stash@{0}`，恢复后作为本包输入。
- 账号多模型兼容：新选择使用显式model并经当前账号云目录/有效价格与服务端图像传输策略核验；省略model的旧客户端/冻结请求保持原默认及摘要语义，不重写历史费用。账号连接仍代表稳定身份，不靠修改连接行的默认模型实现选择；重试保留原冻结模型。上游明确image-generation端点自动进入能力判定，现部署只标openai的三个已知图像别名单独兼容，聊天端点不普遍授予图像能力；价格不做本地硬编码。
- 模型偏好仅本设备保存model标识，以云端目录的apiIssuer/principalId/payer隔离，目录价格和凭据不落此存储；新提交前重读云价，变化则保留输入并要求重新明确发送。按次价不冒称多张总价，用量费率不冒充实际费用。普通生成、方案图像运行、历史重试分别核对自己的冻结模型接缝，不以其中一个通过代替全链。

### 5.4 编排策略
- route: `build`
- immediate blocker: 恢复上一轮改动后确认真实失败，避免基于干净基线重复开发。
- ownership: 主线程负责恢复、合流、任务包、公共入口、最终修复与验收；sidecar 按契约数据、服务端、桌面桥、共享 UI、Web/E2E 和质量门禁提供实现或 findings。
- waiting strategy: 主线程先执行基线验证和独立修复，不因单个审计 sidecar 尚未返回而停工。
- verification gate: worker 局部通过不等于验收；统一以主线程的 `pnpm run check`、双端 E2E 和 Spec 校验为准。

## 6. 成功标准与验证方式
- v2.1 核心用户路径在 v2.5 双宿主可用 -> verify: 共享 features 单测、Web/Electron E2E 覆盖壳、工作台、提示词库、历史、设置、账号/连接和同步路径。
- 四端使用同一页面模块且 gateway 行为等价 -> verify: dependency boundary、contracts/api-client/IPC/platform 测试和 Web/桌面对应场景通过。
- 数据迁移不丢失且生图账本状态可收敛 -> verify: desktop-db/db migration tests、API/Worker/core sync/generation tests 和集成测试通过。
- UI 与 v2.1 基本一致且明确排除桌宠/豆包内部 -> verify: `V25-UI-SPEC.md` 对照、视觉快照、冻结面检查和豆包入口 E2E 证据。
- 仓库达到可交付状态 -> verify: `pnpm run check`、`pnpm run test:e2e` 及桌面 Electron E2E（按环境可用性记录真实结果）。
- 账号模式新增模型可选择且价格可信 -> verify: 契约、云模型/定价读取、API入队与Worker实际发送、桌面IPC和共享产品面联合测试；至少两种不同云价格、云价格更新、失效/未知模型、切换账号、离线/缺失价格、请求模型与展示一致；费用回执保留云端权威与原重试冻结语义。沿现有账号/Gateway链路增量接入，不增加第二套本地价格事实源，不扩展豆包或Cloud MCP写权限。

### 6.1 行为成效指标
- 当前源码的统一门禁可复现通过 -> verify: `pnpm run check` 退出码 0，报告绑定实际版本/工作树和范围；不把历史固定用例数量作为以后实现的硬断言。
- 三形态产品、真实数据库/Worker、迁移/恢复及目标产物有匹配证据 -> verify: 按 `docs/v2.5/V25-MIGRATION-TESTING.md` T0–T5 分阶段执行，跳过与阻塞分列，mock 不替代真实服务、源码构建不替代安装包。
- 最新批次为 2026-09-09 B33；计数与限制见测试手册 §5.26。早期完整 E2E 98/1 fail/5 skip 和 Electron 33/1 fail/1 skip 仅为历史记录，不代表 B8 当前失败；manifest 的报告绑定仍需 G-BASE 补齐。
- 数据/同步/方案路径不泄露凭据或私有路径 -> verify: disposable userData、HTTP/MCP、导出和真实产物分别按对应卡扫描，保留 synthetic/live 与原生/模拟的范围说明。

## 7. 风险与约束
- 风险点：现有改动规模大且未提交 -> 缓解措施：已用 stash 保存并在本包分支恢复，按 ownership 审计，提交只纳入本包边界。
- 风险点：旧版行为与新规范可能冲突 -> 缓解措施：源码/测试优先，规范差异写入 UI 规范，不凭视觉猜测改行为。
- 风险点：真实 PostgreSQL、Electron 或外部认证环境不可用 -> 缓解措施：先运行可离线的单测/构建，再记录具体外部阻塞；不把预检结果写成已发布。

## 8. B13 执行范围补充（2026-09-08）

沿用已授权的剩余开发总目标，先完成费用子阶段 SP-P2/P3。B12 的账号恢复证据仍有效；本批新增独立执行回执、稳定付款绑定、三入口入队授权与 worker 最后发送验证。数据库共享事务层供 API/worker 使用，依赖 contracts；不新增平行支付服务、签名 grant 或管理员审批用户花费。

新增兼容性假设：历史行没有足够信息证明当前付款身份，按 `legacy_unbound` 留存；旧 `runKind=retry` 不能区分普通 create 的参数与专用 retry 操作，歧义来源记 `legacy_unknown` 并保留查询/冲突语义。已硬删的旧幂等键无法追溯。推翻“生成历史行足以长期去重”和“worker 临发送取 latest key 即可”的实现假设，最小路径变为不可删除回执和最终事务绑定，但不改变用户主动生图的直接提交语义。

Ownership：主代理负责 contracts/platform/api-client、部署配置、任务文档与统一证据；cloud_gap_audit 负责 API 三入口/清理及其测试；testing_document 负责 DB 0011/schema/共享校验与摘要；runtime_release_audit 负责 worker/gateway/retention 及故障测试。独立单测可并行，PG 集成、全量 check 与 E2E 按主代理调度串行。阶段未验收前不把 B12 结果移作 B13 通过证据。


## 9. B14：桌面托管图像准备与同源下载

B13 已取得本机实现与合成验收，下一阶段按 SP-P4-A..F 接桌面云执行。首个独立子产物是经过所有者校验的 API 同源资产 content GET：现有302签名默认使用容器内部minio地址，桌面不可达；客户端不能改签名host或把BA bearer转发S3。新GET只接受assetId，经受管objectKey读取、完整期限与30MiB上限/解码、元数据前后核对，保留302既有用户路径。

旧备份安全必须分别证明“同一key不会重复发送”和“不同新key不会利用回退预算自动授权”。稳定remote key不包含authEpoch/凭据版本/当前输入hash，也不能从恢复后新生成的local job UUID派生；如果旧namespace/关联不能证明，固定只核对。恢复整data.db会丢unknown预算，实际托管发送启用前还须不随业务备份回退的保护或恢复受限状态；只做key映射不能宣称预算恢复完成。

主代理负责范围/文档/合流；API代理独占同源下载实现与PG测试；worker维持B13冻结。先局部验证、再API完整回归、最后统一check，PG与全仓构建串行。Cloud MCP维持只读，管理员仍在全部v2.5闭合之后。


## 10. B15：防回退基础先行

B14源码清单1247项无漂移。按费用边界§10.10.2实现无自动启用的checkpoint与主进程锚协调基础：锚prepare落稳后，同步SQLite CAS提交业务修改和checkpoint，再提交锚；首次发送必须等待全部成功。恢复旧DB或无法核对时只读；pending恰好匹配DB目标可补锚，但不得自动补发原调用。独立锚不复制业务账本，无法防止DB与锚一起回退。本批不改变现有BYOK、用户备份恢复或预算路径；后续明确接线才能启用端到端保护，整目标仍保留完整P4及其余迁移。

## 11. B16：正式恢复与托管宿主生命周期

B15 最终1258项源码复核无漂移。当前 restoreBackup 在 closeDb 后仍允许 getDb/initDb 惰性重开恢复库；本批将正式恢复变为本进程不可重新打开业务 DB 的状态，恢复开始使旧生命周期 token 失效，进入隔离后的成功或失败均要求真实重启。文件替换前排空托管操作并停止同步，保留不参与普通prune的安全备份，再持久写锚的restore_pending。未启用托管的旧用户不自动生成锚/namespace；损坏锚不自动重建。真实safeStorage装配必须拒绝不可用状态，测试明文fallback不能成为正式锚的加密器。

B16 审查增量：user_version 不随 Drizzle 增量变化，正式恢复额外验证已知迁移链前缀；普通安全副本从十份保留中排除，共享备份说明同步更新。恢复隔离失败关闭确认框并提供显式重启，不再无意义重试；未修改库前的坏备份保持原可重试路径。新增共享界面测试，因此最终门禁扩大为完整三形态 E2E。


## 12. B17：托管 G 持久关联与预算回执核心

在现有 B15/B16 防回退和正式恢复支撑之上实现受管 SQLite 0009：托管请求从登记/确认阶段就独立标记，关联固定 caller key、namespace、完整非秘密 binding、authEpoch、原始云请求、local call/run 与远端 receipt。首次发送前协调锚和 SQLite 提交；发送许可只存在于创建该请求的活跃协调器，重建实例/重启不能由 unclaimed 行恢复许可，404 仍只查询。

新增 receipt revision/身份/原 run 校验、单调更新和原预算月份记账，终态回执与审计同事务。通用 Automation recover/claim/finish 不接管托管请求；原 BYOK 请求继续走原逻辑。API/PG 与本地进程联测采用合成主体和受控 HTTP，真实进程 SIGKILL 后只读查询；无付费上游。

本批为 P4-D3/E1 的核心支撑，不代表全部现有预算入口已接入防回退协调；共享预算设置、显式托管连接/启用、prepare/确认卡、主进程正式执行 port、下载落盘、匹配恢复核对和 P5/P6 仍需后续完成。生产宿主不能在这些接缝合流前开放托管发送。整包与管理员前置仍未闭合。

## 13. B18：账号捕获、受限 HTTP 与 core 远端执行接缝

主进程捕获已验证的 issuer/principal/authEpoch/session；账号变更在等待订阅者之前同步使旧捕获失效，每次异步准备和网络回包后再次核对持久会话。受限 HTTP 客户端只构造捕获 issuer 下的内部路径，禁止重定向，限制 JSON 字节和完整读取期限；bearer 只留在主进程，绝不充作 Provider API key。

客户端先读取当前 binding，再消费 B17 活跃账本的单次 claim，发送固定请求和原 key；提交失败或 404 保留未知并只允许查询。原 key 回执经账本单调更新，result 只查询原 run。工厂接 B16 托管操作排空，回调结束后旧 client/ledger 不可再用。core 新增不含 key 的 transport 接口，拒绝与旧 execution 混用或套用普通本地 retry；异步返回后再次检查账号/数据库访问资格才写入结果。启动时的旧本地终态补记也排除托管关联，避免错误结束请求或初始化失败。

本批仍无正式产品调用方、取消意图持久化或资产下载/入账实现；它完成 HTTP 与 core 接缝，后续须接完共享预算保护、显式连接/准备确认、恢复核对及 G 全链，才能开放用户托管发送。归档条目上限回归的 ZIP fixture 改用无压缩存储，保留真实 1025 条目和原拒绝断言，压缩比攻击另有独立测试；不放宽产品限制或测试时限。

## 14. B19：共享预算写入与宿主异步结算

在B15/B17的检查点事务内统一授予受管写资格，覆盖策略、兼容用量和没有远端关联的旧托管请求；外部BYOK保持独立。主进程设置/结算经guard与正式恢复work scope，未启用写与enable同锁，不创建namespace；合法旧迁移初始化保留，非法异步回调回滚。重复finish共用Promise并保留预留直到落稳，G/R/S调用等待结算，拒绝或清理失败不放行新自动预算。

真实SQLite/文件与实际Electron IPC/safeStorage/新PID验收，仅覆盖本片。完整用户启用需同时核对/排空旧持久在途与legacy内存预留，随后接显式云连接、冻结准备确认、G执行/取消/下载/恢复；不把拒绝旧写操作当作其恢复完成，不开放管理员阶段。

## 15. B20：启用前持久账本检查

core启用前拒绝整库旧托管非终态、held/unknown、未结束托管调用或残留远端关联；异步锚落盘后在SQLite commit内复查。拒绝不自动结算或删除身份；晚到竞争保留pending/query_only。已结束历史和BYOK兼容。宿主legacy内存预留/操作准入排空、可见启用与恢复、G完整产品链仍按P4联合收口，当前不开放管理员。


## 16. B21：桌面账号云图像首条产品链

联合接显式accountCloud域/共享设置卡、主进程一次性review与legacy预算准入屏障；普通G由固定身份与持久账本提交一次，取消意图落盘、受控资产下载与本机幂等结果投影共用原任务恢复。账号或数据库变化不沿用旧发送资格，未知状态不改成本为0。普通工作台发送已经代表交互授权，不引入Automation二次确认。当前参考图/Prompt引用及微调在发送前拒绝，BYOK和冻结面保持。

本批验收为真实SQLite/HTTP与实际Electron新PID，加密使用正式系统适配；服务器/Provider结果仍为受控fixture。P4真实服务/全部故障与恢复交互继续按P4-R1..R4，P5/P6及其他迁移卡仍开放；当前不开发管理员。详细实际结果以测试手册§5.14为准。

## 17. B22：原任务恢复分页与独立结果状态

在 B21 云连接/持久请求之上，当前 owner 稳定分页包含终态，旧未映射托管记录单独只读诊断。云执行成功、费用结清、本机完整素材分别表达；purge 不永远显示生成中，缺文件只恢复原受控素材并保留资产/费用身份，不创建新生图。就地契约/IPC/SQLite/HTTP/组件、真实 Electron 新 PID 验证；现有真 Hono/PG 回执核心单独回归。正式主进程产品→真实服务合流及完整宿主故障按 P4-R2/R3 后续验，不据此关闭 P4 或管理员前置。实际结果见测试手册 §5.15。

## 18. B23：真实服务产品合流

在隔离测试进程接正式Hono/PG17/队列/worker和B21/B22正式桌面客户端/SQLite/锚；身份认证/status、Provider及S3合成，真实Electron通过四图/丢回包/新PID/缺图恢复验证。修复bound未发送取消金额记0及旧not_sent null兼容，legacy和claimed未知不归零。CI数据库job新增该联合测试但本轮不声明远程CI已跑。完整宿主强杀/恢复确认、重试兼容、P5/P6与其他迁移/发布及管理员前置仍开放，实际证据见测试手册§5.16。

## 19. B24：正式强杀与真实安全备份确认首批矩阵

测试通过正式Electron注入暂停后SIGKILL，不加产品后门；实际Hono/PG/worker/SQLite/OS密文验证原key恢复和素材不重复；过旧/匹配安全备份、显式UI确认和失效review按真实任务测试。修复仅对durable unclaimed取消写入本地0费用，已领取未知不改。实际结果见测试手册§5.17；完整R3仍需确认执行中竞争、实际任务缺损锚和双持久化中点，接路线图§6.12，管理员仍依赖全部迁移闭合。

B25补充证据：本机P4-R3-C1..C4确认故障矩阵已验收；check35/35与完整三形态274passed/7skipped/0failed/flaky。统一报告`tests/v25/.results/b25/validation-summary.json`，源码1306项摘要`7bd7b59729519d834aad5540ef7487aa6486a9674a9a555c98f8e0be8c1a2f6c`。下一步P4-R4；原生Windows、其余迁移/发布及整包验收保持未完成，管理员未启动。


B26 阶段证据：显式重试主链验证通过，check35/35、联合171、API完整集成220、三形态276passed/7skipped/0failed/flaky。源码1307项摘要`5342cbb0df26f103ba89e49887fac42cb583ca1a854fd7e9faa791927d42c9dc`，统一报告`tests/v25/.results/b26/validation-summary.json`。B26 原任务仍未勾选，剩余R4-C1..C5见路线图§6.14：交互/剩余故障/legacy/BYOK/R/S完整矩阵不由同key专项代替；父级与整包仍部分完成，管理员未启动。


B27 阶段证据：R4-C1 工作台/历史三个手动入口的共享交互通过本机验收，check 35/35，组件专项 75、正式服务 15，完整三形态 278passed/7skipped/0failed/flaky。源码1309项摘要`8d7de8a9c596d518a2b567bafb703482a826f7b340596581e29ec1919e4e4f0c`，统一报告`tests/v25/.results/b27/validation-summary.json`。只关闭B27；B26/R4/P4/整包保持开放，兑换后续发的独立错误/并发缺口已登记C4。下一步路线图§6.15 C2→C3/C4→C5；管理员未启动。


B28 阶段证据：R4-C2 本机17项重试故障/恢复验收通过；check 35/35，完整三形态 295passed/7skipped/0failed/flaky。源码1312项摘要`1bae587d59ba8c56efa81e492cd78f1e23cb0a4ee798a658f5cf252c4eaed192`，统一报告`tests/v25/.results/b28/validation-summary.json`。仅关闭B28本机切片，B26/R4/P4/整包保持开放；下一步路线图§6.16 C3实际旧入口→C4 BYOK/R/S/兑换续发→C5，再原P5/P6及云Agent/包/GC/迁移/发布；管理员未启动。


B29 阶段证据：兑换恢复反馈/并发/账号隔离和桌面官方额度引导的本机切片通过；check 35/35，组件62通过，旧入口基线24通过，界面专项5通过，完整三形态300passed/7skipped/0failed/flaky。源码1315项摘要`da51f777c45d0f34758d9999faad1caad34c1c6208f938e48950ea0e376aa8a2`，统一报告`tests/v25/.results/b29/validation-summary.json`。首次void回调、Web fixture错误码和桌面映射失败留证；只关闭B29，C3真实旧入口和C4 BYOK/默认连接/R/S、C5仍待，B26/R4/P4/整包保持开放；管理员未启动。


B30 阶段证据：旧账号未绑定本地发送与默认模型快照缺陷已修；实际 G/R/S/BYOK 子矩阵和三类持久未结启用阻断已验。check 35/35，完整三形态305passed/7skipped/0failed/flaky；源码1321项摘要`1ab821e097e6d54ddf2ffb3245fa31774f9c607ea1b48270e3e7b9ac4c91ef19`，统一报告`tests/v25/.results/b30/validation-summary.json`。首次缺陷/fixture失败及一次既有进程超时均留证。B30总任务、C3/C4剩余条件、B26/R4/P4/整包不关闭，历史unknown模型分类仍待；接续路线图§6.18，管理员未启动。


B31 阶段证据：本地/IPC 共用连接规则、云前后自备兼容与实际 CLI/MCP 本地 G/R/S 已验。check 35/35，单元25项、专项7项，完整三形态311passed/7skipped/0failed/flaky；源码1325项摘要`40aba6dcc793e5f053194a9ee07f2c10107291b5a88dbd4d965814131915335e`，统一报告`tests/v25/.results/b31/validation-summary.json`。四个真实管理缺陷和首次CLI命令顺序fixture失败已留证。仅关闭B31，历史模型缺失/C3/C5、B30/B26/R4/P4/整包保持开放；接续路线图§6.19，管理员未启动。


B32 阶段证据：旧本地模型缺失在 core/IPC 发送前明确拒绝，历史反馈与连接核对后工作台新意图已验，原记录/费用不变。check35/35，core/IPC44项、账号测试43项；完整三形态312passed/7skipped/0failed/flaky。源码1327项摘要`8ef7aa3d4db46ec9bd8134df1275e456b7db64c52108df589e55533f8ed89034`，报告`tests/v25/.results/b32/validation-summary.json`。首次真实缺陷及fixture/注册瞬态断言错误留证；仅关闭B32，C3/C5、B30/B26/R4/P4/整包开放，接续路线图§6.20，管理员未启动。


B33 阶段证据：守护准入关闭、在途取消排空、关库/owner顺序及实际桌面接管已验；旧进程提前删锁后延迟退出/超时仍活以真实子进程fixture覆盖。就地6项、云回执独立复核16项；check35/35，完整三形态313passed/7skipped/0failed/flaky。源码1329项摘要`f4b05a835c6b2fe676530ac0f6d25c3bc7c699175995d2ac024fc7e1e6a708ad`，报告`tests/v25/.results/b33/validation-summary.json`。首次缺陷、诊断格式错误、既有回执等待超时留证；不证明历史安装包/Windows/真实账单，原C3/C5/B30/B26/R4/P4/整包继续开放，接续路线图§6.21。


B66当前进展：原桌面/core修复62b1f744…完整Electron166P/2外部S/0F；新增空日志水位与pull/裁剪一致性修复，df6478b1…九项真实两设备及就地联合35P、check36/36和源码扫描通过。后续六处仅后端/测试变化与原Electron分开归属；完整API556P/1专用小时S、worker93P/0S与10份本轮API归档扫描通过，tests/v25/.results/b66/acceptance.json逐条验收，仅关闭B66/D03.5；D03父卡、全迁移与管理员前置继续开放。


B67/D03.6 已验收：真实 Electron 跨账号、同步同意、会话失效恢复及本机方案隔离通过。继续 D03.2/3 旧库数据等价与备份故障补验；真实脱敏旧库、原生交付、原 E 与正式发布条件保持开放，管理员尚未开始。 本批只改测试设施，逐条证据为 tests/v25/.results/b67/acceptance.json；不据单卡关闭父级迁移或管理员前置。


B68/D03.2–3 已验收：合成旧 SQLite 的逐字段保留、独立方案库升级、真实备份失败和事务回滚恢复通过。当前统一检查通过；继续原 E 的费用/Worker 完整矩阵与数据清理。真实 v2.1 脱敏旧库、原生四端交付、各平台发布及生产回滚仍待，管理员尚未开始。 当前证据：tests/v25/.results/b68/acceptance.json；无生产迁移代码改动，首败和条件skip保留。


B69/原E-W已按W01/W02运行可靠性范围验收：完整Worker集成99P/0F/0S，含18项真实进程和2项正式Linux arm64容器测试；旧epoch不得改写结果，已发送/已认领任务不自动重发。继续费用兼容与完整retention/GC、真实旧库/原生交付和正式发布；远端CI及生产回滚仍待，管理员尚未开始。 逐条核对及源版本见tests/v25/.results/b69/acceptance.json；整包前置不随本卡关闭。


B70进展（2026-09-13）：B70已修复提示词恢复与定期/手动永久删除的真实PG竞争：恢复不被误删、清空只处理锁定集合，同步日志和条数准确。当前完整API562P/1专用小时S、Worker100P/0S、check36/36及源码/20份留存合法ZIP扫描通过。只关闭本片；继续Desktop清空500条上限、完整数据清理/费用与正式交付，管理员尚未开始。 具体证据见docs/v2.5/V25-MIGRATION-TESTING.md §5.69；完整迁移验收保持开放。


B71进展（2026-09-13）：B71已完成Desktop提示词清空修复：501条一次清完、全部事务失败回滚、同步墓碑与其他workspace保护、真实Electron新PID持久性通过。当前check36/36、完整Electron170P/0F/2S/0flaky及源码/实际归档扫描通过。继续B72云端分页遗漏修复，再接Desktop查询与回收站筛选、完整数据清理/费用及正式交付；管理员未开始。 只关闭本片，完整迁移验收保持开放。


B72进展（2026-09-13）：B72已完成云端提示词置顶/微秒/Unicode分页修复与严格游标校验，当前完整API604P/1专用小时S、check36/36及源码/10份实际ZIP扫描通过。接B73桌面SQL分页、显式工作区回读与双宿主回收站查询；费用、完整生命周期和正式交付仍开放，管理员未开始。 只关闭本片，完整迁移验收保持开放。

B73已验（2026-09-13）：当前check、API52P、完整E2E476P/7S及源码/实际归档扫描通过，原七项验收留存。接B74 Desktop分类事务与同步；云硬删除、完整生命周期/费用、真实旧库/原生/发布与管理员前置仍开放，不勾选完整MVP。


### D02.5 原生受管文件系统接缝（2026-09-14）

已用真实跨进程目录替换证明路径检查后仍可误删，因此允许增加 Node-API 8 的 managed-fs 叶子包，仅 Desktop主进程/core 消费，禁止渲染层/云使用；句柄相对 IO 与已校验字节导入代替重复路径检查。该包不新增实体、公共IPC或技术栈宿主。每目标平台编译、句柄生命周期、原件保护、实际安装包和扫描是原GC验收组成；Windows目前只有候选源码，管理员与完整GC前置不关闭。实际失败/修复/门禁见测试续篇§5.82。
