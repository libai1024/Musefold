# 05. 生成历史

## 1. 页面定位

生成历史是结果资产的回看、筛选、比较和再利用中心。它要同时满足：

- 快速扫描大量生成记录。
- 找到某次生成的完整参数。
- 打开大图。
- 继续微调。
- 保存为提示词。
- 加入设计方案。
- 查看单次生成元数据并管理历史文件。

边界：生成历史不承载聚合成本、渠道用量或趋势看板。上述内容统一进入“设置 / 使用统计”；历史详情仍可展示单条记录在生成当时写入的积分元数据。

## 2. ZCode 对比截图

![ZCode 已有任务和右侧上下文面板](./references/zcode/zcode-mainview-dark.jpeg)

继承：

- 中央内容与右侧 Inspector 分栏。
- 右侧面板不覆盖中央内容。
- 顶部工具条承载页面级操作。

替换：

- ZCode 的任务和工具输出换成图像记录。
- 右侧文件面板换成生成详情。
- 任务状态换成生成状态、模型、成本和保存状态。

## 3. 页面布局

```text
┌──────────────────────────────────────────────────────────────┐
│ 生成历史      246 条      [筛选 2] [磁盘占用] [清理] [Inspector] │
├────────────────────────────────────┬─────────────────────────┤
│ Filter Bar（按需展开）              │                         │
│                                    │                         │
│ History List                       │ Detail Inspector        │
│ ┌────┐ prompt / status / time      │ image                   │
│ │img │                              │ prompt                  │
│ └────┘                              │ params                  │
│                                    │ actions                 │
└────────────────────────────────────┴─────────────────────────┘
```

## 4. Light / Dark 页面配色

| 区域 | Light | Dark |
| --- | --- | --- |
| 页面背景 | `--bg-work #fafaf8` | `--bg-work #1d1f22` |
| Filter Bar | `--bg-elevated #fff` | `--bg-elevated #25272a` |
| History Row | transparent/elevated | transparent/`#25272a` |
| Selected Row | accent soft + accent border | accent soft + accent border |
| Inspector | `--bg-sidebar` or elevated | `--bg-sidebar` |
| Image frame | white/media surface | `#2a2c30` |
| Danger cleanup | danger soft | danger soft |

## 5. Page Header

组件：

- 标题与记录数量。
- 筛选展开/收起，已有条件时显示数量。
- 刷新。
- 磁盘占用。
- 清理菜单。
- Inspector 展开/收起。

规格：

- 高度 48-56px。
- 标题 18px / 600。
- TitleBar 下方的页面控制轨高 40px，记录数在左，操作组在右；桌面宽度不换行。
- 操作组内部间距 4px，筛选面板与控制轨之间为 8px。
- 页面主动作最多一个。
- Inspector toggle 使用 icon button，必须有 tooltip。

## 6. Filter Bar

默认状态为收起。Page Header 中保留 28px 高的“筛选”按钮；点击后，Filter Bar 在 Header 下方、History List 上方展开。再次点击收起，现有筛选条件继续生效，不因面板卸载而清空。

筛选维度：

- 状态：成功、失败、排队、取消。
- 时间范围。
- 模型或服务商。
- 画幅。
- 来源：新设计、设计方案、提示词库。
- 是否已保存或已加入方案。

布局：

```text
收起
┌───────────────────────────────────────────────────────────┐
│ 生成历史  246 条                      [筛选 3] [其他操作] │
└───────────────────────────────────────────────────────────┘

展开
┌───────────────────────────────────────────────────────────┐
│ [搜索] [状态 ▾] [时间 ▾] [模型 ▾] [来源 ▾]   已筛选 3 项 [清除] │
└───────────────────────────────────────────────────────────┘
```

Light：Filter Bar 使用 raised surface。

Dark：使用 `--bg-elevated`，选择项用 accent soft。

筛选按钮和选择器的圆角均为 8px；数量徽标圆角 6px，不使用胶囊形堆叠。按钮未激活时为 outline，展开或已有筛选时使用 Ember soft。

## 7. History Row

```text
┌─────────────────────────────────────────────────────────────┐
│ ┌──────┐  雨天城市人像                               成功    │
│ │thumb │  4 张图 · claude · 1:1 · 12 分钟前                  │
│ └──────┘  保存为提示词 · 设计方案来源                 [⋯]   │
└─────────────────────────────────────────────────────────────┘
```

规格：

- 最小高度 80px。
- 缩略图 60x60px，圆角 8px。
- 行圆角 8px。
- 正文 12-13px。
- 状态使用图标 + 文本。
- 右侧低频动作 hover 显示。
- selected 使用 Ember soft 和 1px accent border。

动作：

- 打开详情。
- 打开 Lightbox。
- 继续微调。
- 保存为提示词。
- 加入设计方案。
- 下载。
- 删除/移入回收站。

## 8. Inspector

```text
┌────────────────────────────┐
│ [×] 生成详情                │
├────────────────────────────┤
│            [large image]   │
├────────────────────────────┤
│ 状态：已完成                │
│ 生成时间 / 模型 / 成本      │
├────────────────────────────┤
│ 提示词                      │
│ ...                         │
├────────────────────────────┤
│ 参数                        │
│ 比例 1:1 质量 ...           │
├────────────────────────────┤
│ [继续微调] [存为提示词]     │
│ [加入方案] [下载]           │
└────────────────────────────┘
```

Light：Inspector 使用 Sidebar surface，图片区域使用 white media surface。

Dark：Inspector 使用 `#1b1c1f`，图片区域使用 `#2a2c30`。

细节：

- Dock 外层占列 324px，内部详情 surface 为 320px，形成 2px 级内缩空隙。
- 详情 surface 圆角 12px，使用 1px subtle border 和 `shadow-sm`；Dock 本身不做厚重卡片阴影。
- 图片最大宽度为 Inspector 内容宽度减去 24px。
- Inspector 内 section 之间 16px。
- 内容分隔采用 section heading，不给每个字段包卡片。
- 长提示词默认显示摘要，点击展开。

## 9. Status Presentation

| 状态 | 图标/文字 | 操作 |
| --- | --- | --- |
| queued | 排队中 | 取消 |
| running | 生成中 | 停止 |
| succeeded | 已完成 | 打开/复用 |
| partial | 部分完成 | 重试失败项 |
| failed | 生成失败 | 查看错误/重试 |
| cancelled | 已取消 | 重新生成 |

颜色：

- success 使用绿色。
- warning 使用黄色/棕色。
- danger 使用红色。
- Ember 只表示当前操作和运行强调，不替代错误颜色。

## 10. Image Lightbox

Lightbox 是媒体级 Dialog：

```text
scrim
┌─────────────────────────────────────────────┐
│ [关闭]                             [下载]    │
│                                             │
│                  image                      │
│                                             │
│ [上一张]                               [下一张] │
└─────────────────────────────────────────────┘
```

规格：

- 16-20px 圆角。
- Light scrim `rgba(20,20,24,.42)`。
- Dark scrim `rgba(0,0,0,.72)`。
- 图片不强制铺满屏幕，保持原比例。
- 关闭、上一张、下一张、下载均有 icon button 名称。
- 键盘支持 Escape、Left、Right。

## 11. 统计边界

历史页只回答“发生过哪些生成、单条记录是什么、如何继续使用”。它不回答“累计花费多少、哪个渠道用得最多、模型趋势如何”。

- 不显示“成本看板”按钮、Dialog 或 Sheet。
- 不在历史工具栏放累计积分、成功率或趋势入口。
- 单条记录的模型、渠道、耗时和积分可以继续出现在 Inspector，属于可追溯元数据。
- 跨记录聚合统一由“设置 / 使用统计”承载。
- 删除历史记录会影响本地统计基数，清理确认文案需保持这一事实可见。

## 12. Disk Usage / Cleanup

- 磁盘占用显示当前使用量和可清理项。
- 清理菜单区分缓存、失败结果、回收站。
- 可恢复删除与永久清理文案不同。
- 永久清理必须进入确认 Dialog。

## 13. Empty / Error

### Empty

```text
还没有生成历史
完成第一次设计后，结果会出现在这里
[新设计]
```

### Filter Empty

```text
没有符合筛选条件的结果
[清除筛选]
```

### Error

列表错误提供刷新；单条生成错误提供重试；图片路径错误提供重新定位或隐藏损坏项。

## 14. ZCode 借鉴与差异

借鉴：

- 主内容与右侧详情分栏。
- 顶部页面级动作。
- 行 hover 承载低频操作。
- 可独立关闭右侧面板。

差异：

- 生成历史的主对象是图片，不是文本任务。
- Inspector 要支持“继续创作”，不是只读查看。
- Lightbox 是核心工作流，不是附属预览。
- 存储与清理是 Musefold 专属的历史管理信息。

## 15. 验收

- [ ] 历史列表可以快速比较图片、状态和摘要。
- [ ] Inspector 真实占列，不覆盖列表。
- [ ] 继续微调、保存提示词、加入方案在详情内可发现。
- [ ] Lightbox 具备完整键盘和按钮操作。
- [ ] 存储和清理不破坏主历史工作流，聚合统计不出现在历史页。
- [ ] Light/Dark 的图片承托面有清晰层次。
- [ ] 空状态、筛选空状态、错误和失败结果完整。

## 16. 本轮讨论确认的历史定位

生成历史是结果资产的回看、筛选、比较和再利用中心，不是单纯的时间记录页面。

```text
查看结果
→ 比较结果
→ 查看完整参数
→ 打开大图
→ 继续微调
→ 保存为提示词
→ 加入设计方案
→ 下载或删除
```

与 ZCode 的区别：

```text
ZCode：回看任务和工具执行过程
Musefold：回看图片、提示词和生成参数
```

历史页面以图片为一级对象，同时保持高密度扫描能力。

## 17. 页面空间关系

```text
固定 Sidebar
       ↓
中央 History List
       ↓
可收起 History Inspector Dock
```

```text
┌──────────────────┬────────────────────────────┬──────────────┐
│ Sidebar          │ History List               │ Inspector    │
│                  │                            │              │
│                  │ filter / rows / status     │ image        │
│                  │                            │ prompt       │
└──────────────────┴────────────────────────────┴──────────────┘
```

Inspector 打开时真实压缩列表区，关闭后列表扩展。Inspector 不覆盖列表；Lightbox 才使用媒体级 Dialog。

## 18. Light / Dark 表面决策

| 区域 | Light | Dark |
| --- | --- | --- |
| 页面背景 | `#fafaf8` | `#1d1f22` |
| Filter Bar | `#ffffff` | `#25272a` |
| History Row | transparent/white | transparent/`#25272a` |
| Selected Row | Ember soft + border | Ember soft + border |
| Inspector | `#efefec` | `#1b1c1f` |
| 图片承托面 | `#ffffff` | `#2a2c30` |
| 清理危险操作 | danger soft | danger soft |

Ember 只用于当前选中、当前进行中的任务、继续微调、主要确认和焦点，不作为历史页面的大面积背景。

## 19. Page Header 细节

```text
┌──────────────────────────────────────────────────────────────┐
│ [History] 生成历史  246 条          [磁盘占用] [清理] [Dock] │
└──────────────────────────────────────────────────────────────┘
```

组件：

- 页面标题。
- 历史记录数量。
- 刷新。
- 磁盘占用。
- 清理菜单。
- Inspector 展开/收起。

规格：

- Header 高度 48-56px。
- 标题 18px / 600。
- 数量使用 tabular figures。
- 清理进入菜单，不作为大红按钮常驻。
- Inspector toggle 使用 28px icon button。
- 页面最多一个高权重主动作。

## 20. Filter Bar 细节

```text
Header   [筛选 3 ▾]

点击展开
┌───────────────────────────────────────────────────────────┐
│ [搜索] [状态 ▾] [时间 ▾] [模型 ▾] [来源 ▾]   已筛选 3 项 [清除] │
└───────────────────────────────────────────────────────────┘
```

筛选维度：

- 状态：成功、失败、排队、取消。
- 时间范围。
- 模型或服务商。
- 画幅。
- 来源：新设计、设计方案、提示词库。
- 是否已保存。
- 是否已加入方案。

规格：

- 默认收起；触发按钮位于页面级工具栏，而不是列表内容内部。
- 触发按钮高 28px、圆角 8px，使用 Filter 图标、文字、条件数量和 Chevron。
- `aria-expanded` 与面板挂载状态一致，面板收起后内部控件不可聚焦。
- 收起只改变可见性，不清空搜索或结构化筛选；已有条件时按钮保持 Ember soft。
- Filter Bar 使用 raised surface。
- Light 使用白色表面。
- Dark 使用 `#25272a`。
- 选择器圆角 8px。
- 筛选标签圆角 6px。
- 当前筛选使用 Ember soft。
- 清除筛选使用 ghost。
- 已筛选数量使用 11px tabular figures。

展开和收起只改变 Filter Bar 自身占位，不改变 Header 高度；History List 在同一滚动容器中顺延，Inspector 不受影响。

## 21. History Row 细节

```text
┌─────────────────────────────────────────────────────────────┐
│ ┌──────┐  雨天城市人像                               成功    │
│ │thumb │  4 张图 · claude · 1:1 · 12 分钟前                  │
│ └──────┘  提示词库来源 · 已保存                       [⋯]   │
└─────────────────────────────────────────────────────────────┘
```

规格：

- 最小高度 80px。
- 缩略图 60x60px。
- 缩略图圆角 8px。
- 行圆角 8px。
- 正文 12-13px。
- 摘要最多两行。
- 状态使用图标 + 文字。
- 低频动作 hover 显示。
- selected 使用 Ember soft + 1px accent border。

行内动作：

```text
打开详情
打开 Lightbox
继续微调
保存为提示词
加入设计方案
下载
移入回收站
更多
```

历史列表主要用于扫描和选择，不应把所有动作长期显示成一串按钮。

## 22. History Inspector 细节

```text
┌────────────────────────────┐
│ [×] 生成详情                │
├────────────────────────────┤
│            [large image]   │
├────────────────────────────┤
│ 状态：已完成                │
│ 生成时间 / 模型 / 成本      │
├────────────────────────────┤
│ 提示词                      │
│ ...                         │
├────────────────────────────┤
│ 参数                        │
│ 比例 1:1 质量 ...           │
├────────────────────────────┤
│ [继续微调] [存为提示词]     │
│ [加入方案] [下载]           │
└────────────────────────────┘
```

Light：

- Inspector 使用 `#efefec`。
- 图片区域使用白色 media surface。
- 内容区使用 section heading 分隔。

Dark：

- Inspector 使用 `#1b1c1f`。
- 图片区域使用 `#2a2c30`。
- 文字参数区可以使用 `#121315` inset surface。
- 不使用大面积黑色阴影。

建议宽度：

```text
outer dock:    324px
inner surface: 320px
min:     280px
max:     420px
```

详情内容：

- 大图。
- 状态。
- 生成时间。
- 服务商。
- 模型。
- 成本。
- 完整提示词。
- 负面提示词。
- 画幅。
- 质量。
- 数量。
- 参考来源。
- 保存状态。
- 继续微调。
- 保存为提示词。
- 加入设计方案。
- 下载。

## 23. Image Lightbox 细节

```text
scrim
┌─────────────────────────────────────────────┐
│ [关闭]                             [下载]    │
│                                             │
│                  image                      │
│                                             │
│ [上一张]                               [下一张] │
└─────────────────────────────────────────────┘
```

规格：

- 圆角 16-20px。
- Light scrim：`rgba(20,20,24,.42)`。
- Dark scrim：`rgba(0,0,0,.72)`。
- 图片保持原比例。
- 图片周围保留 16-24px padding。
- 关闭、上一张、下一张、下载均使用 icon button。
- 支持 Escape、Left、Right。
- 打开大图不改变底层历史列表布局。

Lightbox 只负责看图；继续微调、保存提示词和加入方案放在 Inspector，避免弹层内部变成第二个工作台。

## 24. 生成状态细节

| 状态 | 视觉表达 | 动作 |
| --- | --- | --- |
| 排队中 | warning/neutral + 排队中 | 取消 |
| 生成中 | Ember/进度状态 | 停止 |
| 已完成 | success + 已完成 | 打开/复用 |
| 部分完成 | warning + 部分完成 | 重试失败项 |
| 失败 | danger + 失败 | 查看错误/重试 |
| 已取消 | tertiary + 已取消 | 重新生成 |

状态不能只依靠颜色：

```text
图标 + 文案 + 颜色
```

Ember 只表达“正在进行”或当前动作，不能替代 success 和 danger。

## 25. 使用统计跳转关系

历史页不增加常驻“使用统计”按钮，避免页面级动作重新膨胀。用户从固定设置导航进入“使用统计”；单条历史 Inspector 只保留本次生成的渠道、模型、耗时和积分信息。

```text
历史页：查找记录 → 查看详情 → 继续创作 / 管理文件
设置页：查看累计用量 → 比较渠道 → 查看账号积分
```

两页共享同一份本地历史账本，但分别服务记录级任务和聚合级判断。

## 26. 磁盘占用和清理

磁盘信息可以放在页面 Header 或清理菜单中：

```text
已使用 3.2 GB
可清理 840 MB
```

清理分类：

- 缓存。
- 失败结果。
- 已删除结果。
- 回收站。
- 临时文件。

必须区分：

```text
可恢复删除
永久清理
```

永久清理使用确认 Dialog，并明确不可恢复。

## 27. Empty / Loading / Error

### Empty

```text
还没有生成历史
完成第一次设计后，结果会出现在这里

[新设计]
```

### Filter Empty

```text
没有符合筛选条件的结果
[清除筛选]
```

### Loading

- 使用接近真实 History Row 的 skeleton。
- 缩略图保持 60x60px。
- Inspector 区域不因加载状态消失而改变布局。
- 不使用高度变化明显的 shimmer。

### Error

- 列表错误提供刷新。
- 单条生成失败提供重试。
- 图片路径错误提供重新定位或隐藏损坏项。
- 长期错误显示在发生位置，不只使用 Toast。

## 28. 与 ZCode 的语义转换

```text
ZCode 任务列表        → Musefold 生成历史列表
ZCode 任务详情        → Musefold 生成详情 Inspector
ZCode 文件/工具输出   → Musefold 图片和参数
ZCode 继续任务        → Musefold 继续微调
ZCode Fork            → Musefold 新建变体/方案
ZCode 右侧 Dock       → Musefold 历史详情 Dock
```

Musefold 历史页面必须比 ZCode 更突出图片，但不能变成没有文本信息的作品画廊。

## 29. 本轮历史决策

### 29.1 历史主列表

推荐：

```text
图片缩略图 + 提示词摘要 + 状态 + 时间
```

不推荐纯图片瀑布流，因为用户需要比较模型、参数、来源和生成状态。

### 29.2 Inspector 默认状态

推荐：

```text
默认收起
用户选择历史记录后展开
```

首次进入时提供最大列表宽度，选择结果后再展示详细上下文。

### 29.3 Lightbox 层级

推荐：

```text
Lightbox 只负责看图
继续微调、保存提示词、加入方案放在 Inspector
```

## 30. 本轮历史验收

- [ ] 历史列表可以快速比较图片、状态和摘要。
- [ ] Inspector 真实占列，不覆盖列表。
- [ ] Inspector 能查看完整提示词、参数、模型和成本。
- [ ] 继续微调、保存提示词、加入方案在详情内可发现。
- [ ] Lightbox 具备完整键盘和按钮操作。
- [ ] 历史页没有累计成本看板；存储和清理不破坏主工作流。
- [ ] Light/Dark 的图片承托面有清晰层次。
- [ ] 空状态、筛选空状态、错误和失败结果完整。

## 31. 桌面端详情动作实现（2026-08-26）

本轮只升级 Inspector 动作层级和相关浮层，不改变已经落地的历史列表、折叠筛选、统计边界与双栏布局。

### 31.1 动作层级

320px Inspector 底部固定动作区采用“高频动作常驻、低频动作收纳”：

```text
[再次制作]
[存为提示词]
[创建设计方案]
[更多操作]
```

- “再次制作”是唯一高权重主动作。
- “存为提示词”和“创建设计方案”保持可发现，但不与主动作竞争。
- 复制提示词、打开文件夹、复制图片、删除记录与源文件、删除记录进入“更多操作”。
- 文件动作不再以两个双列按钮组长期占据 Inspector 高度。
- 菜单通过 Portal 定位，不改变 324px Dock、320px surface 或底部动作栏高度。

### 31.2 菜单规格

| 属性 | Light | Dark |
| --- | --- | --- |
| surface | `#fdfcf9` | `#2b2d31` |
| 宽度 | 196px | 196px |
| 外圆角 | 8px | 8px |
| 行圆角 | 6px | 6px |
| 行高 | 32px | 32px |
| border | `--border-default` | `--border-default` |
| shadow | `--shadow-pop` | `--shadow-pop` |

- 菜单与“更多操作”右边缘对齐；位于 Inspector 底部时自动向上展开。
- 普通项使用图标 + 文字；不可用的文件动作保留位置并进入 disabled 状态。
- 删除记录与源文件、删除记录使用 danger text，并与普通动作通过真实 separator 分组。
- 可见按钮文字为“更多操作”，辅助技术名称为“生成记录操作”，Content 与 Trigger 使用同一可访问名称。

### 31.3 键盘和焦点

- 打开后首个可用菜单项获得焦点。
- Arrow Up / Down 由共享 Radix collection 处理。
- Home / End 按实际 DOM 顺序跳到首尾可用项。
- Escape 关闭菜单并把焦点归还“更多操作”。
- 切换选中历史记录时，旧记录的菜单和确认 Dialog 自动关闭。

### 31.4 危险确认

“删除记录”从 Inspector 内联二次确认升级为标准 Dialog：

- 16px 圆角、raised surface、`--shadow-dialog` 和 `--scrim-dialog`。
- 使用 Header / `DialogBody` / Footer 三段结构。
- 桌面文案明确“记录会从历史中移除并影响本地使用统计，磁盘源文件仍会保留”。
- “删除记录与源文件”继续使用独立确认 Dialog，并明确源文件不可恢复。
- Web 的既有“移到回收站”语义保持不变，共享同一 Dialog 结构。
- Dialog 通过 Escape 关闭后，焦点回到“更多操作”。

### 31.5 对应实现与门禁

```text
packages/product-ui/src/history/GenerationHistoryDetailActions.tsx
packages/product-ui/src/history/GenerationHistoryDetailScreen.tsx
apps/desktop/src/features/history/components/HistoryDetail.tsx
apps/desktop/src/features/history/components/HistoryCleanupMenu.tsx
apps/web/src/views/HistoryView.tsx
packages/product-ui/src/history/__tests__/history-overlays.test.ts
tests/e2e/test_43_history_overlays_v2_desktop.py
```

`test_43` 固定使用 1440x900 桌面视口，覆盖 Light / Dark 菜单与删除 Dialog、菜单几何、危险项分组、首项焦点、Home / End、Escape 和焦点归还。本阶段按开发约定不执行手机端测试。
