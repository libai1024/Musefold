# v2.5.1 执行计划（2026-09-23 起）

本文件是 [v2.5.1 收口审计](./README.md) 的执行落地页：记录用户已定的决策、审计之后的新核实结果、实际执行顺序与当前状态。审计内容以 README 为准，本文不重复；两者冲突时以 README 与源码为准。逐项修复工作表（每项怎么修、怎么验收）见 [REPAIR-PLAN.md](./REPAIR-PLAN.md)。

**当前状态(2026-09-24 第二轮)**:用户授权推送与发布。已完成:分支 + `v2.5.0` 标签推送至 origin(最终提交 `17fa70b`);macOS arm64 安装包在 `17fa70b` 重建(manifest `release-2.5.0-mac.json`)并发布到正式下载站(`/Musefold/downloads/2.5.0/`)+ 自动更新 feed `latest-mac.yml`(2.5.0);Web 镜像从 `17fa70b` 源码在服务器构建(`sha256:ce4b65a9…`)并滚动上线(页面内嵌构建号已验证,healthz/路由全 200);本机 `/Applications/Musefold.app` 已替换为发布版(app.asar `41d42808…`)。Windows 安装包待 192.168.0.182 恢复在线后在 `17fa70b` 重建发布(机器在传输中途失联,ping 不通;交叉构建因 managed-fs 需 MSVC 原生编译不可行)。G4 三轮含 DB 全量:563P/8F→568P/3F→567P/4F,15 个失败零重复、全部单独复现通过(容器/信号定时类负载抖动);3 份真实旧备份升级 3P/0F。

上一轮状态(2026-09-24 凌晨):阶段 3 门禁执行中(串行)。已完成:阶段 1 全部源码修复提交;候选冻结推进至 `f4a3cf7`;G1 check 38/38(`a43001b` 上,其后仅 core/managed-fs/测试文件变化,最终提交复跑见下);G2 完整 API **1223P/0F/2S**(串行、含 DB,API 输入此后零漂移,等效绑定 `f4a3cf7`);G3 完整 Worker **349P/0F/2S**(同上);G4 三形态首轮 `f4a3cf7` **372P/0F**(212 项 DB 门控 skip 已识别,带 `RUN_DATABASE_TESTS=true` 的全量复跑进行中);**Windows 真机链路全部打通**(详见 §3a)。推送仍暂停待 U1。

## 3a. Windows 真机验证(2026-09-24,P1-01 前置实锤)

机器 192.168.0.182(Win10 22H2 x64/VS2022/Node24)。源码 git bundle 同步至 `f4a3cf7`。

- **修复了 managed-fs 的四个 Windows 缺陷**(此前仅有「候选源码」):afterPack 工作区根探测(`6d31c10`)、FILE_READ_ATTRIBUTES 缺失(`4d2bf17`)、CRT fd 不可跨界 → 整块 IO 原语(`e0510f8`,Unix 平台门控 `a80cbf6`)、NT 相对 `"."` 打开失败(`f4a3cf7`)。
- **NSIS 包构建成功**(未签名按配置跳过)+ **静默安装(/S)成功**。
- **package-smoke 4P/0F/3S**(3 skip 为 darwin 专属)。
- **真实账号全链路 3P/0F**(安装版 App):登录 xiaomiao → 提示词云端 CRUD → 「AI 连接」连接账号云图像 → 选模型 → **一次真实云端生图并渲染成图**(59.8s,计费在既有 D5 授权内)。
- 产品语义记录:桌面端登录 ≠ 云生图就绪,需在设置「AI 连接」显式连接并确认,providers[0].kind==='cloud' 后 Composer 才解锁(建议「无通道」空态文案指路该入口,后续 UI 卡处理)。
- 证据:`tests/v25/.results/candidate-20260923/windows-20260924/`(产物 SHA-256、冒烟与真实链路日志)。

## 1. 已定决策（2026-09-23 用户确认）

| # | 议题 | 决策 |
|---|---|---|
| D1 | 提交与推送 | 按审计 P0-01 分三个提交：修复代码+测试 / 修复记录+3 个 `.spec` 文件 / 审计文档；建立分支上游并推送 |
| D2 | 实网验收路径 | 普通网络路径为主（P0-07 归因、P0-06 付费轮）；SSH 转发仅作诊断对比，单独登记，不用于外推 |
| D3 | Toaster（P0-08） | 按 UI-SPEC §2.4 修：成功 2.5s 自动关闭、错误手动关闭；在共享封装统一默认值并补就地测试，不改 101 处调用点 |
| D4 | `/ceramic-button` 演示页（P0-10） | 删除路由，随最终提交重新部署时下线 |
| D5 | 从零付费轮（P0-06） | 按既有授权执行：指定真实账号现有积分、无总额上限、不自动充值；每轮失败先只读归因再重试，逐轮登记花费与账单；P0-07 归因先行 |
| D6 | 证据归档（P0-12） | 脱敏摘要入库（结果数字、提交绑定、路径），原始证据文件留本机 |
| D7 | 文档敏感信息 | 暂不脱敏；推送前核实仓库可见性 → 已核实为**公开**（见 §2），推送暂停待决定 |
| D8 | 旧部署脚本（P1-05.1） | 退役删除：`deploy:prod`/`deploy:rollback` npm 入口与 `scripts/deploy/run.mjs`、`rollback.mjs` 及其 v1.1 专属测试 |

## 2. 审计后新核实（2026-09-23）

**仓库可见性**：GitHub API 未认证查询 `libai1024/Musefold` 返回 `private: false / visibility: public`。

**敏感信息边际暴露**（待推送集合 = 本地领先 origin/main 的 34 个既有提交 + 本轮新提交）：

| 标识 | origin/main（当前已公开） | 本次推送的新增暴露 |
|---|---|---|
| `zhaozhaoyue.top`（部署域名） | 已存在（23 个文件） | 无新增（历史文档/工作流/生产更新源 URL 本就公开） |
| `45.207.211.136`（主机 IP） | 已存在（8 个文件） | 无新增 |
| `xiaomiao`（真实账号名） | **不存在**（0 个文件） | **新增**：`.spec` 与 v2.5 验收/会话文档多处，含 SSH 会话释放、数据库字段审计等运维细节 |

推送决策因此收窄为一个问题：**是否接受 `xiaomiao` 账号名与 v2.5 运维细节随历史公开**。三个选项：
1. 仓库转为私有后推送（用户在 GitHub 设置操作，代码零改动）；
2. 接受公开直接推送（历史重写会破坏已部署提交 `02d339a`/`165a2f5` 的绑定，审计 P0-10 依赖这些哈希，不建议重写）；
3. 暂缓推送，仅保留本机提交。

其余核实：
- `/tmp` 中 CLI/MCP 修复日志仍在：`musefold-cli-cloud-package.log`、`musefold-cli-cloud-source-scan.json` 与三个失败 check 日志（lint/typecheck/`local-asset-cleanup` 超时）已复制进 `tests/v25/.results/cli-cloud-20260922/`，修复记录路径已改写；`check-3.log` 无失败标记，归类为中间轮，不入证据目录。
- 本机 `/Applications/Musefold.app` 的 `app.asar` SHA-256 以 `2714fe7707f84f24` 开头，与修复记录登记一致；提交后用 `v25-build-manifest` 绑定提交号与该 asar（安装包一致性以最终提交的 P0-05 重建为准，届时替换）。

## 3. 执行顺序（对齐 README §8，含本轮调整）

1. **阶段 0（本提交）**：计划文件 + 三段提交（A 修复代码+测试；B 修复记录+`.spec`；C 审计文档+本文+索引）。推送暂停。
2. **阶段 1 源码修复**（全部完成后一次冻结）：
   - D3 Toaster（`packages/ui/src/components/sonner.tsx` 封装 + 就地测试）；
   - D4 删除 `apps/web-next/src/app/ceramic-button/`（源码无引用，仅 `.next` 陈旧产物）；
   - D8 退役 `deploy:prod`/`deploy:rollback`（`package.json` L34–35、`run.mjs`、`rollback.mjs`；`tests/repo/deploy-pipeline.test.ts` 改为退役守卫断言；`scripts/deploy/README.md` 同步）；
   - P0-13 过期文档修正（v2.5 README、09-21 验收记录补登、ui-parity README、01-shell、数据生命周期、spec §6.1）。
3. **阶段 2 冻结**：`v25-build-manifest` 记录候选提交；此后一切结果绑定该提交，再改源码则重新冻结并重跑受影响门禁。
4. **阶段 3 门禁**（同一提交，串行、不与重负载并行）：`pnpm run check` → 完整 API / 完整 Worker → `pnpm run build` + 三形态 E2E（0F）→ 自然一小时 → macOS 包链（`package:mac:adhoc` → smoke 7 → 3 份真实旧备份 → 扫描 → Worker 正式镜像）。
5. **阶段 4 实网**（普通路径，D2）：P0-07 归因（只读诊断）→ P0-06 从零完整付费轮（D5）→ P0-09 真实窗口（全屏记录实际路径、真实拖动）→ P0-11 余额对账 → P0-08 的 L166 真实重登/双宿主/反向矩阵。
6. **阶段 5 生产对齐**：按最终提交重新部署（**需另行授权**）或逐文件一致性证明；`TRUST_PROXY`/`S3_AUTO_CREATE_BUCKET`/就绪检查/演示路由下线核对。
7. **阶段 6 关闭**（P0-12）：勾选 5 个父项、MVP 7 项、检查清单；`check_spec_package.py` 通过；证据 manifest 20 条绑定提交；证据脱敏摘要入库（D6）；README §9 判定逐条满足。
8. **P1**：Windows → 签名公证 → 远端 CI → 真实旧库与包内迁移 → 发布回滚演练 → 公共站点切换 → Changesets + tag。

## 4. 待用户决定

| # | 议题 | 建议 |
|---|---|---|
| U1 | 推送与公开仓库处置（§2 三选项） | 转私有后推送（零代码改动，历史完整） |
| U2 | 根目录 8 个 logo 文件（随 `dedace4` 入库，09-19 收口记录原定不纳入） | 移除 |
| U3 | v1.1 公共站点切换时机与旧数据处置（P1-08） | v2.5.1 收口后单独排卡 |
| U4 | 桌面托管文本是否开放、真实账单范围（SP-P6） | 维持关闭，登记产品决定 |
| U5 | 桌面是否实现与云端 30 天清除对应的自动保留（§5.3 政策决定） | 先定审计要求再排卡 |
| U6 | 阶段 5 是否重新部署（需逐次授权） | 最终提交后重新部署 |

## 5. 完成判定

见 [README §9](./README.md)。本文状态行随各阶段推进更新；阶段全部完成后本文归档为执行记录，不再作为待办清单。
