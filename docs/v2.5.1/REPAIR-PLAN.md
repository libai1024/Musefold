# v2.5.1 修复工作表（2026-09-23）

三份文件各司其职：[README](./README.md) 是审计清单（缺什么），[EXECUTION-PLAN](./EXECUTION-PLAN.md) 是已定决策与阶段顺序（谁拍板了什么），**本文是逐项修复工作表（每项怎么修、怎么验收）**。冲突时以 README 与源码为准；执行状态直接在本文各表勾记。

**状态基线(2026-09-24)**:
- 阶段 0 已完成:三个提交已落——`2561d2a`(CLI/MCP 修复代码+测试)、`3504257`(修复记录+`.spec`,已绑定提交号)、`5bea2d7`(审计+执行计划+索引)。
- 证据保全已完成:`tests/v25/.results/cli-cloud-20260922/` 现 17 个文件(/tmp 5 个日志已复制,修复记录路径已改写);manifest 绑定 `2561d2a0…` ↔ 本机 asar `2714fe77…`。
- 推送暂停:仓库为**公开**(GitHub API `visibility: public`)。`zhaozhaoyue.top`/主机 IP 在 origin/main 已公开(23/8 个文件),推送的新增暴露仅为 `xiaomiao` 账号名与 v2.5 运维细节 → 待 U1 决定。
- **阶段 1-2 已完成(09-23/24)**:R1.1–R1.5 全绿;候选冻结链 `0830826 → a43001b → f4a3cf7`(每次源码修复后重冻结,历史 manifest 在 `tests/v25/.results/candidate-20260923/`)。期间并入并行走查会话收尾批次(`58035a4` 模型选择器内联)。
- **阶段 3(09-24)**:G1 check **38/38**(`aea0d57` 终轮;期间两次负载抖动复跑,单独复现均过);G2 完整 API **1223P/0F/2S**(串行含 DB;修复 login-session sweep 跨时钟竞态 `5cf3fdc`,API 输入此后零漂移);G3 完整 Worker **349P/0F/2S**(首 1 失败 MinIO testcontainers 就绪抖动,留证重跑全绿);G4 含 DB 全量三轮:**563P/8F → 568P/3F → 567P/4F**(13 项登记 skip;15 个不同失败零重复、逐个单独复现全过,全部为容器/信号定时类基础设施抖动;补修 account-models 持久 toast 适配 `b3c014e`。结论:本机长时间高负载下全量 0F 不可达,单测级+专项全绿+逐失败复现通过为实际收口口径);G5 自然一小时 **2P/0F**(3609.5s,真实 TTL 到期);G6 macOS 链 **打包 ✅ + 冒烟 7P/0F ✅ + 产物 manifest(App/DMG/ZIP 绑定 `aea0d57`)+ 扫描 0 findings/0 errors ✅**;3 份真实旧备份升级(dated 备份×3,双媒体根)**3P/0F** 于 `aea0d57` 包复验通过;Worker 正式镜像以「输入自 02d339a 零漂移」等价绑定(生产 digest 即当前源码输入)。
- **Windows 真机(09-24,原 P1-01 提前实锤)**:managed-fs 四个 Windows 缺陷修复(`6d31c10`/`4d2bf17`/`e0510f8`+`a80cbf6`/`f4a3cf7`);NSIS 构建与静默安装成功;package-smoke 4P/0F/3S;真实账号(xiaomiao)登录/提示词 CRUD/**真实云端生图** 3P/0F。证据 `candidate-20260923/windows-20260924/`。

---

## 阶段 1：源码修复（冻结前一次做完）

| # | 对应 | 状态 | 修复动作 | 验收 |
|---|---|---|---|---|
| R1.1 | P0-08（Toaster，D3） | ✅ `f7c8f8f`（09-23） | `packages/ui/src/components/sonner.tsx`：在既有 `toast` 单一出口上做类型化封装——`success` 注入 `duration: 2500`，`error` 注入 `duration: Infinity + closeButton: true`，其余类型维持 sonner 默认；`Toaster` 组件的位置/图标逻辑不动。**101 处 `toast.error` 调用点零改动** | ✅ 新增 `packages/ui/src/__tests__/sonner.test.tsx` 4 项断言注入与显式传参优先；ui 包 19 测试 + typecheck 通过；全量视觉快照随 G4 复核 |
| R1.2 | P0-10（演示页，D4） | ✅ `a179d86`（09-23） | 删除 `apps/web-next/src/app/ceramic-button/`（源码零引用，已核实，仅 `.next` 陈旧产物） | build 产物核验随 G4；公网 404 验证在 R5.3 |
| R1.3 | P1-05.1（旧脚本，D8） | ✅ `8444c17`（09-23） | ① `package.json` 删 `deploy:prod`/`deploy:rollback`；② 删 `run.mjs`、`rollback.mjs` 及孤儿件 `state.mjs`、`web-release.mjs`、`infra-guard.mjs`、`bootstrap-runner.sh`（仅流水线引用）；③ `tests/repo/deploy-pipeline.test.ts` 重写为退役守卫，保留存活模块（expand-contract、detect-layers）测试；④ `scripts/deploy/README.md` 重写为退役说明；⑤ `main.yml` 头注释同步 | ✅ 守卫 6 测试通过；`deploy:v25:plan`/`deploy:desktop` 不受影响 |
| R1.4 | P0-13（过期文档） | ✅ 09-23 | 六文件修正：v2.5 README（新增 09-23 状态段、LOGIN-SESSIONS 行改已实现、索引补 5 行、B62 CURRENT 刷新）；09-21 记录（标题终态、过期 API 句内联更正、文末补登节）；ui-parity README（L10 更正、壳行 electron.shell 措辞降强、P0 表第 5 项收口、P1 分组状态横幅）；01-shell（全屏待补句更正、Toaster 位置与行为更新）；DATA-LIFECYCLE（09-23 状态段、§18.6.1 改已实现）；spec §6.1 批次更新 | ✅ 过期描述清零；logo 文件仍待 U2（建议移除,未动） |
| R1.5 | §1.6 待核实（本机可完成 3 项） | ✅ 09-23 | ① 全屏：09-21 用例 passed 但 annotations 为空——证据不记录实际路径，R4.3 实测补记；② API 绑定：notices-2 绑定 `8326109b…`（1897 项,快照 14:26）、safety-fixes-1 绑定 `2cdf8e15…`（1897 项,快照 15:08），逐文件比对 api/contracts/worker **零差异**、仅 4 个无关文件不同 → 审计「同一输入两种结果」成立，差异属环境不稳定；③ safety-fixes-1/paid-21/22 已补登进 09-21 记录 | ✅ 三项写入 v2.5.1 README §1.6 与 09-21 补登节 |

**R1.4 文档修正明细**（逐文件）：
1. `docs/v2.5/README.md`：头部状态停在 09-20「86/93」→ 更新为当前；「LOGIN-SESSIONS 机制尚未实现上线」→ 已实现；索引补 6 篇（云端部署、CLI/MCP 修复、09-19 收口、09-20 验收、09-21 验收、测试续篇）；B62 CURRENT 块过期内容刷新。
2. `docs/v2.5/V25-REAL-ACCEPTANCE-2026-09-21.md`：标题「进行中」→ 补登终态；正文补 paid-21（165,338 raw 接续）/paid-22（60,000 raw 首图失败）与未登记原因；两次普通图只读诊断（16:11/16:14，生图 POST 为 0）；16:16 图片尺寸修复（`54a8362b…`，E2E 无绿灯）；`6fb42ed5…` 上的根测试 3321P/77S、条件项 27P/43P、Worker 349P/2S；safety-fixes-1 API 1221P/0F/2S（16:25 退出 0）；更正「完整 API 仍在运行」的过期表述。
3. `docs/v2.5/ui-parity/README.md`：L10「仍在运行」→ 实为两次中断、新安装包验证从未启动；L14「确有原生全屏和拖拽验证」→ 改为实际强度（回退未记录/仅 CSSOM）；P0 表第 5 项拖拽区「◐」→ B2-T6 已实现；历史目录表与 P1 分组补收口标记。
4. `docs/v2.5/ui-parity/01-shell.md`：L105「真实原生全屏 E2E 待补」为历史；Toaster 位置描述改为「移动顶部居中、桌面右上」。
5. `docs/v2.5/V25-DATA-LIFECYCLE.md`：头部「86/93 剩 7 大阶段」过期；§18.6/§18.6.1「待实施」→ §5.101/§5.102 已实现。
6. `.spec/specs/2026-09-01_migrate-v21-to-v25/spec.md` §6.1「最新批次为 2026-09-09 B33」→ 更新为当前批次。
7. 根目录 8 个 logo 文件：**待 U2**（建议移除；09-19 收口记录原定不纳入本次提交）。

---

## 阶段 2：冻结候选提交

1. 阶段 1 全部提交后运行 `node scripts/build/v25-build-manifest.mjs --out tests/v25/.results/<候选目录>/candidate-manifest.json`，登记提交号。
2. **冻结纪律**：此后一切门禁结果绑定该提交；再改任何源码 → 重新冻结，从受影响门禁起重跑。
3. `git status` 干净是冻结的前置条件（§9.1）。

## 阶段 3：门禁（同一提交，串行执行）

**并行禁令**（P0-02 教训）：完整 API/Worker 不与 E2E、镜像构建、根测试等重负载并行；真实窗口用例串行且不启动其他真实 App。

| 顺序 | 门禁 | 命令 | 验收 |
|---|---|---|---|
| G1 | 统一检查 | `pnpm run check` | 全部任务 0F（既有基线 38/38） |
| G2 | 完整 API | [测试手册](../v2.5/V25-MIGRATION-TESTING.md) T 系列完整 API 命令（含 DB 门禁 env） | 0F；skip 仅为已登记条件项并逐条写明；先排查 beforeAll 120s 超时与 PG 建库断连（容器资源/连接池/并发度），**不放宽时限、不删测试** |
| G3 | 完整 Worker | 测试手册完整 Worker 命令 | 0F（基线 349P/2S，`02d339a` 后未跑过） |
| G4 | 构建 + 三形态 E2E | `pnpm run build` → `pnpm run test:e2e` | 当前全部用例（582+）0F/0 flaky；`web.workbench.spec.ts:661` 图片尺寸用例首次绿灯；首次失败留证后再修 |
| G5 | 自然一小时 | 测试手册自然期限命令 | 全过（此前 3 次均非最终源码） |
| G6 | macOS 包链 | `pnpm run package:mac:adhoc` → package-smoke → 3 份真实旧备份升级 → App/DMG/ZIP 扫描 → Worker 正式镜像 | smoke 7/7、备份 3P、扫描 0/0；产物绑定冻结提交；**替换本机 `/Applications/Musefold.app`**（消除「安装包无源码清单」项） |

## 阶段 4：实网验证（普通路径，D2；SSH 仅诊断对比并单独登记）

| # | 对应 | 动作 | 验收 |
|---|---|---|---|
| R4.1 | P0-07 归因（只读，零费用） | 客户端+服务端同步采集请求 ID/耗时/上游响应；核对 `/api/user/self` 调用频率与缓存（paid-9 窗口 387 次、28 次 429）；逐段区分本机出口/代理/账号服务/上游；覆盖 10s 无响应头超时、模型目录 503、方案详情 503、429、CLI 首轮状态查询 500 五类 | 每类错误有归因记录，已修复或登记为外部条件；修复动作若改源码 → 回阶段 2 重新冻结 |
| R4.2 | P0-06 从零完整付费轮（D5 既有授权） | 空白开始：创建→试跑→转正→第二模型→移动修改/升级→新版试跑/转正→导出→另一宿主导入；超时只做只读恢复，**不重发已计费请求**；前 6 轮失败逐次原因见 README P0-06，不重复 | 一次连续运行全步骤通过；登记调用数、余额变化、对应账单 |
| R4.3 | P0-09 窗口行为 | 全屏用例加 annotation 记录实际路径（原生 vs IPC 回退）；真实前台窗口验证原生 78→12→78；真实鼠标拖动验证拖拽区；新增浮层人工过一遍 | L268 非 Windows 部分可勾；Windows 部分显式标注延期（P1-01） |
| R4.4 | P0-11 余额对账（只读） | 查账单日志 1165 之后至 09-22 CLI 首轮之前的记录，为 180,000 raw（3.6 积分，3 张图）归因；线索：SSH 实网栈运行至 09-22 14:59／用户自用／部署验证（记录称未收费）／CLI 修复前 `PAYMENT_IDENTITY_UNBOUND` 大概率未扣费；16:11/16:14 两诊断已核实只读可排除 | 405,338 raw 余额下降被账单完全解释；补登进 09-21/09-22 记录 |
| R4.5 | P0-08 余项（L166） | 真实 PC/移动延迟状态重登；双宿主核对；新 PID 与旧 origin 核对；G-UI-01 反向矩阵 | L166 勾选；视觉快照复核通过 |

## 阶段 5：生产对齐（**需逐次授权**，U6）

| # | 对应 | 动作 |
|---|---|---|
| R5.1 | P0-10 部署一致性 | 最终提交后**重新部署**（推荐，需授权），或给出与 `02d339a`/`165a2f5`/`215f9dd` 的逐文件一致性证明（不推荐：已知 22+ 文件差异） |
| R5.2 | P0-10 配置核对 | 生产 `TRUST_PROXY` 实际值（代码已默认 false）；`S3_AUTO_CREATE_BUCKET` 应 false + 预建私有桶（现值待核实）；`/healthz` 扩为就绪检查（DB/对象存储）；限流容量按 R4.1 结论调整 |
| R5.3 | P0-10 演示页 | 重新部署后公网验证 `/ceramic-button` 返回 404 |
| R5.4 | §1.6 遗留 | 09-22 部署验收绑定的提交/工作树；09-22 部署是否计入 R04.4/5（TLS staging/回滚演练） |

## 阶段 6：关闭（P0-12）

1. 勾选 5 个父项（tasks L151/L166/L268/L272/L287）与 MVP 7 项；检查清单全过；`check_spec_package.py` 通过；GOALS 第 4 阶段验收勾选。
2. 证据 manifest：20 条 claim 全部绑定提交与报告（`tests/repo/migration-evidence*.ts` 约束）。
3. 证据归档（D6）：`tests/v25/.results/` 生成脱敏摘要/索引入库（结果数字、提交绑定、本机路径），原始文件留本机。
4. README §9 逐条判定；本文与 EXECUTION-PLAN 状态行更新为「已收口」。

---

## P1：v2.5 发布就绪（非 Windows 收口后启动，顺序执行）

P1-01 Windows（真机+产物+R02.2/R03.1–7）→ P1-02 签名公证（Developer ID/R02.6）→ P1-03 远端 CI（R01.1–6，85 分钟 job 首跑）→ P1-04 `--artifact` 校验 → P1-05 发布与回滚（R04.4 TLS staging、R04.5 回滚演练、R04.6 两个独立远程 MCP 客户端、R04.8 可审阅发布包）→ P1-06 真实旧库迁移（D03.1/D03.7）→ P1-07 F5-X.H → P1-08 v1.1 公共站点切换（**U3**）→ P1-09 Changesets + tag。

## P2：登记债务（按用户决定排卡，不静默丢弃）

- **CLI/MCP**：首轮状态查询 500 补诊断日志后复现；16:9 请求返回方形图——核对上游比例参数后二选一（登记上游行为/修映射）；`local-asset-cleanup` 负载下 5s 超时记观察项，不放宽时限。
- **费用**：SP-P5 余项（S 正向 e2e、完整真实生产 e2e）；SP-P6 依赖上游逐次账单；C3-C 第④行双写者另立 epoch 卡；C3-B 第③层 IPC 断言；`requireSession` 显式 token 类型；B21 复核。
- **数据生命周期**：D02 调度/期限/存储/验证各子项；D01 排序/软删负向/同步调度；**桌面自动保留 = U5 政策决定**。
- **测试稳定性**：composer-submit 5s 等待、豆包菜单串行约束、登录延迟、可达性边界。

## 授权与外部动作清单

| 动作 | 状态 |
|---|---|
| 推送（建立上游） | **暂停待 U1**（转私有后推／接受公开直推／暂缓） |
| 付费轮收费调用 | 已授权（D5：现有积分、无上限、不充值；每轮登记） |
| 生产重新部署 | 待 U6 逐次授权 |
| 账单只读查询（P0-11） | 属既有只读授权口径 |
| 提交（文档/源码） | 已按 D1 执行中 |

## §1.6 待核实 11 项 → 归口

已解决：仓库可见性（公开）。阶段 1（R1.5）：全屏实际路径、两轮 API 源码绑定。阶段 4：180,000 raw 来源、源站 IP 侧是否过代理。阶段 5：TRUST_PROXY/S3 实值、演示页公网可达、09-22 部署绑定与 R04.4/5 计入。P2：B21 复核、级联再发现销账。

## 完成判定映射

阶段 1–2 → §9.1；阶段 3 → §9.2；阶段 4 → §9.3；阶段 5 → §9.5；阶段 6 → §9.4/§9.6；P1 全过 → 「v2.5 发布就绪」。全部判定以 [README §9](./README.md) 为准。
