# v2.5 全迁移收口记录

目标：完成尚未闭合的关键部分，执行全量测试，最后提交 Git。此前批次通过只证明其记录的源码与范围；本文不降低原父任务的验收条件。

> 09-20非Windows最新终态见[独立验收记录](./V25-NONWINDOWS-ACCEPTANCE-2026-09-20.md)：check38/38、完整API1138P/0S、Worker322P/0S、三端545P/0F/11S、新macOS包7P及三旧备份3P、实际镜像部署18P均已完成；1813项fa3235d0…与产物绑定，扫描0/0。B26/B30按原范围收口，任务89/94；官方网页登录确认AUTH_SESSION_LIMIT，真实同步/方案收费链路仍待，新增收费调用0。Windows延期，完整迁移未验收、未提交。下面记录只代表各阶段当时状态。

> 09-20 用户最新决定：Windows App 验收延期，先完成其余 v2.5 开发与测试。Windows 专属原生/安装/签名结果继续独立保留未验，不再因 SSH 条件停止其他平台工作；macOS、PC Web、Mobile Web、API/Worker 和数据/账号/方案链路按原标准继续收口。此调整不等于四端正式发布通过，也不自动授权生产变更或新增收费。

> 09-20 实网补验：用户重新提供凭据后，当前同源构建的 PC Web、Mobile Web、Electron 真实登录与提示词 CRUD **3P/0F/0S**，桌面 TvT 真实密钥连接 **1P/0F/0S**，均无重试。真实账号身份、15 项账号模型及鉴权定价读取成功，本地正式 API 也返回 15 项目录，其中 3 项支持图像接口并带云价。本轮没有生图请求，不能据此关闭真实云费用、方案文本或双设备验收。原完整报告仍为 544P/8S，不改写历史跳过；其中 4 项已独立补验。Windows 已收到用户指定的局域网 SSH 目标，但本机连接仍停在服务端 banner 超时，尚未执行远端命令。源码 1810 项摘要仍为 4db5b5b8…，任务 87/94，未提交。

> 09-20接续审计最新：当前1810项4db5b5b8…的Skill可信来源、终态零来源重读及缓存账号校验修复已完成本机验证。最终check38/38（root3214P/75条件S、features850P），75条件项逐文件核对：本次服务17P，未变输入58项沿准确报告承接。最新完整三形态544P/0F/8S/0flaky、退出0、56.7分钟（PC167P/2S、移动166P/3S、Electron211P/3S），无重试/无截图更新。8S为3个视口不适用、3个真实账号及2个真实密钥/付费条件，不计通过。新arm64 ad-hoc包7P、三授权旧备份3P且原件不变；源码、App/DMG/ZIP及49份真实ZIP扫描0/0，2份UI占位按原始内容单独通过，17份故意拒绝输入另列。源码与23个包内构建文件收尾无漂移。87/94、完整MVP和真实账号/目标平台/正式发布条件仍开放，未提交。

起点为 `spec/2026-09-01_migrate-v21-to-v25`、HEAD `dcdf8d036c27f87e33de301ad4077a3110bb5b7f`，337 个已修改路径及 708 个未跟踪项。既有源码、报告和用户数据保留。

| 范围 | 当前证据与待完成工作 | 完成判据 |
| --- | --- | --- |
| 方案清理与 D02 | PG purge 已验；共享 gateway/“已移除”确认入口、桌面 v11 持久文件清理与结果保留已接入；继续完整联合验证 | 双宿主明确确认后永久清理、权限/版本/重放/活动任务保护、原生成/费用与共享资产保留；完整生命周期测试 |
| 实网账号与费用 | 新凭据的三端真实登录/提示词 CRUD 3P、TvT 连接 1P；真实账号模型及云价经正式 API 读取成功。此前单张 BYOK 结果保留 | 登录/连接阻塞已解除；真实云生成/方案文本/费用与同步仍分别待验，不重复执行已通过的收费试验 |
| 真实旧库与双设备 | 当前实际包的三份真实旧备份升级、原记录/FTS/真实图片双重启3P；两独立SQLite客户端→真实HTTP/PG同步12P。均限定私有副本/合成账号，不改原数据 | 实际包本机升级证据已取得；原定真实账号联合验收、其他目标平台与受控旧库来源条件分别保留 |
| 原生平台与包 | 最新4db5b5b8…的macOS arm64 ad-hoc包smoke7P、三真实旧备份3P、App/DMG/ZIP扫描0/0；23个desktop/out文件与实际包内字节一致，bundle manifest为7449495f…；旧包按各自源码另存 | Windows目标架构、正式签名/公证及系统安装器交付待验，不能由macOS私有userData升级替代 |
| 发布身份与交付 | release产物/报告哈希已接；专用v2.5镜像计划/受控迁移与当前实际镜像迁移、角色、隔离Compose备份恢复/回切18P。旧v1.1默认部署入口仍不得用于v2.5 | 准确提交身份、远端CI、真实TLS staging/授权后的生产部署及回滚、两个独立远程MCP客户端仍待 |
| 最终验收与 Git | 最新check38/38、完整三形态544P/0F/8S/0flaky（56.7分钟、退出0）及新包/真实旧备份已收齐；未变输入的API1138P含自然一小时、Worker322P、镜像18P可追溯。根75条件项按本次17+未变58逐文件核对。任务87/94（混合父子计数，不是完成百分比） | 8S不计通过；真实账号、目标平台/正式发布、原父任务逐项验收和正式manifest仍待，最终Git提交未执行 |

管理员后台仍按原计划在上述迁移全部验收后接续；冻结和暂缓域沿根 AGENTS.md。

提交范围复核（09-20）：根目录未跟踪的`logo-dark-flat-preview.png`、`logo-dark-flat.svg`、`logo-dark-preview.png`、`logo-dark.svg`、`logo-motion.html`、`logo-reference.svg`、`logo-vector-preview.png`、`logo.svg`未在应用/包/脚本/测试/迁移文档中发现引用，也不属于本次模型与迁移改动；原样保留，不自动纳入本次提交，不删除或加入忽略规则。当前冻结的`src/pet`、主进程pet目录和专用preload无tracked diff；仍未暂存、提交或推送。

## 本次进展

### 09-20 新凭据实网补验与 Windows 接入

- 账号事实源保持既有 New API，TvT 仅用于 BYOK 连接；未修改线上配置、账号密码或模型价格。新凭据只注入本次进程，仓库外原私有配置未覆盖。
- `live-reconfirmed-account-1.json`：3P/0F/0S/0flaky，14.261 秒，命令退出 0；PC/移动端登录、提示词创建/读取/删除及方案列表入口，Electron 登录和提示词 CRUD 均通过。使用独立验证 PG 和临时 userData，不触正式 App 数据。
- `live-reconfirmed-provider-connection-1.json`：1P/0F/0S/0flaky，5.047 秒，退出 0；实际 TvT 连接正常、UI/SQLite 只留密钥尾号，原密钥不落明文。连接成功截图已查看。
- 直接账号预检的登录、fresh self、模型列表及鉴权价格均 HTTP 200；本地正式 API 的账号目录同样 HTTP 200，15 项模型中 3 项图像模型有有效按次云价。独立预检会话已登出。没有执行生图、兑换、价格变更或付费重试，不宣称已取得模型实际出图/账单。
- 本轮 8 份报告/附件和两份临时 userData 共 962 个文件做新密码/Key 精确扫描，0 命中、0 读取错误；自动截图、视频、trace 和失败 DOM 快照保持关闭，唯一人工截图在密钥输入框关闭后取得。新凭据不写报告、源码或配置文件。
- 用户明确 Windows 使用局域网设备并给出 SSH 用户。TCP 22 可达，但本机多次只读 SSH 检查在服务器 banner 阶段超时，尚无主机身份、原生工具链或 Windows 测试结果；不会将端口可达记为 Windows 通过，也没有上传、安装或改动远端数据。
- 完整报告中的 8S 保留原样；本次只为其中 3 个真实账号和 1 个密钥连接补充独立证据。3 个视口适用性跳过及付费入口另列，历史一次 BYOK 成功不冒充当前源码重新实跑。原 7 个父任务及完整 MVP、正式平台/发布条件不关闭；最终 Git 提交未执行。

本轮补验汇总：`tests/v25/.results/closeout-20260919/live-reconfirmed-summary-1.json`。本节取代历史记录中的“缺有效账号凭据”；不能替代真实云收费、双设备同步、Windows 或发布验收。

### 09-20 verified managed Skill兼容缺陷补验（本机验证完成，整包验收仍开放）

- 当前4db5b5b8…最终`verified-s-final-check-2.log`退出0、38/38（root3214P/75条件S、features850P、31缓存）；当前夹具服务17P及定向Electron3P已验。`verified-s-final-package-1.log`构建退出0，实际新包smoke7P、三授权旧备份3P，原备份和真实媒体SHA不变；每份4个不同PID、15条迁移、FTS31/31/19及152/176/178个媒体文件跨两次新进程保留。23个包内构建文件逐字节匹配，manifest `7449495fbce0bf9da180fd9b2870f9bf5d1656d4a15f5e9cbcf4badc1ab746a9`；DMG `5693209bba176a8bfcf5302a77ad9269c5151812f25e01208dd9c2c1bc9413ac`、ZIP `d92fdaf83122f0f5e57d847c31a69ee7c07e5fc6b3e25b3b44a0b8d3595a42b3`，六目标扫描0 findings/0 errors。原b4ab包保存在`pre-verified-s-final-mac-xr42Hf/`。最新完整`verified-s-final-e2e-1`已退出0：552项中544P/0F/8S/0flaky，3402.978秒；PC167P/2S、移动166P/3S、Electron211P/3S。明确无自动重试/无截图更新，完整期间无源码或构建漂移；账号模型、确切B/A身份的R/S与所有受影响夹具使用者均在全量通过。8S分为3视口不适用、3真实账号、1真实密钥连接和1真实付费入口，不能当通过。报告SHA `fe02c176ebf43d64b1a249514694a397c4974ae967da632365ac6ea356ecdbbe`。收尾源码扫描0/0；68份留存文件按SHA核对分类为49真实ZIP、2明确UI占位、17故意拒绝输入。首轮扫描对UI占位产生2读取错误（无findings），保留原报告；第二轮49真实ZIP扫描0/0，两份原始26字节占位用不变内容扫描器单独通过，无改名、放宽规则或例外。真实凭据文件仍0600、mtime为09-19 20:33+0800，尚未收到更新确认，不重试已拒凭据；正式验收与提交仍开放。

- 最新接续：`verified-s-check-3`退出0、38/38（root3212P/75条件S，32缓存）；`verified-s-electron-4`为3P/0F/0S/0flaky（账号模型、R、S），`verified-s-service-2`为17P。S验证真实确认、BYOK文本一次、两张云图像各一次与同进程/新PID重放零来源读取/零新发送。源码b4ab750d…及报告hash落在`verified-s-source-identity.json`；API/Worker/Web/迁移/部署相对aafa2f35…仅七桌面源/测试文件差异的精确重建证明保留。
- b4ab750d…实际macOS arm64 ad-hoc包smoke7P、三授权旧备份3P，原数据及图片不变；23个out文件与包内字节一致，manifest为`2932eaed5fa5f725d2288524e11217663418bc6b91ca8de7a1eb65b164775ed9`。App/DMG/ZIP扫描6目标0 findings/0 errors，DMG `c507f62af6f78ea20df7f263cc3f8c01f124111ebc8cce3e43c8f3fb051b8183`、ZIP `bc18e6069618effe49ed43981a1cad0fae270807230e59aafd1ab564a3ae9638`。该包随后被以下新缺陷修复取代，不能直接用于最新生产源验收；之前模型包原样保存于`pre-verified-s-mac-DFRctJ/`。
- 缓存身份核对：`verified-s-account-guard-before`0P/1F与`verified-s-electron-5`2P/1F均为期望409、实际200；进一步核对发现这两次并未切到B账号——桌面发送email，而合成登录路由只读username，实际始终为A。故两报告保留为测试前置失败，不再声称是实际跨账号缺陷复现。修夹具为email优先、兼容原username，并在新用例断言确切principal。缓存路由本身缺少身份校验已独立以`verified-s-cache-route-before`5P/2F复现（scheme/skill均未调用受控查询），恢复修复后`verified-s-unit-4`相关51P/0F/0S。原失败未被删除或改写。
- 最终定向`verified-s-electron-6`为3P/0F/0S/0flaky、50.0秒；S先正常双图，再同键/新PID零来源重读，随后确切B账号被409拒绝、确切A账号重登200原结果，原账本/文本1次/图像2次/GitHub读取均不变。当前1810项源码`4db5b5b89d0a97198aaf7ba1505f7de45c9ba236b299aafb8f4229b0501b3c43`，八文件替回旧hash精确重建aafa2f35…；新增第八项仅合成服务登录夹具，API/Worker/Web生产源未变。该夹具只由桌面服务集成及12个Electron测试/辅助文件消费（共14处含进程封装），不被API/Worker正式测试套件引用；相关服务与完整三端须当前夹具实跑，不能复用原换号证据。最近生产源完整check38/38（root3214P/75条件S）通过，夹具后最终check/全量/新包待收。

- 原B79只覆盖verified R，S的真实Electron正例仍缺。新增`electron.rsmap-verified-s.spec.ts`串联既有GitHub/文本HTTP夹具、真实账号登录、Hono/PG/Worker及Electron确认卡。首个CLI参数错误未执行测试，另记`verified-s-first.log`；实际首跑`verified-s-before-1.json`0P/1F复现文本一次、云图片零发送、终态cancelled。
- 原`assertChildProjection`无条件拒绝Skill执行器的`skillRuntime`。新增`managed-run-projection`接缝：只允许主进程从已准备且digest校验的Skill来源捕获名称/仓库，且仅限Skill运行；伪造来源、普通方案带Skill信息、提示词/工作台冒用仍拒绝。来源只进入本地历史，不作为云权限或上游请求字段。对应9项就地负例/正例，相关49P、真实服务17P、完整check38/38（root3212P/75条件S，32缓存）通过。
- 首次新构建联合为2P/1F（`verified-s-electron-1`）：实际S已双图成功，失败为新测试将SQL ordinal误当发送顺序；按现有账本预留图像0/1、文本2保留严格三条未知费用断言。第二次2P/1F是新测试对新PID首次重放的HTTP状态误期望200，既有路由为202（同进程重复为200），保留协议并分别验证。
- 第三次2P/1F（`verified-s-electron-3`）揭示真正的重启重放缺陷：费用/图片/文本次数均未增加，但重新读取了GitHub来源。修复终态重放为当前账号授权下的本地持久查询，核对原入口/输入，跳过来源读取和新授权；新增输入/入口冲突与当前账号隔离负例。修复后相关62P，追加换号断言后的完整check正在执行；新构建S/R/账号模型E2E、最终全量与新产物尚待，不用旧543P或旧包替代。
- 这不是新增Skill对话UI，也不扩展云文本付款身份、Cloud MCP写能力或收费授权。原实网/平台/发布条件和7个父任务继续开放，未提交；已确认私有凭据文件mtime仍为09-19 20:33（北京时间），未收到更新确认，不重复尝试被拒凭据。

### 09-20 最终本机全量通过、账号模型子项完成与剩余验收

- `models-final-e2e-1.json`完整549项终态为528P/13F/8S/0flaky，退出1、57.8分钟；没有额外失败。8S分别为3个真实账号入口、2个真实收费/密钥入口及3个不适用视口，不计为通过。首败报告保留。
- 修订后的四文件串行全文件复验`models-final-fixture-e2e-1.json`为34P/0F/0S/0flaky，退出0、149.41秒，覆盖原13F、访客负例及原有恢复/取消/费用保护。无自动重试、未修改截图；这里只证明相关四文件，不替代最终完整回归。
- 当前实际macOS arm64 ad-hoc包对三份已授权真实旧备份完成`models-final-real-backup-1.json`：3P/0F/0S/0flaky，退出0。每份均为4个不同PID、15条迁移、完整性通过且无外键违规；原记录/费用/路径及31/31/19个FTS查询保留。测试副本随后搬移152/176/178个真实图片文件，各经两次新进程的media字节SHA与renderer解码验证；原备份和原图片SHA不变。第三份保留1个历史迁移hash差异，未伪造重写账本。23个包内构建文件逐字节匹配，manifest SHA256为`a9f7c951f2f9ae2779b05012af450c2cc60acff85023f009ae760c850f412556`；不外推系统安装、Windows或正式签名。
- 当前1807项`aafa2f35…`再次复核，七测试/夹具旧hash仍精确重建原`5b2a680d…`；生产源码未变。`models-final-source-scan-2.json`0 findings/0 errors，manifest守卫12P，diff检查通过。最新`models-final-check-2.log`退出0、38/38（28项缓存命中）；root3203P/75条件S、features850P，默认API373P/765条件S和Worker90P/232条件S不冒称数据库全跑。
- 当前API/Web生产Dockerfile构建退出0，linux/arm64镜像分别为`sha256:62ecf71cbb7518b3e1b653022d29ec2c39c870b4328882c4eb3cc2e5095421a2`和`sha256:9933a1ee60c5bb0bfb82d214635c0aaf671bf57f395d1ea65181d9cbbab26a04`，Worker沿用生产源未变的`sha256:b03d0b32…`。三组实际镜像测试`models-final-deployment-1.json`18P/0F/0S、退出0：迁移2项、受限角色8项、隔离Compose业务/备份恢复/历史三镜像回切8项。双视口截图已查看；使用合成账号及图片上游、独立PG/MinIO，不等于TLS远程staging、真实费用或生产发布。根75个条件跳过至此分别补验75项，无未补项。
- 完整`models-final-api-1.json`原进程退出0：96文件、1138P/0F/0S，6484.92秒，包含自然一小时而不是额外加计。自然场景`models-final-lifetime/package-lifetime-natural-GLfWNk/result.json`真实经过3604.227秒，`provesNaturalExpiry=true`：原Worker59286→新Worker66436、真实GET/PUT等待点释放、原期限后的维护清理、8个对象删除及12个对象保留，Worker退出0；不改系统时钟/原租约、不外推真实云S3或付费账号。API仍按原启动输入与七测试/夹具差异证明绑定，未伪称整个API目录从未改变。
- 最终完整`models-final-e2e-2.json`已结束：551项中543P/0F/8S/0flaky，退出0、3354.88秒；PC167P/2S、移动166P/3S、Electron210P/3S。明确`--update-snapshots=none --retries=0`，没有自动重试或修改截图；原13F全部在完整回归通过，首败报告不改写。8S为3项不适用视口、3项真实账号入口、1项付费Provider入口和1项真实Key连接入口，不计通过。当前1807项摘要复核仍为`aafa2f35eef447fbc2d867387ec67f4b241d0d20ef955bfb15ffc6e8fbd72439`，无源码漂移。
- 新增账号模型子项按原verify完成：组件22P验证偏好持久化、换号/凭据隔离、价格变化重新确认、离线/缺价拒绝；完整API账号模型15P及准入4P验证动态新增、同协议版本云价更新、分组、两种模型、幂等与冻结费用；Worker真实PG/HTTP验证实际generations/edits模型。最终PC/移动`web.scheme-account-models`及`electron.account-models`覆盖普通和方案试跑/正式、参考图、实际模型/云价与原费用；恢复重试本轮全过。仅关闭新增功能子项，任务87/94；受控上游不等于真实收费账号验收。

#### 完成审计与外部依赖

| 原要求 | 当前权威证据 | 结论/尚缺 |
| --- | --- | --- |
| 新增模型、可切换、云端价格权威 | 当前源码、组件/目录/准入测试、完整API/Worker/Web/Electron模型用例 | 新增功能本机联合验收完成；真实账号正向仍缺有效凭据 |
| 当前源码完整自动化与自然生命周期 | check38/38、API1138、Worker322、E2E543/0F/8S、自然到期3604.227秒、同源摘要 | 本机范围通过；8S不当通过，首败保留 |
| 旧数据与实际产物升级 | 当前arm64实际包7P、三授权旧备份3P、原数据SHA不变；独立SQLite→HTTP/PG同步12P | 本机私有副本通过；不外推Windows、系统安装器或真实账号双设备 |
| 原平台与发布矩阵 | 本机实际镜像18P、源码/包扫描0/0 | Windows/其他目标架构、正式签名安装、远端CI/TLS/独立远程MCP与获授权生产仍缺 |
| 完整功能对齐、旧入口兼容及最终交付 | 原范围保留，首败/修复/最终报告已收齐 | 其余7父任务与完整MVP尚未验收；正式manifest仍unregistered，未暂存/提交/推送 |

下一外部输入为确认仓库外私有环境文件中的真实账号凭据是否已更新；不在聊天、报告或仓库接收密码/Key，不重试已拒绝且未更新的凭据。Windows/正式签名与远端验收需要可用环境和相应授权，不以本机夹具降低范围。该候选本机自动化结果保留；接续审计发现的verified managed S兼容缺陷按上方最新节修复并重新验证，不用此前全绿替代新增路径。

### 09-20 恢复竞争测试补齐模型身份暂停路径

- 原完整549项在`electron.retry-recovery.spec.ts`新增4F（累计13F）：binding/before-claim × account/restore均等待20秒仍没有命中marker。该文件其余receipt换号/恢复、已接受重试父任务变化和取消回包损坏场景已通过。检查确认主进程fetch钩子仅识别旧`/account/execution-binding`，而显式模型已通过`/account/models`执行第一次身份核对和提交前复核。
- 等该文件全部10项执行结束后，才修改`retry-recovery-helpers.ts`同时识别两条身份路径；原四项的数据隔离、恢复库、迟到响应、零重发和费用断言保留。新增marker必须命中当前模型目录及第1/2次读取的精确断言，并核对子任务冻结model和binding.model未变。`models-final-fixture-types-4.log`退出0，Biome及diff检查通过；Electron尚未复验，原4F不改写。
- 当前1807项源码摘要`aafa2f35eef447fbc2d867387ec67f4b241d0d20ef955bfb15ffc6e8fbd72439`；`models-final-test-delta-5.json`以七个测试/夹具旧hash重建原`5b2a680d…`成功，生产代码未改。原完整三端仍运行；API原进程继续等自然到期，不另起重复测试。最终同源检查/完整回归、产物与原外部验收条件仍开放，86/94、未提交。

### 09-20 新模型重试改走目录，修复测试暂停点并复验真实服务

- 原549项回归新增1F（当前共9F）：`electron.cloud-retry.spec.ts`跨工作台/历史共享pending断言失败。源码核对表明显式模型的主进程身份核对已走`/account/models`，旧夹具`holdBinding`只阻塞`/account/execution-binding`，因而新重试实际继续执行而非停在测试预期的等待点；未修改共享UI或生产重试逻辑。
- 使用独立实际Hono/PG子进程先验证两条HTTP路径，`models-retry-authorization-hold-before.log`退出1，明确记录models在hold期间返回。夹具改为两条身份路径都暂停并暴露仅测试的held计数；修订E2E先等待实际held请求，核对尚无子任务/重试POST，再保留三个页面入口pending断言。新增子任务/实际HTTP仍用原冻结模型断言，凭据换代、唯一父子任务及原费用保留断言不变。
- `models-retry-authorization-hold-after-2.log`退出0：释放前两请求均暂停、零运行/Provider请求，释放后两个响应均契约有效且held归零。第一次after命令在根目录解析workspace包失败，原日志保留为命令环境问题，改用本地契约入口后通过。受影响真实云服务完整回归`models-final-cloud-fixture-regression-1.json`17P/0F/0S、退出0；Biome与`models-final-fixture-types-3.log`类型检查通过。Electron定向仍待原完整回归终态，不能把HTTP或17项结果充作UI复验。
- 当前1807项源码摘要`10511d4bac0f55e79093cbf29781c69623cd55058a5bbb4af014c5ff069cad81`，`models-final-test-delta-4.json`以五个测试/夹具旧hash精确重建最初`5b2a680d…`，生产源码未变。其中API目录变化仅为桌面联合测试子进程夹具，其消费者是`CloudServiceProcess`；不再笼统声称整个API目录未变。原API/Worker和包保持实际启动/构建身份，当前完整E2E已失败的报告不改写。
- 完整API原进程进入自然期限测试：`models-final-lifetime/package-lifetime-natural-GLfWNk/progress.json`记录UTC21:27:38启动、等待原UTC22:27:41左右的到期时间，未改系统时钟或租约。最终报告仍未生成。三端仍在原进程继续；五文件修订最终check/完整E2E、18项镜像条件测试、当前包三真实旧备份和原外部条件仍开放，86/94、未提交。

### 09-20 完整回归暴露账号模型夹具缺失，保留首败待复验

- Electron阶段另发现`electron.account-cloud.spec.ts`两项失败：回环账号服务没有`/account/models`响应，Composer按“云端模型与价格读取失败”正确阻止发送。现补契约校验目录，保留原只发送一次、丢回包后新PID恢复、本地图片缺失/云端已清理、原费用和凭据安全断言，并增加云价显示、实际HTTP model与重启后SQLite model断言。`models-final-fixture-types-2.log`退出0，Biome通过；两项浏览器复验未执行。原完整回归当前共8F（Web6+Electron2），不能因修订了夹具而改写为通过。
- 当前1807项摘要更新为`d86c6b936681543a34f89e26848563981d6a4c4f7d127a872011ddc3516f526b`；`models-final-test-delta-3.json`替回三个测试文件旧hash后精确重建原`5b2a680d…`，生产/API/Worker仍未变化。此前两文件差异为中间检查点。修订的两个E2E文件均在本轮对应场景已执行失败后修改；未构建或另起Playwright。manifest守卫`models-final-manifest-3.json`12P，当前及三份回切镜像ID只读检查存在；后续实际镜像/包/完整回归仍待原进程终态。
- 迁移README、路线图及逐屏目录已明确链接当前收口记录，并将早期“Web方案关闭/开放能力0%”摘要标为历史，避免旧索引被误用为当前产品缺口；未据此关闭任何父项。

- 原`models-final-e2e-1`完整549项仍在原进程运行，当前已出现6F：PC/移动各三项方案成功、准备拒绝、运行取消协议测试。失败现场均为Composer明确提示“请先登录并核对当前账号”并禁用发送；旧`web.design-schemes.spec.ts`为这三项也返回账号401，且没有账号模型目录。产品登录保护未被放宽。
- 仅为需要云运行的三项建立经契约校验的已登录账号和模型/价格目录，其余默认访客场景保留。新增访客方案禁止目录读取/准备/发送并保留输入的负例；成功路径另核对准备请求的所选model与expectedBinding，确认目录初读及提交前复核。Biome和`models-final-fixture-types-1.log`类型检查通过；浏览器复验尚未执行，须待原完整回归终态后串行运行，不把夹具改动直接记为6项已通过。
- 两个测试文件相对原候选有变化：独立DB迁移断言和上述Web夹具。1807项当前摘要`810fd87081a588ae9d7c92b1b7579c7d2de225e123a0127aab3eb7f38269790c`；`models-final-test-delta-2.json`替回两个文件各自旧hash，精确重建原`5b2a680d…`，证明生产/API/Worker无变化。旧单文件差异报告保留为阶段记录，不能继续称当前E2E源码未变；正在运行的报告仍保留实际启动输入。
- API与三端原句柄均再次确认存活，未另起回归或构建。后续顺序为收齐完整失败→修订场景定向复验→最新树完整check和全量回归；18项实际镜像条件测试、当前包三真实旧备份及原外部平台/账号/发布条件仍须完成。86/94，未提交、未推送。

### 09-20 补跑根目录条件测试：旧迁移断言修正

- 默认check中的75项条件跳过逐组补跑：`models-final-root-runtime-1.json`27P/0F/0S（正式桌面云运行→Hono/PG/Worker、实际MinIO方案清理、PG提交顺序同步）；`models-final-root-sync-1.json`12P/0F/0S（两独立SQLite客户端与真实鉴权HTTP/PG、HTTP发布竞争）。两进程退出0，合成身份/上游不替代真实账号费用。
- 三组显式隔离PG测试首轮`models-final-root-db-1.json`16P/1F/0S：唯一失败为`generation-execution.integration.test.ts`仍将完整迁移链记录数写死为12，而当前实际链已有30条；原数据、付款身份、费用与幂等断言未失败。改为读取实际SQL迁移链，逐条比较账本hash/created_at及顺序，保留其余断言，不修改生产迁移或删除测试。完整三文件复验`models-final-root-db-2.json`17P/0F/0S、退出0；首败保留。
- `models-final-evidence-plan-1.json`26P/0F/0S、退出0：部署计划14项（含实际Compose校验）及manifest守卫12项。`models-final-root-types-1.log`类型检查退出0，修改文件Biome通过、`git diff --check`通过。这里只关闭75项中的57个条件测试缺口；另18项为当前API/Web/Worker镜像迁移、受限角色与隔离staging/回切，须等三端回归结束后构建当前API/Web镜像并实跑，不能继承旧镜像结果。
- 唯一源码变化是上述独立DB测试文件，当前1807项摘要为`74b0b9c6a7ea28bdab26cd7a9f0df830f4f512e37770678ea1544731e5410a26`。`models-final-test-only-delta.json`保存该文件前后哈希：在当前全树仅替换回旧文件哈希，即重建出原`5b2a680d…`输入摘要，证明生产/E2E/API/Worker源码无其他变化。正在运行的API/549项E2E与现有包仍按实际启动输入记录，不改写其身份；修改后最终完整check待三端终态后执行。
- 三份已授权真实旧备份的准确路径已从本任务原执行记录恢复；只读校验各自SHA256仍与`real-backup-package-2.json`一致、均无活动WAL/SHM。后续仅用私有副本验证当前包，不新增读取活动库或凭据的范围。完整API/三端原进程仍在运行，未另起重复回归；86/94及原外部条件保持开放。

### 09-20 当前模型版本：完整检查、Worker 与实际包终态

- 已重新核对当前1807项源码，仍为`5b2a680d570544bec4fc1cf7e87b7d59083493fc3a09624475b8d98f9a18a23b`，打包后无漂移。`models-final-check-1.log`退出0、38/38，root3203P/75条件S、features850P；普通API/Worker任务的条件跳过不作为完整数据库通过。源码扫描0 findings/0 errors。
- `models-final-worker-1.json`完整322P/0F/0S，进程已退出0；使用生产Dockerfile构建的不可变镜像`sha256:b03d0b32ee5f4fc3902f0535d6ac6ed2b447b2baf79db79100ddb52b2b66303e`，开启数据库及实际容器测试。`models-final-api-1`仍确认存活，开启自然一小时生命周期，最终结果尚未取得；不重启或重复派发原进程。
- 当前macOS arm64 ad-hoc包构建退出0；实际包`models-final-package-smoke-1.json`7P/0F/0S/0flaky、退出0，`models-final-package-security-1.json`退出0、6个扫描目标/2个交付归档、0 findings/0 errors。DMG SHA256为`8ec059c281d67a26e1b1f06a65ec19f5f65a8d1e3bf8162c792bae8f1f44cda0`，ZIP为`2099186ed6bba9a99cf954c433e0ea2e678a76b6d19ce5de7070616c636126f4`。旧包保留在本轮报告目录的`pre-model-mac-GhJOA6/`，未操作正式App。此结果不替代当前包的三真实旧备份升级、系统安装、Windows或正式签名。
- 所有构建和包测试结束后，`models-final-e2e-1`已从当前生产构建启动完整549项三形态回归，`RUN_DATABASE_TESTS=true`，未更新快照、未增加自动重试；终态待收。较早完整530P/2F/8S与定向修复报告原样保留，不由本段改写。
- 当前证据索引为`tests/v25/.results/closeout-20260919/models-final-source-identity.json`。文档与报告更新不改变源码摘要；正式manifest仍保持未注册，不以dirty工作树和局部测试填成最终pass。任务86/94、真实账号/平台/远端CI/TLS/远程MCP/生产条件保持开放，尚未提交或推送。

### 09-20 方案共享模型入口与实际宿主联合收尾

- 方案试跑/正式运行已启用共享模型选择，并将model/expectedBinding经共享与桌面映射送入权威准备/运行入口；目录复核先于建会话，保留输入与引用生命周期，同步提交锁防双击，取消/换号后不继续提交。创建/修改仍用独立文本授权，不读取图像目录。features相关64P、桌面映射3P；第一轮创建夹具在终态后误断言普通目录不应读取，改为保持文本任务在途验证，保留首败。
- `models-scheme-ui-full-check-1.log`完整check退出0：38/38、root3199P/75条件S、features850P、API373P/765数据库条件S；不等于完整数据库套件。本段后续新增联合测试/夹具/截图仍需最终完整检查。
- `models-scheme-ui-joint-1.json`为3P/0F/0S/0flaky：PC与移动实际API/PG/Worker以gpt-image-2试跑、musefold-image-pro正式运行，核对不同云价、实际编辑HTTP模型及参考图SHA、试跑资格/相册和原历史费用；Electron真实IPC/SQLite/Hono/PG/Worker验证普通与正式模型、价格变化首次零发送、再次明确发送、连接默认模型不改、目录失败隐藏旧价。上游账号/价格/图片是合成服务；Electron从预合格方案夹具开始，真实试跑资格由Web路径取得，不冒充真实付费账号或桌面完整试跑验收。
- Web首轮定位器漏算选项的价格文本且清理方法写错，第二轮PC180秒超时；定向trace证明转正成功通知遮挡主动作、鼠标悬停暂停通知计时。移开鼠标并等待自然关闭后真实点击通过，无force点击或生产通知改动。Electron新增测试空解构lint与测试辅助文件未登记导致类型检查失败，修复后`models-scheme-ui-types-4/5.log`退出0。
- 已逐图核对PC/移动完成态：模型区增加Composer高度、登录身份/会话标题与现行重试资格导致有意差异，原用户消息仍存在且滚动至顶部可见。新增可达性断言后两项仅截图失败，保留`models-ui-visual-assertion-1`；仅重收两张`workbench-finished.png`，关闭更新后的完整工作台文件32项已通过（`models-scheme-ui-joint-2`中Web部分），Electron参考图/提示词投影与身份变化扩展仍在验证。
- Electron扩展复现了生产缺陷：路径哈希先解析macOS的`/var`别名，原受管上传目录判断只比较声明路径，合法图片因此被拒绝且零图片POST。新增独立路径测试先3P/1F，只规范化系统父目录别名、不跟随app-owned根或uploads替换链接后，路径/本地图片/方案驱动/账号运行联合60P。`models-scheme-ui-full-check-2.log`退出0（38/38、root3203P/75条件S、features850P），源码扫描0/0。当前新构建`models-scheme-ui-joint-3.json`为14P/1F：参考图实际HTTP模型和SHA、提示词仅本地投影、费用保留均已走通；唯一失败是身份变化错误同时显示在inline和toast，测试文本定位器歧义，已改为核对inline testid，定向复验中。全量门禁仍须纳入最后的测试增量。
- `models-scheme-ui-electron-4.json`定向1P/0F/0S：实际Electron所选模型与真实参考图经主进程上传、实际编辑HTTP的SHA一致；本地提示词快照保留且其ID/工作台元数据不传云端，原模型/费用不被后续运行改写；凭据版本变化在提交前拒绝且保留输入，目录读取失败隐藏旧价并禁止发送。唯一定位器修正不改变产品断言，原14P/1F报告保留。实际提交前截图已查看，模型/价格和组合上下文可见。
- 当前1807项输入SHA256为`5b2a680d570544bec4fc1cf7e87b7d59083493fc3a09624475b8d98f9a18a23b`，记录`models-final-source-identity.json`。最终完整check与完整API（开启真实一小时生命周期）已启动，Worker正式镜像正在重建；终态未收，完整三端/安装包与manifest仍待。86/94和原外部条件保持开放，未重试无效实网凭据、未执行真实收费或修改正式App/生产，未提交/推送。

### 09-20 普通模型产品面与双端方案模型执行接续

- 普通生成共享 Composer 已接账号模型选择、云端单价、按账号隔离的本机模型偏好、发送前重新核对和目录失效阻断；不保存本地价格兜底。首次默认展示的模型原先会在目录移除/缺价后静默改选，负例先复现20P/2F，修复后22P。完整`models-ui-full-check-2.log`为38/38、根3180P/75条件S、features837P；PC/移动键盘选择、刷新持久化、换号隔离、价格变化与实际请求专项`models-ui-browser-1.json`为6P/0F/0S。两张实际截图已查看。随后工作台全文件32项回归`models-ui-workbench-browser-1.json`为30P/2F，失败均为完成页视觉基线差异；仍待逐图核对与关闭更新复验，不提前当作已接受的基线变化。
- 云端方案设置增加可选model与显示身份期望；API准备/运行从当前账号目录授权，固定计划、任务providerModel、Worker请求保持同一模型。已受理重放不再读取当前目录，改变模型复用key拒绝。契约/计划103P，真实PG/API/回执联合`models-scheme-api-2.json`为23P，含真实Worker子进程对受控HTTP分别发送试跑与正式模型。旧省略model输入维持原默认摘要。
- 桌面方案准备与运行入口现已接主进程核对后的模型绑定；云连接行的默认model不改，方案计划与方案/生成历史冻结所选model。保留确定性方案编译和相册语义，云图像通过既有R/S持久执行通道发送，单次运行共享预留、每个原图一个子请求；本地工作台/提示词引用只用于受控本地投影，不发到云API。普通旧R/S未显式选模型时保持旧请求形状。已受理重放只查询原账本，目录失效不触发重发。
- 桌面模型运行负例首先复现28P/4F，固定默认模型、缺价未拦截和子模型不匹配问题修复后32P。新增联合夹具先后因来源枚举错误、缺必填数组、未选择封面而失败，按真实产品前置补齐；发送后取消专项另揭示旧HTTP夹具把claimed写成not_sent/0，账本正确拒绝矛盾回执。诊断证明取消意图已持久化，修夹具为unknown/null后通过，未放宽账本规则。首败/诊断均保留。
- 最终桌面联合`models-scheme-desktop-joint-3.json` **167P/0F/0S**，包含真实SQLite+回环HTTP的试跑/正式不同模型、两张历史归组/模型/回执、试跑相册与正式不入相册、准备后缺价零发送、发送前及已领取后取消、原费用结算和剩余图片零发送；也覆盖旧固定计划/IPC/adapter回归。新驱动有就地注册失败/取消/重放/异常传播测试。不是实际Electron多模型UI或真实付费上游证据。
- 当前完整`models-scheme-desktop-full-check-1.log`退出0：**38/38**、根**3196P/75条件S**、features**837P**，含双端构建及最后Web构建；默认API765S、Worker232S属于数据库条件跳过，不能冒称本轮完整数据库验证。`models-scheme-desktop-source-scan-1.json`为0 findings/0 errors，`git diff --check`通过。当前构建串行执行的既有Electron工作台回归`models-scheme-desktop-electron-workbench-1.json`为**11P/0F/0S/0flaky**（30.22秒），含既有方案成功/取消路径；不替代新增账号多模型UI联合验收。期间未覆盖应用构建。Spec校验退出1，仍为86/94及两项未勾验收，不提前归档或提交。
- 仍待：共享方案提交映射与模型选择入口、参考图/提示词引用和身份变化的跨端组合、当前真实Electron账号模型联合路径、工作台视觉失败、最终同源完整API/Worker/E2E/安装包与验收manifest。旧源摘要及旧包不作为本增量身份。任务86/94和原外部账号/平台/发布条件保持开放；未改正式安装App、线上配置或执行真实收费，尚未提交/推送。

### 09-20 账号多模型：权威准入、实际发送与桌面持久请求

- 账号目录的鉴权定价、身份/凭据/分组前后校验已接通 API、Web gateway 和桌面 IPC；上一片联合单元212P、真实PG/Better Auth/受控上游51P，完整`models-full-check-2.log`退出0（38/38，根3168P/75条件S）。这些结果不代表模型选择产品面已完成。
- 云请求增加可选`model`，执行绑定支持冻结模型标识；不把客户端`expectedBinding`变成授权。新显式选择逐次读取当前账号目录，检查账号可用性、图像传输和有效云价格；事务锁定账号后再次比对目录身份/凭据。已受理幂等键先重放、不依赖当前目录；换模型复用旧key冲突。重试保留父任务模型，重新校验当前可用性，不修改父任务费用。省略model的旧客户端/旧冻结请求仍保留原默认模型与摘要语义，不能凭expectedBinding选新模型。
- 图像能力与价格分离：采用New API明确的`image-generation`端点；当前部署仅公布`openai`的三个图像别名（musefold-image-pro、musefold-image、gpt-image-2）作为兼容传输名单，仍必须出现在当前账号目录并有适用云价格。未将所有聊天模型放行，未引入本地价格或本地分组倍率；端点定义依据[上游类型](https://github.com/QuantumNous/new-api/blob/main/relaykit/types/endpoint_type.go)。尚未逐模型执行真实收费能力探测。
- Worker在最终发送事务核对请求model、运行providerModel、执行回执binding.model和请求摘要，HTTP JSON与编辑multipart均使用同一冻结模型；模型不一致时零图像POST。新增选择不能借目录价格生成虚构已结费用，既有未知费用仍为null。
- 桌面主进程在登记和发送前从当前账号目录构造模型期望，本地持久请求/支出绑定/生成记录保留所选模型，登记或子请求模型不一致即拒绝。账号连接行保持原身份，不通过修改连接默认模型影响历史记录。新模型目前已可沿内部协议提交，尚未提供共享Composer选择入口、偏好持久化或价格展示。
- API准入/目录/原回执联合`models-admission-api-2.json` **52P/0F/0S**；Worker真实PG和受控HTTP`models-dispatch-worker-pg-1.json` **27P/0F/0S**，包括新模型两种端点及请求/记录/回执错配；契约/DB/domain单元53P、Worker网关与生命周期单元74P。桌面真实SQLite/回环HTTP、持久账本和契约`models-desktop-transport-2.json` **123P/0F/0S**，两种模型进入本地记录和HTTP body，目录读取不传凭据给渲染层。
- 保留首败：API第一轮44P/8F来自测试未清理独立持久回执，以及缺价格时先报告能力不可用；前者只修测试隔离，后者明确价格失败语义。桌面首轮121P/2F是新增测试混淆本地success与云端succeeded，按现有契约修正；生产成功状态不改。完整检查首轮在api-client旧目录夹具缺新增imageGeneration字段处失败；第二轮37/38（根3178P/2F/75S），另发现IPC夹具同样缺字段及R/S参考图变更测试轮询可能错过两子任务之间的变更窗口。补字段并在实际子任务间增加测试暂停点，完整保留第二张不上传/不POST/不收费断言，不修改生产发送逻辑；两个文件定向38P。
- 最终`models-dispatch-full-check-3.log`完整`pnpm run check`退出0：**38/38**，根**3180P/75条件S**、features**811P**，包含双端及末尾Web构建。默认跳过的数据库套件不能记为全量通过，本片仅有上述52P/27P实际集成；最终完整API/Worker与三端E2E仍须在产品面完成后重跑。源码扫描**0 findings/0 errors**（`models-dispatch-source-scan-2.json`），`git diff --check`退出0。当前1796项输入摘要`0546831427c85034b9b7fe553eef1226fc084da32532daaf877dbfe76777193e`（`models-dispatch-source-identity.json`），HEAD仍为dirty的dcdf8d0，不冒充已提交发布版本。
- 本片不关闭账号多模型或其他迁移父任务；共享UI/账号隔离的选择持久化、完整Web/Electron交互及最终同源包/全量回归仍待。未调用真实付费上游、未改变正式安装App或线上配置、未提交/推送。

### 09-20 真实旧备份实际包升级、中文搜索修复与多模型接续

- 最新完整三形态报告已结束：`deploy-full-e2e.json` **530P/2F/8S/0flaky**（3572.82秒）。两个失败分别为Electron分类删除/分类同步用例的登录前置失败；未改代码原样重跑相关四项 **4P/0F/0S**（`taxonomy-reproduce-2.json`）。保留全量2F，定向通过不替代新源码完整回归。
- 首次真实备份包验证 `real-backup-package.json` **0P/3F**：前两份发现旧SQL迁移重建FTS时丢失中文分词；第三份发现历史迁移hash与当前SQL不同。未修改历史SQL/接管机制或已应用账本；新增SQLite `0014_prompt-fts-state`，在core初始化时原子重建派生FTS及版本标记，失败回滚并可重试，100行分页保留完整64位rowid和workspace隔离。第三份测试改为验证历史账本原样保留、新增后缀匹配当前包，明确记录1项历史hash差异，不伪装来源一致。
- FTS先以独立用例复现0P/1F；修复过程中的游标忙错误和测试种子匹配过宽分别留存首败。最终数据库/迁移/安全夹具联合 **67P/0F/0S**（`fts-upgrade-suite.json`），完整check退出0、**38/38**、root **3125P/75条件S**、features **811P**（`fts-full-check-2.log`）。此前中断的`fts-full-check.log`只完成36/38，不记为通过。
- macOS arm64 ad-hoc重新打包退出0，旧App/DMG/ZIP完整保留于`pre-fts-mac-vYh1hS/`。严格包测试 **7P/0F/0S**（`fts-package-smoke.json`）。三份显式授权真实旧备份各由四个独立PID验证迁移/重放/媒体两次读取，最终 **3P/0F/0S**、39.45秒（`real-backup-package-2.json`）：均升级至15条迁移、integrity ok、无FK错误；分别保留31/31/19条原搜索查询、全部原行/列值与迁移前路径；前两份另回填77/87条历史运行和75/84条资产。之后只在停止的测试副本中重定位图片路径，分别读取并解码152/176/178个文件，核对199812333/254882176/260024413字节及SHA。样本间可能重叠，不把合计506个文件副本当独立作品数。
- 每份原备份和原图片均SHA不变，私有副本随测试清理；未读取活动用户库、未导出私有正文/图片/凭据、未改正式安装App。测试使用拒绝连接的云端地址，不产生真实调用。此为实际macOS包+私有旧备份副本，不是系统安装器升级、Windows、正式签名或真实账号验收。
- 本片打包/测试输入为1787项源码摘要`4023c3d889f136d4a04145b410aad9ce1af49252bbd9bd3c1969aaee9294b90f`，dirty HEAD仍为`dcdf8d0`；源码扫描0 findings/0 errors（`fts-source-scan.json`）。DMG SHA256 `730be5a55b3692638a591ccecf9ca3fe88632f6394c413a4bade917470941f9e`、ZIP `55d0b7856b21c454eb1885902fa8b3aa1e4d1bdf1cbbe339710ebda8e689a92b`、ASAR `0109d2978341cfbcbdffcb6ce8cf50bdc91639b6a1133c4dd9b4f2cb3959e420`，运行ASAR的21项out文件manifest为`809e341fce265cbf19e239667e6beff428dcb66d63cc5c7c112f5828c7e1c561`。后续多模型生产修改不能继承为最终同源验收。
- 账号多模型新增项开始落地：公开`/api/pricing`只读实际200，当前列有`musefold-image-pro`、`gpt-image-2`、`musefold-image`，价格不同；公开分组倍率不当作已登录账号适用价格。New API上游[定价控制器](https://github.com/QuantumNous/new-api/blob/main/controller/pricing.go)会覆盖账号分组倍率；传输层正在新增鉴权定价、严格价格/模型校验，缺失价不再补0，协议版本不作价格缓存依据。实际云目录将这些图像模型一并标记通用`openai`端点，不能仅凭该字段断言图像能力或放开全部聊天模型；账号目录/执行授权/共享选择/实际发送与费用联动仍须继续完成，父任务不勾选，未提交或发布。

### 09-20 隔离 Compose 业务、备份恢复与本地版本回切

- 通过实际计划生成器部署四个不可变运行镜像，独立 PG17/迁移 owner/三个受限角色/MinIO，受控 HTTP 账号和图片上游。完整专项最终 **8P/0F/0S**、72.54秒，`staging-rollback-3.json`。包括两种 Worker 的真实队列消费、运行角色 DDL 拒绝、1280/390 生产 Web 登录/提示词创建/刷新、owner/版本冲突/同步重放、图片成功/失败/排队取消、幂等无重发、实际 S3 字节与权限、物理清理及退出。两张已登录截图人工查看仅证明该页面呈现，不当作完整视觉 parity。
- 停止四个服务后实际 `pg_dump`，私有备份文件再复制回隔离容器用于 `pg_restore --single-transaction`；截断备份失败后新目标没有部分业务表且服务仍停。有效副本恢复到同一 PG 集群的新库，**69张表/108行**逐表逐行 hash 相等。两份真实对象各110字节，私有文件备份后复制到另一 MinIO；复制一项后注入异常、保持停服、原样续传并逐字节核对。重放镜像迁移/角色授权后仍保持数据库快照，实际 API 读取原任务/资产，原幂等 key 不新增图像调用；恢复库修改不影响原库。
- 图像响应的 `expiresAt` 由当前读取时间投影访问 URL 有效期，不是持久保留期限；首轮误作持久字段导致1F，已按实际服务语义检查其未来有效性，其他全部任务/资产字段仍逐项比较，底层全表比较不减弱。原首败保留为`staging-recovery.json`，正确复验6P为`staging-recovery-2.json`。
- 使用生成的真实 rollback steps，把 API/Worker/Web 分别从 `ea5a2312…/8a22f71e…/483eec1e…` 切到实际不同的历史镜像 `dc104a94…/be6aca12…/36ea4e7a…`，逐个核对运行容器 `.Image`，在当前扩展 schema 上读取双视口提示词/旧图片、完成一次受控生成，再切回当前镜像核对结果与图像调用次数。迁移账本不变，无数据库 down。初次三版本回切7P、补双Worker探针后8P、最后磁盘备份/损坏副本联合8P，分别保留报告。
- 初始化首次失败为Vitest `expect.poll`不能在beforeAll中使用，改为有界`vi.waitFor`。第二轮3P/2F来自测试表名误写和缺Origin退出被正确拒绝；改为实际`object_cleanup_queue`，保留缺Origin403负例，正常退出走产品页面。最后物理S3清理断言仍保留，不以API404替代文件已删。
- 新部署测试全部纳入`tests/repo/tsconfig.json`，类型检查首次发现两个闭包内数组隐式any，显式类型修正后通过。最终非应用构建检查 **36/36**，root **3117P/75条件S**、features811P（`staging-nonbuild-check-3.log`）；其中新增8项环境条件S已由上述实际镜像8P独立覆盖。源码扫描 **0 findings/0 errors**，`staging-source-scan.json`。当前1778项摘要`7d7c783d7815fae14f525ee4207860b1f6df38b5bb7d72d5d64631d7856ba54e`，`staging-source-identity.json`。
- 本增量仅为部署测试/fixture、类型检查接入、测试环境白名单和运行说明，未改生产应用逻辑。完整540项E2E仍在原`87010207…`输入运行，已进入Electron，确认原session存活；不重复启动、不在其间覆盖应用构建。当前增量完整check待其结束后执行。Spec仍退出1、86/93、完整MVP未验；尚未提交。
- **边界不变**：这里是loopback、受控账号/图片上游、实际MinIO及本地留存镜像，不证明真实账号/TLS、正式发布来源或生产安全配置。源身份字段为明确夹具值，不是镜像来源证明；恢复保留原全局角色/密钥，未覆盖跨集群/在线PITR/备份后撤权/整机故障；历史构建非已发布正式候选。完整R04、其余迁移与外部平台条件不关闭，未改线上、未收费、未tag/push/feed发布。私有临时备份和自建容器随测试清理，用户数据及原安装App未动。运行入口与边界见`scripts/deploy/V25-README.md`。

### 09-20 接续：运行角色与回归终态

- 当前API镜像 `sha256:ea5a2312c13ffc2fdf052cd66a9c91fab1b7be9f08cf14e8a220e5da247b62e9` 受控迁移 **2P**（`deploy-image-current.json`），构建退出0。
- 新增 `infra/v2.5/runtime-grants.sql` 与真实镜像角色测试。首次 **4P/1F** 揭示 healthz/启动日志虽正常，API入队仍被Graphile `_private_tasks` RLS拒绝；没有授予BYPASSRLS或停用RLS。四张Graphile私有表只向三个指定运行角色授予定向策略，未知schema版本/表集合要求重审；事务重放、DDL/账本写入拒绝、无关角色即使获DML仍不能入队、未知表回滚授权均验证。当前同一API和Worker镜像实际启动/两个任务消费/SIGTERM正常退出，最终 **7P/0F**（`deploy-runtime-roles-2.json`）。首败保留。此为受限服务角色，不冒称租户RLS/域隔离或完整staging。
- 部署计划显式插入迁移后、运行前的授权审核步骤，计划仍不执行发布；新增角色步骤后的planner **14P**（包含实际Docker Compose校验，`deploy-plan-roles.json`）。Turbo根测试补充角色测试所需镜像变量和infra输入，避免遗漏SQL变动。
- 容量三故障 **3P**、HTTP retention **4P**；第二轮完整API **1112P/1F/1专用小时S**，没有上一轮未处理PG异常。唯一失败是双真实Worker进程/PG/HTTP/S3接续默认5秒超时；原样定向复验1P（1.92秒）后，为真实进程用例设30秒边界，保持所有PID、来源次数、S3与重放断言。第三轮完整API `deploy-api-full-2` 进行中，不据定向通过记全绿。
- 原跨端540项和自然一小时测试仍待终态；移动视觉旧基线差异已核对符合已登记D44入口。新增“已移除”可见/可访问名/视口内及无横向溢出断言，尚待原回归结束后更新PC/移动快照并复验。Web生产镜像另在构建，用于下一步隔离部署；不代表已完成staging。
- 本节变更使下节 `b0a10838…` 身份过期；最终完整check、当前全量E2E、扫描和源身份须另行更新。所有发布父条件、外部账号与平台限制不变，尚未Git提交。

### 自然一小时与部署增量检查终态

- 原一小时生命周期测试已按原始期限结束：**2P/0F/0S**、命令退出0（`purge-lifetime.json`，总3613.85秒；自然期限用例3604.21秒）。真实API请求阻塞、Worker新PID、到期前保护/到期后清理、保留回执和已采用资产均通过；不修改时钟或期限。实际组件为Better Auth/Hono/PG/Worker bin，S3为受控HTTP夹具，不能据此改称真实MinIO小时耐久或实际云账号验收。
- 新角色/部署步骤/视觉断言后的非应用构建检查 **36/36**，root3117P/66条件S、features811P；角色7项条件S已由真实镜像专项单独7P覆盖。仍不是完整check。源码扫描1778项，0 findings/0 errors（`deploy-roles-source-scan.json`）。
- Web正式Dockerfile构建退出0，镜像 `sha256:483eec1e47825f3c8ba8317db27ffc2b2b2cdc544a0166548029a7521cd323bf`；尚未以此镜像完成完整staging业务验收。
- 第三轮完整API终态 **1113P/0F/1专用小时S**，93文件、694.30秒、命令退出0（`deploy-api-full-2.json`）；一小时S由上述独立自然2P覆盖，保留两次完整首败与原样定向证据。
- 角色所有权检查进一步改用PG共享依赖目录，覆盖函数/类型及其他DDL对象；新增实际函数所有权拒绝用例，最终实际镜像 **8P/0F**（`deploy-runtime-roles-3.json`）。没有只检查表后漏放可修改的自有函数。
- Web镜像以非root/read-only/cap-drop配置实际启动，`/settings`及16个Next脚本资源均200；SIGTERM退出143，已移除本次临时容器。记录在`deploy-web-liveness.json`，仅为存活和资源完整性，不是同源登录、假Provider生图或完整readiness。
- 原540项完整跨端终态 **531P/1F/8S/0flaky**，3774.30秒（`purge-full-e2e.json`）。唯一失败仍是D44移动旧列表基线；8S分别为三端实网账号3、真实Key连接1、独立收费生图1、互斥视口3。账号输入仍待有效核对；收费用例沿LIVE25已通过单张证据，不重复收费。按UI规范D44与UI/UX无溢出规则，Playwright仅重收PC/移动方案列表两张截图并实际看图：PC按钮同排，移动新建合法换行、没有新增裁切或横向溢出。新增入口可见/可访问名/视口内断言；更新2P、关闭更新后独立复验2P（`deploy-list-baseline-{update,verify}.json`）。旧完整报告仍保留1F，不改写为零失败。
- 当前1775项源码摘要为`870102073dc4631aa09bf97d9ca58e194ba1507e2a7bce64b96cf4205c4c274d`（`deploy-roles-source-identity.json`）。角色8P后源码扫描再次0/0；最终完整check已启动，当前完整E2E待重跑。新包和完整staging/原外部条件仍开放。
- 当前完整`pnpm run check`已退出0：**38/38**、root3117P/67条件S、features811P，包含双端构建及末尾Web构建（`deploy-full-check.log`）。API/Worker数据库条件跳过由本节完整1113P+自然小时2P及前节315P分别覆盖，不能把根默认skip计成通过。三端完整重跑尚待。
- macOS当前包构建前，已保留原09-15 DMG、ZIP与完整App到`previous-mac-Ss83pK/`；旧DMG SHA256 `283a35f9ea70a917c70e91cb06a50412355ba853915e07d91531da96597acfb6`、ZIP `91a0f7c169aa7804a3549dafe7ba383808b90c3e7cc9406cce82799a39577477`、ASAR `b2f55c602ccb711c79058e595fd03387aa9edadef71775443aee514a4dd197a4`。本次`package:mac:adhoc`已启动，仅本机arm64临时签名，不修改正式安装App、不发布或更新feed；构建/严格包冒烟/产物扫描终态待验。

### 当前macOS包已验；完整跨端重新运行

- `package:mac:adhoc`退出0，codesign ad-hoc验证通过。严格包测试 **7P/0F/0S/0flaky**（32.4秒，`deploy-package-smoke.json`），包含CLI中断/新进程清理、随包native句柄、内联迁移/重启、受管前缀10/11升级及作品/费用/永久删除身份保留、关闭端口和上传副本清理。此为实际包与受控前缀，不替代真实用户旧库安装升级、Windows、其他架构或正式签名。
- 产物扫描 **0 findings/0 errors**，6 targets、2 installers（`deploy-package-security.json`）。当前DMG SHA256 `f47a190b7542bc58d90aa09be4b27a0bc0decc82ca4c64d6d9b02dc91adda0dd`、ZIP `06ae7eb441cd8ae8c81a60a023327c7fe6d9b50867f36e7a95c6ddd70f15bea5`、ASAR `2c4bdaf2d207a09c2afac3f1029c9ee1af56af59dd1b5f3652cffc80ec47bd90`；清单`deploy-package-build-manifest.json`。清单HEAD仍是旧提交，必须与dirty源码`deploy-roles-source-identity.json`联合阅读，绝不是clean release/远端CI证明。
- 启动前重新计算1775项源码，仍为`87010207…`，漂移0。当前完整540项`deploy-full-e2e`已开始，先完成构建再逐项执行；终态尚未取得，不能提前写全绿。该次JSON因Playwright相对路径解析会先落在`tests/v25/tests/v25/.results/closeout-20260919/deploy-full-e2e.json`，结束后移到标准报告目录；已完成的两次基线与包报告已移回标准目录，内容未改写。
- Spec验收命令退出1：任务86/93、完整MVP仍未勾选、整包验收未通过（`deploy-spec-check.log`）。保留这一真实边界，不归档或提前Git提交。完整staging/readiness/回滚/远程双MCP、真实账号/平台/安装升级/远端CI与生产授权仍按原父任务继续。

### 部署入口与完整回归续进

- 新 Worker 镜像 `sha256:8a22f71e…` 构建成功。首轮完整 Worker 为312P/3F：2项实际对象存储 IO/停启用例触及默认5秒；1项混用固定 now 与实际入队时钟，长于1秒时尚不可领取。原样定向18P后，保持全部业务/数量断言，为两项实际IO设30秒总时限，清理用例统一显式时钟并新增持久 next_attempt_at 断言。最终正式镜像完整 **315P/0F/0S**，`purge-worker-full-final.json`，包含真实五分钟 cron，不改生产 Worker 行为。
- 首轮完整API为1102P/1F/1专用小时S，另有1未处理PG异常。容量断连用例最初仅等首次PUT时抓取的租约：增加只读现场后，实证 final lease 可比初始快照晚95ms，按旧期限维护是否碰巧晚于新期限决定结果。已改为等待已停止导入的最终持久租约及 outbox 期限，不修改时钟或数据库日期；原失败与诊断通过分别保留。DesktopSync夹具沿已验证的socket-end等待模式修复：pool.end后等每个连接end再停PG，意外连接错误仍令关闭失败；真实HTTP retention 4P。完整容量三种故障和包含新迁移入口的完整API正在复验，未提前记绿。
- 已实现不发布的 `deploy:v25:plan`：严格源身份、镜像摘要、独立project/四份凭据文件、回环Web端口、只读非提权服务、单独migration profile与应用回滚计划。拒绝旧资源名、可变tag、目录穿越、未知字段和execute；readiness明确未验证，healthz不冒充就绪。旧部署资源未改动。与旧部署/桌面发布回归联合 **36P**，包含实际Docker Compose配置校验。
- API新增包内受控迁移入口，独立MIGRATION_DATABASE_URL/full SHA、PG并发锁、应用+Graphile顺序迁移、幂等回放和失败连接回收。真实PG/CLI **10P**；首轮CLI因缺connect错误处理器产生警告，补齐后通过；首轮类型检查因PG重载ReturnType推导成void，改用驱动PoolClient后通过。实际非root/read-only API镜像 `sha256:dc104a94…` **2P**，空库30条应用迁移/19条队列迁移及回放、拒绝runtime凭据回退均通过。初次“drizzle-kit不在生产镜像”假设被实际镜像推翻：其存在，但pnpm确实不存在；已纠正文档与断言。该镜像早于仅类型声明/测试调整，当前源码最终镜像另在构建，不冒称完全同源。
- 新增入口后的非应用构建检查（lint/typecheck/test/boundaries，包含native依赖就绪检查）**36/36**、root3117P/59条件S、features811P；不是完整`pnpm run check`。源码扫描0 findings/0 errors。当前1773项源码SHA256为`b0a10838df68217864741aa7364467400d056aef7cc4d709334e2304bfb534ba`，记录在`deploy-source-identity.json`。
- 原540项跨端回归仍运行，已出现移动端方案列表视觉1F。人工核对expected/actual：新增“已移除”入口使“新建”按钮换到下一行，文字和卡片未裁切；旧基线未含新入口。保留首败，待当前回归终态后由Playwright更新并复验，尚未覆盖基线。自然一小时测试仍按原16:15:25–26 UTC期限运行。当前旧完整回归不能作为新部署增量最终全绿证明。
- G-RELEASE-04仍未闭合：完整readiness、隔离staging、业务回滚与备份恢复、远程双MCP客户端、CI及生产授权均待验；角色权限后续结果见上节。新入口/计划说明见`scripts/deploy/V25-README.md`；不以此关闭全迁移或提前Git提交。

### 永久删除联合收尾（后续增量，不继承下方较早的全绿结论）

- 共享逐方案固定版本确认、账号切换隔离、失败显式重试及游标列表已接入。桌面事务保留运行/步骤/评估与结果资产，原生文件清理意图持久化；core 专项47P、组件专项34P。
- 新方法接入后全量检查首次37/38、根3095P/5F/56S：5F分别为旧方法数量断言和未覆盖purge的调用矩阵，已补齐且保持坏信封/坏响应/缺桥断言。更新检查另复现等待上游期间无生命周期保护（2F），已增加retain/finally释放，相关专项62P。随后`purge-check-3.log`完整check退出0（38/38，root3102P/56S、features811P，含最后Web构建）。
- 实际API/Better Auth/PG的PC与移动永久删除2P，覆盖分页故障、取消焦点、失败显式重试、原版本重放与刷新保留。Electron首轮1F：旧方案空sourceLabel在主进程默认生成后，被客户端再次解析拒绝；新增契约往返测试复现1F，修正默认值与合法值矛盾后105P、`purge-check-4`退出0。不删空来源旧数据、不用跳过或改夹具规避。
- Electron继续发现：未生成过图片时Pictures根目录不存在，导致其他合法受管根中的图片也返回404；主进程协议与就地测试均复现。修复为各受管根独立核对，缺失根不提供任何许可，其他路径及符号链接保护保持；专项25P。期间修正测试自身两处错误：renderer的fetch不属于CSP允许的图片读取路径，改为实际Image.decode加main协议字节核对；原1px PNG夹具不能完整解码，换为完整有效PNG。没有放宽CSP或生产图片校验。
- 最终专项`purge-e2e-complete.json` **3P/0F/0S/0flaky**（27.2秒），涵盖PC/移动实际HTTP/PG与Electron v9→v11、221条分页、确认取消/删除、专用文件清理、运行/步骤/评估/费用保留、三个不同PID、幂等重放与原图片解码/字节。截图人工核对仅覆盖确认框，不冒称全页视觉验收。`purge-check-5`完整check退出0（38/38、root3104P/56条件S、features811P及双端构建），源码扫描0 findings/0 errors。
- 当前源码身份为`purge-source-identity.json`：1765项、SHA256 `930b6676b2a964d179d670c3000f6b414809df1524b70bd6d127b47500667165`（非已提交版本）。完整540项三形态回归`purge-full-e2e`与单独自然一小时`purge-lifetime`已启动；后者原deadline为2026-09-19 16:15:25–26 UTC，不改时钟或期限。两者终态待收，Worker当前正式Dockerfile镜像构建亦在进行，不能提前登记通过。
- 原始首败、终态日志、截图与独立报告均位于`tests/v25/.results/closeout-20260919/`。上述属于开发构建、隔离数据与受控认证证据，不替代真实账号、安装包升级、Windows/签名、部署回滚或最终全量验收；原父任务保持开放。

### 较早检查点

- 已复核根、desktop、web-next、packages 约束及现有任务包；Spec 路由为继续实施。
- Docker 的既有 PG/MinIO 服务可用；当前本机为 macOS arm64，尚未取得 Windows 或远端发布验收结果。
- macOS/Windows 产物清单已接入生成、独立上传和 Release 附件，避免改变安装包 artifact 根目录；修正 runner 元数据字段为 `RUNNER_OS` / `RUNNER_ARCH`。相关 21 项测试通过。
- 桌面独立方案库增加 v10 迁移（不是主库 Drizzle 迁移）：允许终态运行保留原 scheme/revision 身份后解除关联，步骤和评估保留；永久删除身份表及 INSERT/UPDATE 防复活触发器已加入。回滚、损坏旧引用拒绝、记录保留专项通过；永久删除服务及文件清理尚未接通。
- 共享列表增加显式 `deletedOnly`；桌面改为 SQL keyset 分页，不再截断到 200 条；云端游标保留 PostgreSQL 微秒精度。两端游标绑定查询范围，拒绝旧式/损坏/跨范围游标；搜索按文字处理 `%` / `_`。
- 合同/仓储/IPC/发布专项 191P；真实 PG 5P，其中 205 条已移除方案逐页删除仍完整遍历；真实 Electron 开发构建 1P，v9→v10 并跨两个 PID 遍历 221 条软删方案，步骤、评估和费用政策保留。这里只证明开发 bundle，不替代安装包或真实用户旧库验收。
- 完整 check 的 SQLite pragma 类型首败、新增测试空解构 lint 失败已修。随后 37/38 暴露既有 C3-B 三进程恢复用例默认 5 秒总时限；PG 205 条用例并发负载下亦触及 5 秒。全部行为与数量断言保留，将这两类真实进程/DB 用例设为 30 秒；复验 C3-B 3P、PG 5P。最终 `pnpm run check` 38/38（0 缓存），根单测 3080P/56 条件 S、features 803P，双端构建通过；该构建再次运行真实 Electron 升级用例 1P/0F/0S/0flaky（8.1 秒），源码扫描 0 findings/0 errors。首败与终态报告保留在 `tests/v25/.results/closeout-20260919/`。
- 只读 SSH 检查：`musefold-cloud` 可达，仍运行 v1.1 服务，未修改线上状态；`libai` 连接超时，未取得其他原生平台验收条件。已请求用户核对仓库外私有配置中的账号凭据，不在聊天接收密码。
