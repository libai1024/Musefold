# v2.5 真实验证准备与首轮验收报告

2026-09-19，macOS arm64。本轮已达到可执行真实账号与中转站验证的本地环境状态；TvT 单张实网生图及重启保留通过。**账号正向验收未通过：既有账号服务拒绝所提供的凭据，待用户确认账号服务或更新密码。** 这不是 v2.5 全平台发布通过报告。

## 当前可用入口

| 组件 | 状态 |
| --- | --- |
| 桌面开发版 | 已运行，保留现有本机数据；主进程账号 API 指向本机验证服务 |
| PC / Mobile Web | [本机验证页](http://127.0.0.1:3399/settings)，当前 standalone 构建，HTTP 200 |
| API | `127.0.0.1:8787`，`/healthz` 返回 `ok` |
| PostgreSQL | 独立 `musefold_live25_20260919`，30 条迁移；原 `musefold_v25` 仍为 7 条，未覆盖升级 |
| S3 / 生图 Worker | 独立验证 bucket 已建立，Worker 启动且周期任务成功 |
| 方案 Agent Worker | 已启动；使用上游公开定价列表中的 `musefold-agent` 别名。尚未执行真实账号的文本编排 |

配置与重跑说明见 [真实验证操作说明](./V25-LIVE-VERIFICATION.md)。账号服务为 `https://zhaozhaoyue.top`，TvT 连接为 `https://ai.tvt.wiki/v1`；两者职责不同。凭据保存在本机仓库外的私有环境文件中，没有写入本报告或源码。付费生图开关已复位为关闭。

## 本轮完善

1. 新增 `pnpm run test:e2e:live`：先验证环境变量，再构建两个宿主，使用独立实网配置执行；缺凭据直接失败。
2. 新增 TvT 实网闭环用例：通过设置保存密钥、测试连接，工作台明确生成 1 张标准质量图片，检查图片读取、SQLite 记录、新进程重启后原图片 hash 与记录不变。
3. 真实生图需要独立开关、明确 HTTPS 地址及模型；关闭自动重试，避免测试失败后再次收费。
4. 修复验收工具的信息暴露：Playwright 的自动失败页面快照会包含密码输入值。实网模式现禁用此快照，独立配置同时关闭 trace、自动截图和视频。首轮失败报告已脱敏，含快照的生成 HTML 已移除，失败结果保留。

本轮新增 4 个源码/测试文件、修改 3 个配置文件，没有变更业务契约、数据库 schema、产品页面或版本号。上一轮设计方案永久删除后端和旧数据库副本验证见 [测试续篇 §5.113](./V25-MIGRATION-TESTING-CONTINUED.md#5113-d02purge方案永久删除后端与真实旧库副本验证2026-09-19)。

## 实测结果

| 验证 | 结果与范围 |
| --- | --- |
| `pnpm run check` | 最终 38/38 成功，33 缓存；根单测 3058 通过、56 个条件跳过；双端构建成功 |
| 实网前置条件单测 | 8 项通过：缺凭据、独立收费开关、缺地址/模型/key、URL 凭据及参数拒绝 |
| TvT `/models` | 实际密钥请求 HTTP 200；包含 `gpt-image-2`、`gpt-image-2.5` |
| 真实中转站生图 | 1 项通过，0 跳过/失败/自动重试；实际采用 `gpt-image-2`，1 张图片，1,377,412 字节 |
| 图片与重启 | 页面图片可解码；新 PID 后同一 run、同一资产、同一 SHA-256，SQLite 中仍只有 1 条生成记录 |
| 密钥检查 | 关闭 Electron 后扫描独立 userData 849 个文件，该 Key 明文 0 命中；本轮 39 个报告/附件文件再次扫描 0 命中 |
| 失败报告脱敏回归 | 在真实 Chromium 中填入合成私密值后故意失败，5 个生成文件均无该值；预期失败被正确记录 |
| 源码安全扫描 | findings 0、errors 0 |
| 真实账号 | PC Web / Mobile Web / Electron 共 3 项失败，均停在凭据拒绝；上游独立请求同样返回 `success=false`，未建立会话 |

账号失败后没有继续重复尝试，也没有绕过认证、修改上游密码或采用其他身份。提示词正向 CRUD、云额度出图、云端方案编排及真实账号同步因此未在本轮取得正向证据。TvT BYOK 成功不能代替这些结果。未取得可信上游账单，所以不报告具体费用或“零费用”。

首次统一检查已通过；追加 CLI 入口后的检查有 1 项既有 `managed-cloud-runtime` 素材恢复测试在默认等待窗口内未达到 `available`。该文件单独复验 26 项通过，保持代码和断言不变，完整检查再次 38/38 通过。首败和复验日志均保留，未用重试后结果抹除首败。最初一次 provider 命令因 CLI 参数顺序错误没有执行测试，修正参数后仅执行了上述一次实际收费用例。

![实际中转站成图与重启后的工作台](../../tests/v25/.results/live25-20260919/real-image.png)

## 证据归属与剩余范围

- 分支：`spec/2026-09-01_migrate-v21-to-v25`；HEAD：`dcdf8d036c27f87e33de301ad4077a3110bb5b7f`，包含大量既有未提交工作。本轮未提交、推送或部署。
- 本轮最终 1738 项源码输入摘要：`159045f071f5c643bde698bc32000a0fa3f9bd3fa49d77ae9ad8d6ed9b66bad1`，最终复核漂移 0。不能只用 HEAD 代表这些工作树产物。
- 脱敏原始证据：`tests/v25/.results/live25-20260919/`，含首败/通过报告、图片、迁移日志、最终检查日志、源码清单及扫描结果；汇总为 `acceptance.json`。
- 本轮没有复跑完整 Web/Electron 所有用例、完整数据库条件 API/Worker 集成测试，亦没有生成新的签名安装包。上轮数字只保留其原证据归属。
- 迁移父任务保持 86/93；这个数字不代表完成百分比。跨平台安装包身份绑定、真实旧库在原生安装包中的升级、Windows 真机、完整实网费用/双设备、正式部署与回滚仍按原范围开放。

本地开发和实网验证准备已完成；下一项外部输入是核对账号服务及有效登录凭据。更新私有文件中的密码后，可在保持 `MUSEFOLD_LIVE_GENERATION=0` 的情况下继续三端账号验证，不会再次生成收费图片。
