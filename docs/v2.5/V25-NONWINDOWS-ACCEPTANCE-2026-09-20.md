# v2.5 非 Windows 接续验收（2026-09-20）

> 09-20最新：登录会话机制开发、本地全量与产物验证、上游New API账号扩展生产部署均已完成。已获明确授权，先备份并实际恢复验证；仅账号服务重启，7.016秒健康，旧会话保留、真实新协议/释放通过、测试会话归零。事后字段比较仅xiaomiao.last_login_at因验证登录更新，积分/计费/其他用户不变。本地源码1850项bb580e98…，check38/38、API1153P且2条件另验、Worker322项实际覆盖、三端552P/0F/13S/0flaky、最终macOS包7P/真实备份3P、扫描0/0且构建字节匹配。13S原明细保留，未计全部实网通过。任务91/96，Musefold v1.1站点未切换；真实双客户端同步/收费方案仍待继续，不宣布非Windows整体完成，不提前提交。依据见[会话机制§8.3–§10](./V25-LOGIN-SESSIONS.md)。以下记录按日期保留历史，不覆盖最新终态。

> 09-20 12:36上海最新：按用户新授权SSH释放xiaomiao的50条阻塞会话，真实新登录/列表/退出/旧令牌401验证通过；其他418条会话摘要、余额与业务用量不变。两次免费探测产生的登录已各自退出，最终active=0。此前AUTH_SESSION_LIMIT阻塞已解除，但完整真实同步/收费方案尚未重跑，新增机制也仅完成设计，不能计为非Windows整体完成。详见[会话释放与设计](./V25-LOGIN-SESSIONS.md)；下文是前一轮原始证据，不改写首败。

## 范围与授权

用户要求 Windows App 验收晚些进行，先完成其他 v2.5 开发和测试。Windows 专属原生、安装及签名验收独立保留，不能记为通过；本次不再重试 Windows SSH。桌宠及其他冻结/暂缓域不扩大。

随后用户明确允许使用 xiaomiao 账号现有全部积分完成必要的真实收费测试，未设额外总费用上限。只消费必要测试调用，不自动充值，不把余额当成必须耗尽的预算；此授权不自动扩展到独立 BYOK 余额或生产发布。

## 本轮初始候选与增量

本轮初始候选源码 1813 文件，SHA-256 `91c6fca03d6d8b5fcdab042378aa0c1753a5afca5244029f5b485c2398e5f2cd`；逐文件清单位于 `tests/v25/.results/closeout-20260919/nonwindows-source-1.json`。下文最终候选已补结算夹具修正；未提交，未改版本号。

- 统一检查 `nonwindows-check-1.log` 退出0：38/38、0缓存；root3216P/75条件S，features850P。新增实网脚本随后完善，其最终完整门禁仍须再次确认。
- New API 登录 HTTP409 被错误映射为密码错误：先红后绿修复为脱敏、可重试的服务器错误，客户端完整87P。初次观察为409 / `success:false` / `Conflict`；后续官方网页对照确认`AUTH_SESSION_LIMIT`，详见下文，不能据此断言密码错误。
- C3-B 正式 Electron 主进程结算窗口：配置月预算25后，通过正式 Automation R HTTP入口提交新意图，持久状态仍为`pending_confirmation`；正式IPC拒绝后HTTP403，原请求费用仍unknown、没有新增Provider调用。`c3b-new-reservation-2.json`1P/0F/0S，无重试。工作台显式interactive同意不属于无人预算放行，未改变其产品语义。
- 完整API（包括自然一小时）、Worker（包括实际生产镜像）、根条件项与三端E2E正在本轮重新执行；尚无最终结果的报告不计通过。

### 本轮实际镜像与部署

三个正式Dockerfile构建均退出0，仅保存在本机，未推送或部署生产：

| 镜像 | 不可变ID |
|---|---|
| API | `sha256:43f3b2c3d42299d1a62cbfc0fd059e36d1d2fd53307564962da0ebaf86ae4432` |
| Worker | `sha256:878a95c27da7855db5062d0457e971c7fe21ea64d29cdd511f4591506e480e6f` |
| Web | `sha256:a2b32a2093127c352de2756208bf089e064b0dc32db578c40df8bcfe574e3246` |

`nonwindows-deployment-1.json`18P/0F/0S、进程退出0：实际镜像迁移2P、受限运行角色8P、隔离Compose/真实PG和对象存储/浏览器/队列/备份恢复/三旧镜像回切8P。回切到本机历史model-closeout镜像，原生产服务、正式数据未受影响；这不代表远端正式发布或生产灾备已验。`nonwindows-source-scan-1.json`源码扫描0 findings/0 errors。

根默认75个条件跳过已逐文件补齐：`nonwindows-root-conditions-1.json`40P/30S（其中27项条件覆盖、13项默认计划测试）；30S因部分历史入口要求`RUN_DATABASE_TESTS=1`而非`true`及独立Compose开关，未算通过。第二组显式配置这些开关并使用本轮新建、结束即关闭的独立PostgreSQL容器，`nonwindows-root-conditions-2.json`43P/0F/0S（补30项条件覆盖，重复13项默认计划测试）。加上述18项部署条件，恰好75/75条件项均有实际通过报告；两组进程均退出0，不重复累计总数。真实外部账号同步仍另列，不能由合成上游的双进程测试替代。

## 实网现状与证据安全

新增不收费跨客户端链路：PC Web创建提示词→macOS明确选择空库与同步同意→桌面编辑→移动Web编辑→新Electron PID拉取→仅删除自有测试提示词。

首跑`live-sync-1.json`在Web登录/创建成功后，桌面登录遭上游409拒绝；第二次`live-sync-2.json`在Web登录阶段遭同样上游冲突，界面已正确显示服务暂不可用。两个失败保留，不记为同步通过。公开状态接口200，未认证self接口401。

随后对官方网页做一次真实对照登录：公开状态明确`password_login_enabled=true`、`turnstile_check=false`，官方页面使用同一`/api/user/login`接口。网页实际返回409、`success:false`、`code:AUTH_SESSION_LIMIT`、`message:Conflict`，确认是账号会话数量达到上限；不是密码错误，也不是仅Musefold界面的报错。探测进程退出0仅表示成功取到失败响应，不能算登录通过；新增收费0。秘密只经关闭回显的stdin进入进程，未写文件/截图/trace/日志。两个前置探测脚本启动错误未提交登录。已请求用户在仍可登录的官方控制台或服务端释放不需要的会话名额，不擅自撤销其他设备会话。当前New API客户端仅有登录/刷新读取接口，无上游会话管理接口；不通过抽取旧令牌冒充新的正常登录。

官方页面客户端对此码明确提示：在已登录设备的Login sessions管理页释放旧会话；无法访问任何已登录设备时由用户自行重置密码。它与“近期签发次数过多”的`AUTH_SESSION_ISSUANCE_LIMIT`是两个不同错误，不把当前问题解释成等待冷却即可解除。另只读核对隔离LIVE25数据库，本轮唯一`Live sync <UUID>`测试提示词已软删（total1/active0/removed1），没有遗留活动测试提示词；此次核对没有写库。

首败的自动DOM报告包含密码框内容：仅将本次自有报告中的秘密替换为`[REDACTED]`，保留错误、调用栈与源码；没有删除失败报告。新增共享实网登录helper在提交捕获输入后清空受控密码框，trace/video/screenshot仍关闭。第二次真实失败报告确认不再包含密码值；本轮5个自有产物/临时目录共684文件精确扫描，密码及Key均0命中/0读取错误。`PLAYWRIGHT_NO_COPY_PROMPT`不足以单独防止matcher附带的DOM快照，不能再以该开关作为唯一证明。

另以独立无头浏览器、全合成账号响应检查上述helper，0ms及250ms响应均正确提交已捕获的密码并进入已登录视图，进程退出0；未向真实上游发送请求、未改源码。这证明脱敏清空不破坏受控成功路径，不替代实网登录和跨客户端同步验收。

收费联合脚本设独立`MUSEFOLD_LIVE_ACCOUNT_GENERATION=1`及显式账号图像模型，登录测试或仅存在Key不会授权消费。覆盖普通生图、真实Agent创建、试跑/封面/转正、另一模型正式运行、移动端修改/再次试跑/升级、无来源免费检查和真实方案包导出。它没有制造模型结果或写库授予试跑资格；目前尚未执行收费请求，新增上游消费为0。真实有变化GitHub来源更新与更完整异常矩阵仍结合受控正式服务证据判定，不能用无来源检查冒充。

## 原始失败与运行边界

- C3-B首次命令因`--project`参数吞入文件名而在选项解析阶段失败，未执行用例；第二次正确命令通过。
- 完整E2E第一次启动漏设数据库开关，已中断，不作完整验收；第二次因本轮中断留下的独立Next预览进程占用3399，在开跑前失败。核对PID、启动时间及cwd后，仅停止本轮自有进程；第三次显式`RUN_DATABASE_TESTS=true`并拒绝复用旧服务，继续执行。三次记录均保留。
- 历史544P、账号3P、TvT连接1P及旧arm64包证据见[前一收口记录](./V25-CLOSEOUT-2026-09-19.md)，不能直接替代本次变更后的最终验收。正式发布/签名公证/远端CI与TLS/MCP条件未由本机回归自动满足。

完整非Windows验收仍未宣布完成；Windows延期不改变其他项的真实证据要求。

## 结算夹具复核修正与最新终态

独立复核发现旧`armSettlementWindow`在暂停前调用`response.body.getReader()`却不释放锁，生产解码器无法读取该响应。因此仅凭原换号负例的`ok:false`不能独立证明身份保护已被执行；不沿用该负例作为充分证据。

先新增同账号正例，原钩子下`c3b-positive-red-2.json`实际失败（期望恢复成功，实际false），不是只做静态推断。删除钩子中reader获取，保持响应流原样交给生产解码器后，`c3b-barrier-green-1.json`3P/0F/0S：同账号成功/四图落盘且费用unknown、换号拒绝与新意图待确认、SIGKILL新PID原键恢复；无重试/无截图更新。测试没有修改生产服务或放宽旧断言。第一份positive命令因本轮中断残留的预览进程占端口而未执行，保留记录。

当时正在跑的完整E2E第三次主动中断，不能记完整通过；仅修此测试文件后重新冻结源码、统一检查和完整E2E。API/Worker及镜像的生产输入未发生变化。`nonwindows-worker-1.json`完整322P/0F/0S、进程退出0，包含上述新Worker镜像的实际容器测试。

最新冻结源码为1813文件，`fa3235d06000560b36b0322687ac67bb302879300ac38fb4a549da4587e6afbc`，`nonwindows-source-2.json`精确记录相对第一候选仅`electron.c3b-settle-main.spec.ts`一文件变化。最终`nonwindows-check-2.log`38/38、32缓存、退出0；第四次完整E2E在该构建和源码下执行，数据库开关开启、1 worker、0重试、0快照更新。间隔后免费登录核对仍为502/INTERNAL_ERROR（上游409被正确脱敏映射），未开始收费测试。

### 最终三端回归已完成

`nonwindows-e2e-4.json`进程退出0，545P/0F/11S/0flaky，58.56分钟，重试与快照更新均关闭：PC Web167P/3S、移动Web166P/4S、Electron212P/4S。11S中3项为视口不适用，8项为独立实网门禁（账号3、跨客户端同步1、真实Key/独立BYOK生图2、账号收费联合脚本双项目2）；跳过不算通过，收费脚本实际执行时只在PC项目发起一次并切换移动视口。

修正后的C3-B三项在本次完整回归中再次通过；macOS原生全屏、drag/no-drag、实际SIGKILL/新PID、磁盘写满、容量及恶意包拒绝、方案跨端交换均有本轮结果。darwin下的Win/Linux控件带用例不验证Windows三钮几何，不能外推Windows验收。

源码摘要确认未变后，旧App/DMG/ZIP保存至`pre-nonwindows-final-mac-7uFUBZ/`，新`package:mac:adhoc`退出0；当前新包smoke/三授权旧备份/产物扫描继续串行执行。三份备份原始SHA已预检与授权记录一致，仅在副本上测试。API自然小时原进程于UTC03:13:10开始等待原始到期，尚未到终态。

### 当前macOS包与产物验证已完成

打包/7项smoke/3份真实旧备份/安装包扫描四阶段均退出0，`nonwindows-mac-summary-1.json`记录收尾源码仍1813项fa3235d0…无漂移。smoke7P/0F/0S/0flaky，旧备份3P/0F/0S/0flaky。每份备份分别由4个不同PID验证，15条迁移、integrity ok、无FK错误；分别保留31/31/19条原FTS查询，图片152/176/178份逐字节与渲染解码核对。第三份原有1项历史迁移hash差异原样保留；原备份与原图片均未变化，不把重叠样本相加当独立作品。

App、只读挂载DMG、ZIP扫描0 findings/0 errors，包仍为2.5.0 macOS arm64 ad-hoc，不是Developer ID签名、公证或正式系统安装验收。DMG SHA256=`e2761c0488d1e39cebec88ca552d136f38991f77ad6c234908291ed35b92f075`，ZIP SHA256=`2807573faed8144d990611c853e29a9226108fa013d583dffebfbb8c9ef62954`。

本次完整E2E产出的68份方案文件全部分类：49份真实ZIP/合法容量边界包经标准扫描0/0；2份明确的UI下载原始占位字节按原内容扫描0命中；17份故意超限/损坏/恶意拒绝输入以本轮metadata及SHA逐一核对，不作正常导出包通过声明。第一版清单因两份服务端UUID文件名与历史不同而在扫描前拒绝；仅改为同一已知测试目录内唯一UUID下载的严格映射后通过，保留原失败日志，未改安全扫描器/规则或文件内容。见`nonwindows-artifact-inventory-1.json`及对应scan报告。

## 本轮终态与明确剩余项

完整API原进程最终退出0：`nonwindows-final-api-1.json`96文件、1138P/0F/0S，107.8分钟。自然小时从UTC03:13:10.571至04:13:14.940，共3604.369秒，实际worker正常退出；8个到期对象删除、12个已采纳对象保留，原回执重放不新写、不延长原期限。其存储上游为受控HTTP服务，真实MinIO覆盖归上述实际镜像部署测试，不能混淆证据层级。

原B26/B30的非真实收费开发/验证范围本轮关闭：R4-1..4已有§5.107逐条件台账，本轮再补正式结算窗口新意图及正例排除旧夹具假阳性。当前完整三端内C3-B3P、重试故障7P、重试身份恢复10P、恢复确认16P、云重试2P、Web兑换恢复4P；旧托管生成拒绝1P、未结启用阻断3P、BYOK兼容1P、CLI/MCP G/R/S1P、本地连接策略5P均通过；最终check中的规则/真实SQLite交错/serve升级/接管另16P。它们原boundary明确不调用真实付费上游；P6真实账单、Windows延期平台、外部破坏双锁的独立纪元卡不再被误挂为这两个子任务的本轮出口条件。只关闭B26/B30，不能连带宣称整个P4/P6或迁移完成。

任务89/94、验收仍部分通过，剩余5个父任务包括方案完整parity、共享页面parity、Electron关键路径/平台、整包统一收口及最终知识/验收清单。它们不是“五块都没有代码”：本轮本地受控实现与门禁已验，真实账号正向链路仍被官方`AUTH_SESSION_LIMIT`阻挡，需用户释放旧登录会话后继续正常登录、真实双客户端同步和账号文本/图片方案链路；收费授权保留，新增收费调用0。Windows专属项按用户要求延期；正式签名/公证/系统安装、远端CI/TLS/MCP与生产发布另列，未假定额外权限。

未修改版本号，未提交或发布，8份无关logo草稿未纳入；管理员未启动。本轮临时LIVE25 API由其已核对PID的父包装进程正常停止，未停止正式App或原有PostgreSQL/MinIO容器。完整API、E2E、包和扫描进程均已结束，不留下仍运行的“待收结果”作为完成结论。

最终机器汇总为`tests/v25/.results/closeout-20260919/nonwindows-final-summary.json`，逐源文件终态为`nonwindows-source-final.json`：1813项fa3235d0…与冻结候选完全一致；最终源码扫描0/0、`git diff --check`通过、staged0。Spec校验实际退出1，准确报告89/94、尚有1个MVP验收检查未勾选及整体部分通过，不将此预期的未完成状态记为门禁通过。
