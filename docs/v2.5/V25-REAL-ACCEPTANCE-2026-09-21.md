# v2.5 真实跨端与收费验收（2026-09-21，正文补登至当日终态）

本记录承接非 Windows 收口。Windows 继续延期，正式签名/公证和完整产品发布另列；未切换 Musefold v1.1 公共站点，不充值、不扩展独立 BYOK 消费，不操作正式 App 数据。最终回归、产物验证与 Git 提交仍未完成。

## 隔离与证据

- 本次实际 API、生成 Worker、Agent Worker 使用独立 PostgreSQL 库 `musefold_live25_20260921_e6358a49` 和独立对象桶。账号认证与收费才访问已升级的真实 New API。
- 凭据仅经关闭回显的 stdin 进入进程；Playwright trace/video/自动截图关闭，登录提交后清空密码框。各次实网报告的密码精确扫描均为 0 命中。
- 本次报告统一在 `tests/v25/.results/live25-20260921/`。失败报告保留，独立重跑不覆盖历史结果。

## 真实跨端同步：通过

`sync-6/report.json`：1P/0F/0S、无重试、退出0，46.7秒用例。PC Web 创建→macOS 明确选择空库并同意同步→macOS 修改→移动 Web 修改→新 Electron PID 恢复→双方退出重登→Web 删除→macOS 界面和数据同步删除。身份、deviceId、consent 和内容保留；SQLite integrity=ok、外键检查空、outbox 空。旧/新 PID 为53003/53085；无收费。

此前五轮首败完整保留：清理缺 JSON 请求体、未等待删除提交、直接 IPC 绕过 UI 查询刷新、启动同步轮询等待、登录限流。修正测试的实际操作和精确清理，不用放宽业务断言换取通过。前轮遗留的唯一测试登录已精确删除其本地 Better Auth session，经正式释放队列变为 released；未触碰其他设备。另两条前轮测试提示词已通过正常登录、逐ID核对原标题及expectedVersion后软删除，仍可从回收站恢复；`cleanup-prompts-1789952089101.json`记录两条version 3→4及退出200。本轮隔离数据库最终无本地登录，26条释放责任全部released；只停止本轮自建API/Worker/诊断Agent进程，未停止正式App或共享基础设施。

`account-final-1/report.json`三端账号回归3P/0F/0S、无重试、退出0：PC/移动/真实Electron登录、提示词创建删除与Web方案入口，Web同时读取真实模型/云价。未发送付费请求，每个上下文均正常登出，凭据报告扫描0命中。

## 限流错误映射：已修复并专项验证

实网首次登录502的上游实际状态是429。只读核对部署源码与 Redis TTL：关键请求默认同IP20次/20分钟；未重置计数、改限额或更换/伪造来源绕过限制。

账号域原来将429丢成INTERNAL_ERROR，认证层再返回502。现在保留RATE_LIMITED/429，并使用固定脱敏提示；普通网络故障仍与限流区分。完成请求限流后保留原operation/selection，禁止替换操作，允许原请求重放。

实际 Better Auth+PostgreSQL 与就地测试 `rate-limit-final.json` 24P；先前红测13P/2F、首次仅改认证层仍失败均保留。客户端/辅助测试 `free-unit.json` 98P/2条件S（上游真实镜像入口另行验证，跳过不计通过）。类型检查在修正新增测试的unknown访问后退出0。

## 收费流程：全链节点逐段通过，最终整轮复验待完成

账号云目录实际给出 `musefold-image-pro` 基础$0.04、账号分组倍率3、60,000点/计费次；不能把基础价当该账号实扣价。文本模型为 `musefold-agent`。

- `paid-1` 未提交生成：模型Select焦点恢复与设置Popover操作相撞，测试等待已选中的张数按钮。为诊断读到当前等待位置后主动结束该次，不冒称自然超时。后续等待Select恢复焦点并设置动作30秒上限；没有强制点击或绕过界面。
- `paid-2` 普通生图成功：run `c5e8bfd9-5737-475a-8c15-89d86702e123`，实际1254×1254图片1张、1,611,248字节。图像响应没有权威费用字段，产品保留costPoints=null，而不是编造结算值。后续Agent创建调用已收费但返回非法结构，未授予草稿/试跑资格。
- `paid-3-diagnostic` 复用上述成功图片，只新增一次独立Agent诊断调用，没有自动重发原execution。实际返回中7条constraints.domain分别为中文“构图/色彩/主体/排版/质感/输出/安全”，全部违反固定英文枚举。原提示词该字段仅写省略号，未告诉模型合法值。诊断仅在私有隔离Worker包装器记录返回并原样传递，不修改供应商输出，不部署诊断钩子。
- 创建与修改的共享提示词已直接引用contracts枚举，并补文本/图片字段互斥、每个模板的变量列表规则；没有放宽模型输出或文档校验。正式红测 `prompt-guidance-before-2.json` 7P/2F，修复后领域11文件134P；首个红测提取示例JSON错误也保留。受影响API认证/文本专项35P，类型检查退出0。
- `paid-4-fixed`：真实Agent创建成功，execution `838a20a6-ee70-4b27-8f1c-3ea1af2b1abc`、方案 `e38ab7e3-4a23-41f4-95dc-49eec1bbbd2a`，1次发送/1次完成，严格文档校验通过。测试在导航完成前读取URL而失败，不能记整轮通过；随后增加详情可见及URL与真实结果ID一致的等待，不改产品导航。
- `paid-5-resume-draft`：通过真实Agent历史入口恢复上述草稿，不重复收费创建。真实试跑 `dde26934-f4d7-4525-a69b-e6fbc9ba0cc1` 完成，实际1024×1536 PNG、1,365,929字节，内容SHA与资产记录一致；封面选择及初版转正成功。之后换模型的正式运行未提交，提交按钮禁用而超时，整轮仍失败。测试此前可能在字段挂载前跳过必填输入，现改为选模型后等待必填字段可见、填写并断言；不以该推断宣称产品无缺陷，下一轮从已转正版本继续确认。
- `paid-6-resume-formal`：真实第二模型 `gpt-image-2` 正式运行成功（generation `73b12abb-78ad-46dd-b40d-e45ece56b20e`）；移动端修改成功（execution `49588ef1-5d44-4364-a3e2-4c222c4cc90f`），新版本 `55b09687-9bd0-4273-883d-bf38b622cec1` 的真实试跑也成功（generation `75696066-7d55-4df1-9517-95c11ae29cfc` / scheme run `de2ae6a5-e684-4a1e-b434-594dd73f1622`）。随后读取新版详情429，整轮失败，未完成新版转正/导出；真实账本中4次图片生成均succeeded且冻结模型一致，没有未知结果重发。
- 429诊断：本地限流桶均未超限，上游无覆盖配置的API默认限额为360次/180秒/IP。只读日志在该次测试窗口记录`GET /api/user/self`共360次，其中334成功、26次429；每次业务读取都进行新鲜上游身份校验，运行状态/事件原每秒各读一次，测试还额外轮询Agent。将方案运行轮询调整为与工作台一致的3秒，测试Agent额外观察改为5秒；未缓存认证、放宽服务端限额或重置窗口。轮询节拍红测9P/1F→10P，保留单次收费POST和事件游标不重放断言。
- `paid-7-resume-modified-tried`：新版真实PNG为1,466,151字节、SHA `16e0bba357f52b0ad28ebaff9f68629f5c1aea6b471cb0eb781f80dc74a974e1`；核对父版本/成功试跑后，实际移动界面完成封面与新版转正，无新增收费。服务端导出归档已准备，但测试等待browser download超时：当前Chromium支持原生保存器，而该helper只观察download事件；原生选择器权限/取消/写入由既有host测试负责。未将服务端准备成功冒称交付文件。
- `paid-8-export`：明确选择移动端兼容的browser download路径（仅隐藏测试浏览器的showSaveFilePicker能力，无响应/文件伪造），复用已转正新版、无收费重发，1P/0F/0S、无重试、退出0。实际下载1,479,766字节的格式2包，shared codec严格校验通过，SHA `6d737e01ce88f77e74691f6994629a35f93653f423299e90aa7271f75700c886`；人工查看移动端截图，实际正式方案及“已交给浏览器下载”反馈一致。余额仍90,750,504。

真实链路结论：普通图→Agent创建→初版试跑/封面/转正→第二模型正式运行→移动修改→新版试跑/封面/转正→正式包导出各节点均有实际持久记录。它是上述保留任务的跨轮接续证据，不是将第2至7轮的失败报告改成“一次全绿”；没有SQL授予试跑资格、伪造模型输出或重发未知收费任务。

只读服务器最终账单 `charges-1789952202486.json`：普通图片60,000点，第一次失败文本94,320点，独立诊断文本90,600点，修复后成功文本89,633点，初版试跑60,000点，换模型正式运行60,000点，移动修改文本101,625点，新版试跑60,000点；合计616,178点，精确等于余额91,366,682→90,750,504的差额。八条日志同测试托管token_id=25，模型/时间/用量与本地记录对应；失败文本费用完整保留，没有把token用量推算当实扣。第7/8轮及三端账号复验没有新增费用，最终请求数158；早期各账单仍保留其原时点。

## 最终门禁边界

修复提示词前曾启动完整API回归，因发现真实产品缺陷而主动SIGINT停止（退出130），不记整轮通过。提示词修复时源码1851文件摘要 `3b341e3d89c80d5c2f9d6946c4db5dd68a18fee746d6f4c25709b8b0ce17a87f`；后续完整check/API/E2E与macOS包须绑定最终源码。以前552P及旧包结果仅为旧输入基线，不能替代本次最终交付。

完整API第2轮终态失败：97文件通过、1文件失败、1文件条件跳过，1143P/17S。`account-models.integration`在创建迁移schema时测试PG连接意外中断，15个用例未执行；不能把这15个与原2个条件入口一起算通过。原报告`api-full-2.json`保留，第3轮使用2个worker完整重跑，于上海09:14退出0：98文件通过、1文件条件跳过，1158P/2条件S，1467.49秒，报告`api-full-3.json`。两条件分别是真实New API镜像生命周期（本轮已独立1P）和自然一小时（仍运行）；不重复计算其中的预检用例。轮询修复后类型检查和Web构建也已通过。

最终候选源码快照1851文件 `ee593e6d01b220e0e8e4106d611075145b55d92fe3f1fcdfa112f2a0a0b93b1b`：完整check已退出0，38/38任务，root3234P/77条件S、features860P、api-client106P、Web host48P。条件跳过仍须按独立报告补齐。上海09:00首次全量缺少真实数据库开关，发现异常跳过后主动终止，退出143；日志和当轮产物保留在`e2e-without-db-gate/`，没有将它计作通过，也没有进入打包。09:07启用`RUN_DATABASE_TESTS=true`后重新完整运行565项三端测试，禁重试/禁更新快照；流水线要求552P/13明确分类S/0F/0flaky才继续当前包、三旧备份、安全与逐字节验证，每个阶段强制源码清单不漂移。运行中不宣称通过。

09:21补验：根条件两组`root-conditions-27.json`（27P）与`root-conditions-43.json`（43P）均退出0、0跳过；43项含13项默认也会执行的部署计划测试，不能把70项相加误称覆盖70个默认跳过项。实际覆盖57个默认条件入口，其余18个镜像入口由下述部署测试覆盖，两个上游协议入口另记。

当前源码的API/Worker/Web镜像重新构建后，真实Worker容器2P、隔离生产镜像部署/受限数据库角色/双视口同源页面/恢复回切18P均退出0、0跳过。记录`runtime-image-summary.json`绑定最终源码与三个不可变镜像ID；这是本机隔离验收，不是远程切换Musefold公共站点。Worker完整源码测试`worker-full-final.json`已退出0，320P/2容器条件S；两个条件用例与当前镜像2P按文件名和完整用例名对应，合计覆盖322个不同用例，不用容器冒烟替代完整测试。

09:35条件覆盖核对：根测试独立复跑`root-default-final.json`为3234P/77条件S、0F；真实Go HTTP协议1P与生产同镜像默认50会话容量1P分别见`new-api-wire-final.json`和`new-api-image50-final-2.json`。前者使用实际Go控制器，后者使用隔离PG与合成账号，均未操作真实用户。默认50测试首次辅助脚本误以为PG显式设置POSTGRES_USER，在创建库前失败；修正为实际默认postgres后通过，首败保留，未因此改产品代码。

`conditional-coverage-final.json`对上述根77个跳过和Worker两个跳过按测试文件+完整名称逐项匹配实际passed记录，缺失0；根合计3311个不同用例、Worker322个不同用例覆盖。核对前重算1851文件摘要，仍为ee593e6d…；各原报告及跳过原貌保留，不把额外重复执行的13项部署计划测试叠加计数。API自然小时、三端E2E、最新安装包与付费整轮仍须各自终态，不由此判定完成。

09:40补验：`lifetime-preflight-final.json`独立预检1P/1筛选S、退出0，实际在途锁、Worker重启、解锁和归档完整性通过；筛选跳过的自然小时仍在原进程继续。完整API第3轮也已执行并通过同一预检，不重复累计。最初长时进程的预检未正常完成、初始化目录为空；其最终错误需待该进程终态取得，不提前将启动压力推断当作已确定原因。

验收登记守卫另发现历史初始化假设（禁止任何pass、要求登记文档绑定自身HEAD）妨碍正式可追溯登记。忽略目录内的修正候选已用9个实际临时Git仓库用例通过验证；候选尚未接入正式源码，1851文件冻结输入不变。待当前E2E与打包退出后正式接入并运行相应回归；实际业务源码/后端/宿主输入不应因文档登记而重复改动。该问题尚未关闭，任务清单仍91/96。

真实收费方案下载包补做完整归档安全扫描：`paid-export-scan.json`，1份实际格式2归档、0命中/0错误，包括私有路径检测；不以服务器ready或UI完成文案替代下载字节验收。

09:46补存独立报告：共享features为67文件860P/0S，Web宿主正确使用其package test脚本后11文件48P/0S（`web-host-final-2.json`）。首次直接调用Vitest漏掉该脚本的Node 25 `--no-experimental-webstorage`配置，44P/4F；原报告保留，不将测试入口错误改写为产品修复。

09:57旧版反向审计发现真实偏好迁移缺口：v2.1的外观、引导和工作台偏好存于localStorage，新版仅读`v25-preferences.json`。现有偏好功能测试不证明旧值继承，已回写原共享产品parity父任务，准备隔离复现和修复。当前冻结E2E/安装包流水线仍继续，但其结果只能证明修复前输入；后续迁移改动必须重新运行对应宿主、check和产物验收。

09:58自然小时终态：原进程退出1，`natural-hour-final.json`如实为1P/1F；失败是最先执行的preflight在120秒内未完成，未写出初始化进度，不能再细化为已证实的PG或Worker根因。同一报告的自然到期用例完整通过，`package-lifetime-natural-RYGCtj/result.json`为passed，开始00:57:41Z、结束01:58:01Z，原期限不改动，真实在途请求跨期限、Worker新PID、清理与保留资产断言均通过。preflight另有完整API第3轮及独立`lifetime-preflight-final.json`两份passed记录；按精确用例匹配证明两项均有成功执行，不将该混合报告改为整轮全绿。

API条件证据已按文件名和完整测试名匹配到`api-conditional-coverage-final.json`：完整1158P加真实镜像生命周期及自然小时各1P，覆盖1160个不同用例；原预检失败报告原样保留。偏好迁移候选21项隔离单测通过，但正式主进程未接入、真实Electron红绿测试尚待，不能计为已修复。

10:11反向OAuth入口审计：当前生产Web的`GET /login`与`GET /consent`均404，API MCP配置仍重定向到这两个页面。原授权码/PKCE/撤销测试证明服务端行为，不证明用户浏览器首次授权；缺口已回写原共享产品parity父任务，需接回登录和同意界面及安全返回路径并完成真实浏览器闭环。当前只做只读路由探测，未新增授权、令牌或收费。

10:10完整三端终态：`e2e-final.json`为551P/1F/13S/0flaky，3776.36秒；唯一失败是`electron.package-capacity`的`manifest-over`测试在`PackageExchangeProcess.ready`等候120秒后报fixture timeout、输出为空，尚未进入产品包上传断言。根因未定位，不把“启动失败”推断为已证实的环境问题。协调进程退出1并停止，**没有进入新打包**；原报告与超限真实归档保留，后续完整重跑仍必需。

真实上游镜像→生产API→共享PC/移动界面补验`image-ui-final.json`：2P/0F/0S/0flaky，15.6秒退出0。验证满额自动弹窗、用户选择释放后继续登录、主动退出释放名额；仅隔离合成账号，无收费。偏好缺口已在真实Electron复现：`preferences-red.json`两个旧origin均启动后恢复为system/comfortable，而不是旧dark/compact；保留红测，尚未修改正式源码。

## 10:11之后增量：旧偏好修复及整轮收费首败

上述1851文件冻结输入已结束，不能继续作为以下新代码的最终身份。旧偏好迁移正式接入主窗口之前：白名单读取file/app旧origin、字段逐项校验、原子且不覆盖地写新偏好文件；已有v25文件不改写，读取失败留到后续启动重试，旧存储不删除。真实Electron红测2F→`preferences-green.json`2P（20.3秒）；引导/设置/窗口与迁移联合`preferences-host-regression.json`20P/0F/0S/0flaky（53.2秒）。偏好/启动/验收守卫就地`preferences-guard-unit-2.json`49P；build、typecheck和依赖边界通过，但新代码尚未全量check/E2E/打包。

验收源码登记守卫已正式接入，检查真实完整commit、HEAD祖先关系、工作树及Git index中的执行输入，不再要求文档提交自我引用。补出的“暂存修改但工作树恢复旧内容”负例先失败，再修复；`evidence-guard-final.json`22P。文档后续提交允许，实际源码漂移仍拒绝；尚未登记虚构的passed commit，未提前提交。

`paid-9-full/report.json`从零真实复验为0P/1F、7.7分钟、退出1，原报告保留。普通图`d40fb6cf-ed5d-4221-85e7-cd1da4e469d9`、Agent方案`2fc923ab-7dbc-4af1-a80e-326d30d6575b`、初版试跑/封面/转正、第二模型正式运行均已在服务器完成；第二模型generation `d622308d-ce6c-4c57-af92-6e0fc2cad75c`与scheme run `f283e89f-0125-4fbe-aef1-14750f335159`实际成功，但界面等待期间返回“账号操作过于频繁”，正文没有清空。本轮未进入移动修改/导出，不能记整轮成功，也不能重发已经成功的请求。

只读账单`charges-1789957226440.json`比`charges-1789956692895.json`新增4条：1141普通图60,000、1142创建文本92,378、1143试跑60,000、1144第二模型60,000，合计272,378原始quota单位；余额90,750,504→90,478,126完全相等，请求数158→162。连同此前616,178，本日这组验收累计888,556原始quota单位（显示积分需÷50,000），未充值。失败不能抹去已发生费用。

10:30只读上游日志：paid-9窗口`02:12:23Z–02:20:30Z`含387次`/api/user/self`，其中28次429；没有输出凭据/来源IP，也未改限流计数。源码另发现每条方案进度事件立即失效四组查询，与状态/事件/侧栏/时间线轮询叠加。只改方案为3秒仍不足，当前补有界只读429退避及事件刷新合并；写请求不自动重放，401/403/404/503和未知网络结果不隐藏。请求层红测9P/5F、事件红测8P/1F；首轮全client119P/1F暴露市场搜索原显式重试合同，保留市场限流立即反馈后`read-backoff-client-2.json`120P，工作台/运行处理87P。真实浏览器故障验证与新的收费整轮仍在推进，本段不宣布问题已全面关闭。

## 限流修复回归与继续反向核对

`source-read-backoff-1.json`记录1863个执行输入，摘要`78689811c5216db4683202587e7f36678230defcb43e9480f2f6072921500ed1`。该版本完整`pnpm run check`退出0，38/38任务成功；根3286P/77条件S、features861P、api-client123P、Web48P。默认API/Worker跳过数据库条件集，不用这次check冒称完整后端验收；旧偏好、验收源码守卫及GET退避正式代码均在该次检查内。

PC/移动正常429恢复首轮`read-backoff-browser-1.json`4P/0F/0S，使用实际隔离API/PG/Agent/生成Worker和受控图像上游，无真实费用。后续追加撤权负例的`read-backoff-browser-2.json`与`-3.json`均为12P/2F，原报告保留：前者错误等待普通提交错误而实际进入账号恢复；后者新增真实403核验，却以网页原origin匹配已转发到隔离API的响应。修正只涉及测试：仍要求10秒冷却期间撤权、恢复原路径/游标的GET得到实际403、原输入保留、提交禁用且整个过程只有一次生图调用。没有把权限错误改成成功或放宽付费次数断言。

继续核对旧Worker时确认另一个启动行为缺口：基线`0e5be61ccaeaa497ec01fa121f7767456ebd1a7f`的`ensureStorageBucket`仅对404尝试创建、尊重`S3_AUTO_CREATE_BUCKET=false`并在存储不可用时拒绝启动；当前`apps/worker/src/bin.ts`捕获任意HEAD失败后尝试创建，创建失败也继续启动消费队列，env已丢失该开关。需补回明确布尔开关、仅缺桶时创建和启动失败保护，并覆盖真实入口不消费任务的负例；当前仅已完成源码确认，尚未修复，不以既有Worker全绿掩盖该缺口。

修正转发地址后的`read-backoff-browser-4.json`正常退出0：14P/0F/0S/0flaky，覆盖双视口事件/状态429、等待中实际撤权403，以及原有文本/参考图事件与状态读取故障；保持同一任务与单次生图提交。

`paid-10-full/report.json`为0P/1F（5.3分钟），输入1863项`abc1bef3b600a54f0a4a2c4a68db64dec54c4ff7bfaf19ad69b1fe590289a4fb`。全新普通图`0e9607b0-39d5-421d-a5bf-48468d647e21`、Agent创建`c3af2cc4-1f1c-42ed-bcb9-9084d617cc01`、方案`49cc1cec-4078-48ce-b8cc-d02a6ec3670d`初版试跑/封面/转正、第二模型、移动修改`d157c492-b012-456e-9961-757a1c5556e9`与新版`f55b4df9-119c-4def-b375-eda3d2facb7a`试跑全部实际成功。原始附件记录325次API读取，HTTP失败0；没有重发收费任务。最终失败在新版选封面：详情深链导航后，相册helper仅等5秒，而主区域快照仍为空的加载态。新增等待实际`runtime-scheme-detail`最多30秒后才核对精确资产，并从原成功版本继续免费验收；未据此提前断言完整产品无故障。

实扣核对：`charges-1789959831790.json`对比`charges-1789959366398.json`，新增1145–1150共6条，四次图片各60,000，创建95,220、修改125,880，总461,100原始quota；余额90,478,126→90,017,026、请求162→168完全一致。本日此组验收累计1,349,656原始quota（26.99312显示积分）。失败和费用均保留；后续只恢复已有图/任务，不重复收费补造成功。

云API反向核对另确认代理信任遗漏：旧版配置禁止无条件信任代理，新版`clientIp()`直接取任意`x-forwarded-for`首项。通过当前真实`createApp`请求入口和只记录key的限流替身，两个未验证来源的保留测试IP分别成为两个限流key；未访问生产、DB或收费上游。首次诊断依赖替身缺少recovery方法而未启动，补全后才得到该结果。需正式回归并接显式代理信任链，不应以部署可能过滤请求头代替应用边界证明；当前未修复。

## 方案版本刷新、存储启动和可信代理修复

上述“当前未修复”为发现时状态；本节记录后续正式增量，不改写原失败。

`paid-11-resume-modified-tried/report.json`0P/1F、退出1：复用paid-10成功结果，实际选新封面并完成新版转正，紧接免费检查更新却因旧expectedVersion被拒绝。确认产品根因是mutation完成不等待query invalidation，菜单仍可冻结旧版本，不是继续加测试等待就能关闭。新增组件红测第一次未等列表就点击导致准备失败；补正确准备后的`promotion-refresh-red-2.json`确实失败在菜单仍enabled。正式修复等待失效刷新、关联动作禁用/状态提示，并使current-selector读取错误不被缓存working数据隐藏；只读重试不重放写入。

`promotion-refresh-green-2.json`3文件57P/0F/0S；包括转正延迟、封面/初次转正延迟和current刷新失败后只读恢复。共享features全量`promotion-features-full-1.json`865P/0F/0S，根类型检查和Web构建当时通过。`promotion-browser-1.json`PC/移动2P/0F/0S/0flaky、94.37秒：实际隔离API/PG/两个Worker与受控模型上游，分别完成创建、试跑、正式化、修改和更新两次新版试跑/转正、真实导出重导入；转正后的GET被传输层阻塞时相关动作禁用，释放后读取新版本，每次只有一次转正且没有新增图像/文本调用。已人工查看两种视口等待状态截图；无截图基线替换、无实网费用。

`paid-12-export/report.json`1P/0F/0S、无重试、退出0，26.2秒：在新Web构建中复用paid-10普通图/Agent及paid-11已转正版本，从export阶段恢复，完成实际免费检查和移动端下载。归档2,159,628字节、格式2、SHA `0c3dd98134ce78b747c5dfae3243966a55745ac77c95e856c331e00fb2788e2e`；严格codec与图片hash核对通过，人工截图显示正式方案和“已交给浏览器下载”。密码精确扫描4文件0命中。`paid-12-export-scan-2.json`明确zip递归及detectUserPaths=true，0命中/0错误；首份扫描误用了未识别privatePaths配置，不据其宣称私人路径已验。账单`charges-1789960958363.json`与`charges-1789961047924.json`余额90,017,026、请求168及日志集合一致，paid-11/12没有新增收费。该次是接续通过，paid-10/11失败不变；仍需修复齐备后从零完整收费复验。输入快照`source-paid-12-input.json`1866项`c8d2d1b8…`；真实API/Worker仍是已有隔离运行进程，此次验证不冒称加载了后续存储启动或代理修复。

Worker正式入口红测`worker-bootstrap-red-1.json`1P/10F/11筛选S，4种实际HTTP缺桶/拒绝/服务错误/创建失败都错误进入started；配置6个断言也失败。恢复严格true/false开关（默认true）、仅确认缺桶才建、区域配置和启动失败关闭连接后exit1。`worker-bootstrap-green-1.json`40P/0F/0S，含实际bin/PG/Graphile/HTTP负例：原任务保持queued、队列attempts=0、无模型调用/对象写入、子进程连接为0；允许创建时真实生成和落盘仍通过。`worker-full-bootstrap-1.json`完整源码349P/0F/2镜像条件S、退出0；两个镜像入口需新镜像复验，不能沿用旧镜像当本次完成。

API代理正式红测`proxy-trust-red-1.json`5P/23F：真实Node HTTP入口证明任意未信任XFF可选择限流subject及缺少安全配置。新`TRUST_PROXY`默认false，显式IP/CIDR与旧命名网段、由实际连接向内逐跳、首个非可信节点停止，异常/过长链回退连接来源；归一IPv6及映射IPv4，禁止全信任/跳数/主机名/空规则。`proxy-trust-green-2.json`47P/0F/0S：配置/地址单测、实际HTTP和3个真实PG限流用例；改动伪造前缀仍命中同一HMAC桶，触及原生产上限后返回429。只在隔离测试库设置边界计数，不操作生产限流。新增HTTP夹具曾因ServerType包含HTTP2而出现TS清理方法错误，已改为能力检查；后续统一check结果另记。

本机Compose已透传两个恢复配置，部署手册说明可信拓扑和生产预建桶。没有部署上述Musefold产品改动或切换公共站点；OAuth首次授权、其余反向核对、全新完整实网、最终全量与macOS/备份/提交仍开放。

## OAuth 首次浏览器授权与启动诊断（12:22 接续）

方案版本刷新、Worker启动与可信代理三处修复后的`check-three-fixes-3.log`已38/38、退出0，根3286P/77条件S、features865P；该输入1870项`56671e47…`早于OAuth增量，不作最终版本全量证据。独立`minio-bootstrap.log`已验证真实隔离MinIO在auto/us-east-1区域的创建、既有桶HEAD、PUT/GET，以及关闭自动创建时缺桶保持不存在；临时容器已关闭，不替代新Worker镜像验证。

OAuth正式实现：共享`OAuthAuthorizationScreen`及Web薄宿主`/login`、`/consent`已接通，复用原账密/2FA/登录满额表单。API扩展使用已安装的Better Auth OAuth Provider签名验证与客户端解析，不自造签名；先核对客户端/只读权限/当前账号，确认引用绑定精确请求、客户端元数据、账号和会话、5分钟到期。继续地址由服务端从有效请求构造；拒绝任意returnTo，回调仍由原provider验证。受限身份、过期/修改/禁用请求不得同意，不确定提交不自动重放。Cloud MCP只读范围未扩张。

实际PG/Better Auth专项`oauth-browser-green-6.json`27P/0F/0S（新增浏览器边界15项、原授权谱系12项）。范围含签名篡改、缺签名/重复参数/真实provider过期核验、Origin、受限恢复会话、客户端停用/元数据变化、确认引用串用/换会话/过期、同意/拒绝、PKCE错误、授权码重放及撤销。`oauth-browser-green-5.json`26P/1F原样保留：过期测试仅替换Date.now，而provider源码用new Date比较，导致测试没有实际推进其时间；改为仅虚拟Date、网络/PG计时器保持真实后通过，不放宽产品期限。

`oauth-browser-e2e-6.json`PC/移动2P/0F/0S/0flaky、14.3秒：真实Next生产构建、同源HTTP前门、实际Hono/Better Auth/PG、两个独立HTTP客户端，经共享UI创建登录与consent，实际state/PKCE回调换码；只读tools/list仅返回get_prompt/search_prompts。拒绝B不创建B授权；强制重登后再接A，确保两者都能调用，然后设置页明确撤销B，B返回401而A仍200。375×667、667×375按钮/取消可滚动到达、无横向溢出；暗色/减少动效、Tab焦点和Enter同意已验证，PC/移动授权及撤销截图已人工核对。上游New API和S3是隔离受控夹具，无真实收费；不是远端生产TLS或两个远程第三方客户端验收。

保留浏览器首败：第1轮原route mock未承接原生跨客户端302，改为真实同源前门；第2轮fixture资源初始化竞争，改为等待auth.$context完成，不手工重复插入资源；第3轮登录/换码已成功，但探针误用产品明确拒绝的旧initialize协议，修正为现用2026-07-28逐请求tools/list。第4轮2P，第5轮补双有效授权隔离和小屏2P，第6轮再补实际工具名单/暗色/键盘及等待撤销弹窗关闭截图2P。原失败未覆盖、未删除测试或放宽产品协议。组件15P、API client3P、契约2P、Web宿主58P及Web构建分别见同目录oauth-*报告。

原完整E2E的manifest-over用例120秒启动超时仍无可证明的根因；没有延长原时限。为实际夹具加固定阶段名/耗时IPC诊断，覆盖2项就地测试，`package-capacity-recheck-1.json`同一原用例连续3P/0F/0S/0flaky，三个独立子进程启动分别3299/3088/3225ms，实际PC/移动/Electron均执行。未复现不等于原整轮全绿，需随当前全量再次收取。当前OAuth及诊断输入1884项`88104ad1…`已启动完整check；新镜像、最终完整实网/全量/包/三旧备份/扫描/提交未完成，原5个父任务保持开放。

## 12:43 接续：当前检查通过、实网故障留证与重新验收

`check-oauth-1.log`完整检查已退出0、38/38任务成功，无缓存；根3293P/77条件S、features871P、API默认418P/801数据库或专用条件S、Web宿主58P。`oauth-source-scan-1.json`零命中/错误；`source-oauth-recheck-2.json`复核1884项摘要仍为`88104ad1e5c0f4f457829f331ce30eb0afe718c2dd51b323d9a3f8e28b866922`。默认条件跳过不计通过，完整API仍在原运行进程中；当前三形态清单为579项，尚未取代旧551P/1F/13S报告。

真实API/生成Worker/Agent已在确认旧任务无在途后正常退出并由当前源码新PID启动。`paid-13-full`登录成功，但模型目录503，未提交收费任务；随后免费API预检登录502（约10秒）。公开状态200、服务器容器仍运行，只能排除整体进程退出，不能证明具体请求健康；未改超时/限流或生产配置。后续免费预检实际登录、touch及连续三次模型目录均200（15模型，耗时10603/2881/1916ms），退出200。原502/503根因尚无充分证据，不能将恢复可用表述为已修复。

`paid-14-full`在登录界面20秒等待结束时仍为Loading，0P/1F，尚未进入普通生图；密码扫描3文件0命中。`charges-1789965367063.json`与`charges-1789965539364.json`账号余额90,017,026、usedQuota9,982,974、requestCount168及完整18条日志集合一致，此轮无新增费用。第一次只读SSH账单连接失败，第二次成功，失败输出保留。免费浏览器计时诊断第一轮漏设首启哨兵、未触发登录，不计服务端失败；补与正式E2E相同的首启状态后，`login-timing-1789965598664/report.json`实测登录200用时3411ms、已登录界面5204ms、touch/status均200、退出200。没有凭此放宽正式测试时限或改写paid-14失败。

恢复后开始`paid-15-full`全新整轮（非旧结果接续）；普通图`f2a88ecf-6afa-4494-9cb5-0149c848a967`实际succeeded，后续方案流程仍在执行。当前源码三镜像构建与最终回归协调脚本另存新文件，不覆盖旧报告；最新包、三旧备份、所有条件补验及最终提交仍待。没有部署Musefold公共站点、充值或操作其他用户会话。

## 13:09 接续：收费原结果保留、登录故障定位与当前全量回归

当前生产代码的三份不可变镜像已完成构建；`oauth-worker-image-final.json`2P、`oauth-deployment-images-final.json`18P，均0F/0S。镜像ID、输入1884项`88104ad1…`及真实构建/运行退出码见`oauth-runtime-image-summary.json`。这是本机隔离镜像、受限数据库角色、恢复回切验收，不是Musefold公共站点发布。

`paid-15-full`最终0P/1F：普通图与Agent创建方案实际成功，方案`0f2cefcb-9c3c-40f9-aad8-2471c85408e7`，Agent execution `64f39872-f64d-4118-982f-ec703c685985`；试跑前模型目录503，界面显示明确读取失败和刷新按钮，不允许从禁用模型选择器继续。该轮新增图片60,000和文本91,118原始quota，合计151,118，余额90,017,026→89,865,908、请求168→170与账单一致。原失败保留，不能记整轮通过。

`paid-16-resume-created`复用上述普通图和Agent，实际初版试跑run `f1116fbe-a9ab-46cf-a40d-e5867e153984`完成，generation `3c544cce-602e-4f6b-8048-c59824fe6814`成功；设置封面POST 200。随后测试默认5秒内未等到权威详情刷新，原报告0P/1F，尚未提交转正。新增图片60,000，余额89,865,908→89,805,908、请求170→171与日志1156一致。测试封面辅助函数改为有界30秒等待，仍核对精确资产且不重发设置请求；就地红1F→绿1P留证。PC/移动实际API/PG/Worker的`cover-delay-browser-2.json`2P/0F/0S，故意延迟真实详情请求6.5秒，核对单次封面写入、200、最终持久cover ID、模型和历史成本未变。第1轮因测试新增route.fetch绕过原上下文后端转发而401，已改为延迟后fallback；不把测试路由修正说成产品修复。

新增`initial-tried`实网接续入口，只能复用已持久成功的草稿试跑；仍读取校验资格、原图字节/hash及封面，不伪造成功或补发已完成生成。`paid-17-resume-initial-tried`20秒登录等待失败，未进入收费；免费`login-timing-1789966936807/report.json`随后实测登录200耗时8363ms，完成touch/status后账号可见耗时15214ms，退出200。登录包含串行托管登录、确认与账号读取，实网辅助函数总等待改为60秒并记录路径/状态/耗时，不改产品逐请求期限、不自动重复登录、不记录请求体/凭据。辅助函数与清理联合`live-helpers-final-1.json`7P，类型检查通过。

`paid-18-resume-initial-tried`仍0P/1F，其新增计时证据确认登录200在4684ms、touch 200在11112ms、随后账号status 503在21136ms。界面明确显示账号服务暂不可用；不能再把该失败仅归为20秒断言过短，也未证明上游网络失败根因。`charges-1789966855862.json`与`charges-1789967246891.json`目标余额89,805,908、usedQuota10,194,092、requestCount171及21条收费日志集合未变；paid17/18未新增费用。两次原报告密码扫描均3文件0命中。只读账单首次SSH连接关闭，后续成功，首败保留。

当前1886项输入`776bf50ea8fcd587a12b5727954472627a457b0e4e489e45d675eb79bcc4ef5c`相对上述1884项仅6个测试/辅助文件变化，生产源未变。`check-oauth-2.log`退出0、38/38，根3296P/77条件S、features871P、Web58P、API默认418P/801条件S。数据库/同步条件`root-conditions-oauth-27.json`27P和`root-conditions-oauth-43.json`43P均0F/0S；后者含13项默认也执行的部署计划测试，不能相加冒称覆盖70个默认跳过项。完整API及自然一小时原进程已确认仍在运行。

上海13:08启动`e2e-oauth-final-1`完整579项、真实数据库条件开启、0重试/不更新快照。协调脚本绑定上述1886项输入，只有完整通过才继续新macOS包、7项包冒烟、三旧备份副本升级与产物扫描；原551P/1F/13S整轮报告不改写。完整实网、旧版反向复核、当前产物和提交尚未完成，5个父任务保持开放。

### 随后反向审计：账号服务公告迁移缺口

旧基线`0e5be61ccaeaa497ec01fa121f7767456ebd1a7f`的`AccountSignedInPanel.tsx:167`实际展示服务公告及“全部已读”，并非仅后台未启用字段；`AccountSection`与`account-section-helpers`保留按稳定内容ID的已读记录。当前NewApiClient的getNotices实现仍在，但生产消费端未接入，account契约/gateway/页面无对应动作，也不在冻结或暂缓域。该缺口回写共享产品parity父任务。补齐前不关闭父任务；当前1886项全量只可保留为补齐前证据，停止其自动打包协调链，不将有意中止计为通过。完整API/自然小时/Worker可继续取得原输入报告，不因观察超时重启。

## 公告补迁与前一批报告终态（13:44接续）

状态询问时只读核实：原完整API `api-full-oauth-1`已退出1，101文件通过、1失败、1条件跳过；1196P/23S。失败在`session-query`组beforeAll迁移建立schema时连接意外断开，21条正文未执行，另2条为原独立条件；不把“0条断言失败”当作整轮通过。Worker完整`worker-full-oauth-final`349P/2镜像条件S、退出0；改公告之前按真实文件+完整用例名逐项核对`conditional-coverage-oauth-final.json`：根77条件、Worker2条件都有独立通过报告，对应3373/351个唯一用例，1886项输入776bf50e…未漂移。此证据不包含公告增量。

原全量协调进程9373有意停止，观察工具句柄56196终态143；日志保留到第129条PC用例后生命周期退出，没有完整JSON，不计通过且没有继续打包。13:40只读发现原13:08:49启动的Next PID9460成为PPID1孤儿，cwd是本仓库`.next/standalone/apps/web-next`、仍占3399且无Playwright进程。核实后仅TERM该已废弃的自有测试服务，再开始新构建；没有停止正式App或真实账号栈。

公告已正式接入：contracts有界纯文本/内容ID/服务来源，NewAPI严格独立读取区分失败与空；公告内容通过公开GET读取，不额外登录或读取模型。API读取前后仍核对授权，真实受管会话的核对会调用上游profile；此前“不增加上游profile”表述不准确，已按源码纠正，不能因测试夹具未登记受管会话就推断实网调用数。桌面沿safeStorage会话的主进程请求并核对epoch/issuer；共享卡片保留文本/换行、显式已读、焦点、存储错误和账号切换隔离。新已读仅本设备按账号与两级服务保存；旧全设备内容ID通过白名单偏好迁移/旧Web存储保留，不复制正文或秘密。

证据：传输永久红测`notices-transport-red-1.json`5F（空白ID不一致/重复、纪元时间0丢失、失败伪装空、畸形和超限未拒绝），修后`notices-transport-green-1.json`新旧联合通过。契约/偏好迁移`notices-contract-migration-1.json`通过，Gateway/主进程专项`notices-gateways-1.json`通过。组件前两轮9F由Node测试环境的localStorage占位对象造成，第三轮8P/1F是重挂仍复用5分钟查询缓存的测试假设；明确用新QueryClient模拟页面刷新后`notices-ui-4.json`9P，未修改产品缓存时限。真实API首轮23P/2F为观察器getter名称和数据库枚举测试夹具错误；修为真实requests getter和recovery_only后`notices-api-session-query-2.json`25P/0F/0S、退出0，其中公告4项，会话分页21项；这不是完整API失败根因已关闭。当前完整check已启动，真实PC/移动/桌面公告和旧origin升级回归待执行。没有新增收费、部署或提交。

## 14:26 接续：公告联合验收、真实第二模型结果与网络层对照

公告补迁又核对了旧内容ID算法：旧字符串公告按原始内容（含首尾空白）生成已读ID，当前规范化内容只算新ID会重新显示旧已读公告。契约增加仅含公开内容ID的有界 legacyReadIds，传输层保留别名并合并重复项，共享卡同时识别新旧ID；就地先红后绿，契约/传输/桥接17P、组件11P。真实PC/移动及Electron测试注入旧原始ID，新PID与app/file旧origin均验证。fixture HTTP/HTTP2关服类型与IPC测试旧手写方法表分别修正；不是产品协议放宽或删除失败断言。

notices-check-3、notices-check-4均实际退出0，38/38成功；最新根3309P/77条件S、features882P、API默认418P/805条件S。条件跳过不算完整数据库/API通过。完整API api-full-notices-1绑定1897项输入08e96385…仍在原句柄97363执行，已进入真实进程中断/自然10分钟租约场景；没有因输出静默重启。原自然一小时包寿命两项已退出0、2P、3609.82秒，归其原输入。

公告第一轮宿主12P/1F为移动尺寸切换时读到了共享Button过渡中间帧；改等待实际44px稳定高度，不降低阈值。第二轮12P/1F唯一失败为新增24px账号卡组间距后旧移动截图。两轮实际截图已批量核对，独立finish评审ship仅限公告卡和局部间距，detector一次[]；不声称实际Tab焦点环或计算对比度已完整验收。notices-baseline-update-1只更新该移动截图1P，随后notices-hosts-3零重试/不更新快照13P/0F/0S/0flaky（48.7秒）。UI-SPEC与账号对照局部证据已回写；这些专项不关闭整页parity或完整迁移。

paid-19-resume-initial-tried原报告0P/1F保留。初版已实际正式化，第二模型gpt-image-2正式run 0d6f3927-8fae-4c6a-9607-44f6b3bca7ba完成，generation 0ebeca31-7d48-46d3-baaa-677da3b3088e成功；实际输出1,538,620字节、SHA256 a8f91665e1441ee34fedfc0563eca8d2d54347d39664d96827b93e8614eca7d2。随后方案详情GET503中断，另有生成列表503，不计整轮成功。charges-1789970615146到charges-1789970799797：余额89,805,908→89,745,908、usedQuota10,194,092→10,254,092、请求171→172、日志21→22；新增60,000原始quota（1.2显示积分）。截至此轮当天累计1,620,774原始quota（32.41548显示积分），包含失败轮中实际收费，不能只计最终通过轮。

免费诊断首轮登录502、退出200，profile-transport-1789971358306保留。确认隔离栈无在途生成/方案/Agent后，仅正常停止原自有栈27314并新PID启动。当前API另有透明网络诊断入口：只记录上游路径、方法、时间、HTTP状态/标准错误类，不记录任何header/body/凭据，不改请求、重试、超时或响应；生产bin源码不变。随后profile-transport-1789971552871的12次getSelf均成功，账号/原方案读取200、退出200。

连接对照profile-transport-1789971680403交替进行6组全新HTTPS连接：系统DNS（本机解析为代理虚拟IP）与显式原站IP两侧各有2次10秒超时，合计8成功/4超时，其中3次尚未完成TLS，1次完成TLS后未收HTTP头。两侧都失败，不能仅归因DNS。之后默认fetch复用连接的12次getSelf均成功，账号及原方案200、退出200。公开接口单次观测本机域名8.789秒、显式原站1.638秒、服务器本机0.039秒；不是负载基准。以上确认真实网络连接层会超时，但还不能将paid19每个503都归到同一具体网络设备，也没有因此修改系统代理、证书校验、产品10秒期限或鉴权规则。charges-1789971791771与paid19后账本完全一致，免费诊断无新增费用。

测试增加alternate-completed接续状态，必须读取精确原正式run/生成ID、验证同方案同版本、成功资格、第二模型、提示词与实际下载字节/hash；不得仅跳过收费步骤伪造通过。paid-20-resume-alternate已启动，继续移动修改/新版试跑/升级与导出；它仍是接续，不替代从零整轮。当前仅移动快照、实网测试及其显式环境变量登记增加，生产代码未改；全量API、三形态回归、新包/三备份和最终提交继续开放。

## 14:55 接续：真实网络超时证据、当前镜像及条件补验

paid-20-resume-alternate终态0P/1F、26.94秒：在账号模型GET503处失败，尚未进入既有正式结果核验或新的收费节点。透明网络记录经字段白名单检查、token行号归一化后保存为`paid20-api-network-metadata.json`（47条，仅方法/路径/时间/HTTP状态或错误类，没有header/body/凭据）：06:23:53.957Z的`/api/user/self`在10,001ms、06:24:04.408Z的`/api/pricing`在10,004ms抛AbortError，均未取得响应头。退出释放请求也在10,002ms超时，既有持久补偿随后06:24:36.909Z发起并返回200。该证据证明此轮包含真实上游传输超时，不据此指定某个代理设备为唯一根因，也不改产品鉴权、TLS或请求期限。独立公开请求对照中，系统DNS与显式本机代理各7次成功/1次超时，未证明替代链路稳定。

只读账本`charges-1789971964688.json`与paid19后及paid20前一致：余额89,745,908、usedQuota10,254,092、请求172、日志22，paid20及免费诊断没有新增费用。完整收费流程仍欠一次从零完整通过；既有真实成功节点和所有失败报告保留，不连续重发已完成的收费任务来碰运气。已向用户询问网络链路情况，未更改系统代理或公共服务配置。

当前执行输入`source-notices-current-2.json`为1897项、SHA256 `8326109bb720c1fbf6c761c9ebf07b662099b81ef9830a1e79feeb9e26ccb0de`。`notices-check-5`已实际退出0、38/38。14:31宿主中断后，先核实OS进程：原完整E2E32910/32997仍活，保留原运行并由新协调器接管等待，没有重跑；原API与真实账号栈已消失且没有完整API JSON，原api-full-notices-1按中断未完成保留，仅此后重新启动api-full-notices-2。新API、自然一小时、补验和镜像协调器均脱离会话启动，防止再次会话中断误杀或丢失结果；不把观察超时视作进程终态。

当前镜像三份均构建退出0，精确ID与源码摘要见`notices-runtime-image-summary.json`；实际Worker镜像2P及部署/恢复/回切18P均0F/0S。这是本机隔离生产镜像验证，不是Musefold公共站点发布。根默认3309P/77条件S、数据库/同步条件27P及43P、Worker完整349P/2条件S；实际上游镜像容量2的API专项1P、默认容量50的客户端专项1P，均0F/0S。原Go树9个overlay文件与当前仓库及历史摘要逐项相同，Go 1.25.1实际HTTP控制器与当前TypeScript客户端联合1P，Go进程也退出0；覆盖密码门、满额选择、幂等重放、激活、撤销屏障与refresh后精确清理，测试容器和内存数据随结束释放，不涉及线上账号。

`conditional-coverage-notices-final.json`按实际测试文件与完整用例名逐项映射，根77条件跳过及Worker2条件跳过都有独立通过证据，分别覆盖3386/351个唯一用例，missing=0；27和43中有重复默认用例，未直接相加冒称70个条件覆盖。源码再次核对未漂移。完整API、582项三形态E2E及当前自然一小时尚在运行，新macOS包/三旧备份需等待完整E2E通过后执行；5个父任务、整包验收与Git提交仍开放。

## 15:10 接续：完整回归发现的OAuth表单竞态与轮询测试时序

原完整`e2e-notices-final-1`真实暴露3项失败：PC/移动的账号模型用例均在默认5秒内未见成图；移动OAuth在强制重新登录时提交钮保持禁用。后者发生在隔离受控服务，不归因外部网络。确认源码原因并在隔离测试重现后，核对精确Playwright PID32997归属，再发SIGINT有序结束该已失败轮以接入修复；不是因静默或观察超时重启。原JSON终态：361 passed、3 failed、1 interrupted、9条件skipped、208 did-not-run（报告stats将后三者合计为218 skipped，不能将其冒称218个计划条件）；pnpm明确报告退出130，时长36.6分钟。原协调器因3失败拒绝继续，未生成新包，原全部错误上下文和报告保留。

OAuth源码以`account.dataUpdatedAt`作为review key的一部分，晚到的同账号status会切到无缓存review、短暂卸载AuthForm；再次挂载时密码清空、用户名回到remembered值。隔离复现6P/1F，等待表单返回后仍确定性看到输入被替换；正式就地回归`oauth-input-race-formal-red.json`9P/1F。正式修复只在前次`loginRequired=true`且精确签名请求相同时，保留未提交登录表单的占位展示；仍重新请求服务器，不复用旧consent权限/确认引用。换签名请求、校验失败或真实登录完成均不沿用旧凭据。新增迟到status、失败校验、旧授权禁止复用、跨请求清密码四项与原6项联合`oauth-input-race-formal-green.json`10P，退出0。隔离候选10P仅为诊断过程，已另存，不代替正式结果。

模型用例的夹具先queued→running，然后两次列表读取递减到成功；产品当前3秒轮询及UI规范一致，默认5秒断言早于至少6秒的合法完成。仅在该用例先增加与同文件原“排队→运行→成图”一致的15秒有界succeeded核对，然后继续验证真实资产、模型请求、底部留白和无横向溢出；不改产品轮询或网络期限、不把夹具提前置成功。OAuth浏览器用例同时新增屏障：只延迟实际后端status响应，输入用户名/密码后释放，等待真实review200并确认输入仍在，然后完成原双客户端/拒绝/重登/撤销流程。没有伪造身份或绕过禁用按钮。

`source-safety-fixes-current-1.json`仍1897项，SHA256 `2cdf8e156780513aeb9603560c78d0633ed4793f9a1f7799573f66db98871639`。`safety-fixes-source-delta.json`逐项证明相对8326109b…只改变共享OAuth源码、其就地测试及两个Web E2E文件，API/Worker/契约/数据库/上游Go输入未变。原完整API、自然小时继续原进程与原报告；新源码的完整check→PC/移动4项确定性浏览器→完整582项→新包/三旧备份/扫描协调链已启动，各阶段失败即停止后续，不将旧报告重命名为新整轮。

网络补充只读证据：`route -n get`显示原站45.207.211.136和代理解析地址198.18.0.71均经198.18.0.1网关、`utun5`。所以之前指定原站IP的对照没有真正绕过代理隧道，不能据此排除代理链路或认定服务器为根因。已询问是否允许仅该IP临时直连并恢复原配置，未擅自改系统网络；原网络检查问题也未收到回复。

## 15:20 接续：最新修复的正式验证及剩余补验边界

`safety-fixes-check-1`实际退出0、38/38；根3309P/77条件S、features886P，不能把默认条件跳过计为通过。`safety-fixes-e2e-1`实际退出0、PC/移动共4P、0F/0S/0flaky、31.7秒：OAuth用例延迟真实status响应并验证输入保持，模型用例仍经真实轮询看到完成资产。批量人工查看四张当前产物（PC/移动账号模型时间线与暗色键盘OAuth同意页），关键动作和焦点可见；图片/账号为隔离夹具，不冒称真实收费或全站可访问性认证。本轮没有追加视觉改造。

最新源码2cdf8e15…的完整582项于07:11:21Z开始，OS仍确认原协调器与子进程运行；完整API和自然小时也保持原进程，没有因观察等待或UI变动重启。07:18:12Z启动独立镜像验证链，三份镜像以当前源码重新构建并用不可变image ID执行现有部署/回切测试；构建仅发生在Docker内，不重建正在被E2E使用的宿主Next产物。新安装包/三备份仍由完整E2E全绿后串行启动。以上运行中项目不写作通过，最终Git提交仍待完整目标达成。

## 15:37 接续：当前镜像通过、API首败保留及隔离网络替代路径

最新1897项2cdf8e15…的三份镜像均构建退出0，实际Worker镜像2P、隔离部署/迁移/恢复/回切18P且0F/0S，五个阶段全部退出0；不可变ID见`safety-fixes-runtime-image-summary.json`。这次包含最新OAuth修复的Web镜像，不再引用旧Web镜像证明当前产物；不是公共站点发布。

原完整`api-full-notices-2`在07:24:46Z实际退出1，3035.25秒：1197P/2F/24S。24S中只有上游镜像和自然小时2项是计划条件，另外22项因`design-scheme-modify` beforeAll超过原120秒而未执行；不能称为正常跳过。两项失败在`login-session-lifecycle`：退出后首轮sweep的attempts仍0、恢复正确issuer后的首轮released仍0。原始日志/JSON完整保留。完全未改源码的两个文件联合复验`api-final-failures-repro-1`为37P、退出0、19.51秒；在忽略目录中复制生命周期测试、仅补不含凭据的时刻记录并使用6个独立数据库重复，`login-release-clock-diagnostic-2`90P、31.32秒，未复现。隔离诊断第一轮仅别名配置错误、没有执行测试，另存为diagnostic-1，不改写。尚未确认原因，不宣称修复、不放宽原断言或初始化期限。

07:35:56Z在原API确已结束、全部镜像构建结束后启动`api-full-safety-fixes-1`，仍为原1223项、单worker，绑定当前2cdf8e15…输入；额外每10秒在本任务PG中只读采样数据库与宿主时间差，不改任一测试的时钟/期限/SQL或产品行为。采样仅是同VM环境信息，不能替代失败点的精确时刻证据。原自然小时和582项E2E继续原进程，不因这次API终态重启；完整API仍未通过。

为继续排除外部网络因素，经已有`musefold-cloud` SSH连接建立仅监听127.0.0.1:18443的TCP转发到账号主机127.0.0.1:443；不改本机DNS/路由/代理、不改服务器配置。curl以原URL/SNI/证书身份发公开`GET /api/status`，普通路径与隧道交替各8次均200且TLS校验0；`ssh-tls-public-probe-1.json`保留16项实际观测。随后只在隔离验证模块使用现有Undici connector调整TCP目的地，保留Node原生fetch的URL/HTTP Host、TLS SNI与证书校验、原请求/Signal/期限；六个新Agent各两次公开读取，`ssh-fetch-public-probe-1.json`12次均200。两个自有SSH转发进程均实际结束，18443已确认无监听。没有身份伪造、付费请求、真实凭据传输或系统网络改动；短样本不证明普通网络已恢复，也不证明真实账号长流程已通过。隔离预加载模块仅为后续实网验收准备，尚未用于账号/收费流程。

## 15:50 接续：自然小时通过与本机容器端口转发故障

`natural-hour-notices-1`原进程07:39:17Z退出0，正式2P/0F/0S；完整命令3617.94秒，实际自然期限案例3605.440秒，12个已采纳资产对象保留，原Worker和接续Worker正常退出。`natural-hour-notices-verified.json`绑定原8326109b…输入及`package-lifetime-natural-sePrvf/result.json`字节摘要；期间只改OAuth/UI测试，不改API/Worker/期限代码，没有修改时钟或用旧自然小时报告替代本轮。

原`e2e-safety-fixes-final-1`在移动端provider-after-dispatch/text用例等待`AgentBrowserProcess.ready`120秒超时；错误上下文为空页面，未进入登录或方案业务，其他三个移动同组用例正常。只读核对两个同任务Testcontainers PostgreSQL发现容器健康、内部SQL可读且public表数0，但已公布的50396/50217端口在127.0.0.1和::1均ECONNREFUSED；经现有VM网关192.168.215.1的相同端口可实际SQL查询，直接容器IP也可读。证据`pg-localhost-forwarding-1.json`：这是本机localhost转发缺失的真实观测；创建时间与失败初始化接近，但没有进程→容器ID的原始绑定，不把时间相关性冒称两个首败的唯一根因。Docker内存观测未见接近15.65GiB限制，没有因此删容器、重启OrbStack或修改系统网络。

使用现有Testcontainers的`TESTCONTAINERS_HOST_OVERRIDE=192.168.215.1`做一次独立新库验证，真实启动+查询2810ms通过，自有探针容器随后删除。发现六个隔离子进程的显式env白名单只继承DOCKER_HOST、丢弃该标准host override；新增正式就地12项，原代码6P/6F（六类均丢配置）→各增加一条白名单后12P，未增加任意环境继承，额外验证父进程密码不传入。Turbo仅注册测试所用DOCKER_HOST/host override，并补根单测原来遗漏的`tests/v25/**`输入、排除`.results`，防止本地测试缓存忽略这些就地测试及其helper改动；不影响产品配置或构建环境。

确认原Playwright精确PID归属后，在已有失败且可修复测试接线明确时有序SIGINT，保留终态330P/1F/1中断/8计划S/242未执行、退出130；stats聚合251 skipped不代表251计划跳过。原包链拒绝继续。新源码1898项`0b5434120a215340f8d88921b1d346f9a6b8c003beeeac8fc115ebb0df13d003`，相对2cdf8e15…仅六个E2E helper、新12项测试及turbo配置8文件，`container-host-source-delta.json`明确API/Worker/产品/PG/上游Go未变。07:48:55Z开始新完整check→8项真实浏览器Provider故障→独立上游镜像PC/移动满额登录2项→582项全量→包/三旧备份的串行链，只有本轮测试进程显式使用host override，未写死任何产品地址、绕过就绪检查或延长120秒期限。原完整API继续已有进程与原输入归属；由于全仓输入已变，其旧全仓摘要守卫不能作为新输入整轮通过证明，需按实际终态与API执行输入边界核对。整包验收与Git提交仍开放。

## 16:02 接续：类型工程修复与实际浏览器补验通过

`container-host-check-1`实际退出1：新增环境继承测试引入的notices/OAuth/package-recovery三个helper尚未列入复合TypeScript工程，报TS6307；原报告保留，后续浏览器与包阶段未执行。只在`tests/repo/tsconfig.json`补三条include，独立typecheck退出0。当前1898项输入`6fb42ed548f9c869dd66cc4a1a4a929dd2e7c0d141ce0db595d710ddbbfa8634`，相对上一摘要仅此测试配置变化，无产品源码变化。

新`container-host-check-2`07:59:20Z实际退出0、38/38，根3321P/77条件S、features886P；条件跳过仍需各自实际补验。`container-host-e2e-1`PC/移动Provider发送前后、文本/参考图共8P/0F/0S/0flaky、2分钟，原120秒初始化期限不变；包括此前失败的移动after-dispatch/text用例。随后`session-browser-container-host-1`用精确New API镜像和全新自有PG数据库验证满额选择、继续登录和退出释放，PC/移动2P、15.8秒，测试容器和库已移除，不触碰生产账号或收费。为先完成真实收费闭环，本次协调器在定向验证后正常结束，582项完整回归与新包未启动，不能将这10项专项写成整轮通过。完整API仍为原64345/64368进程，尚未取得终态。（2026-09-23 补登更正：该轮即原进程，16:25 以 1221P/0F/2S 退出 0，见文末「补登」节。）

## 补登（2026-09-23，据证据目录核对）

以下结果发生在 16:02 接续之后，当时未写入本记录，现按 `tests/v25/.results/live25-20260921/` 的原始 JSON/日志补登；不改动上方任何原始叙述，只补齐登记缺口。

- **完整 API safety-fixes-1 终态（16:25）**：1221P/0F/2S，退出 0。源码绑定经逐文件核对（2026-09-23）：notices-2 绑定 1897 项 `8326109b…`（快照 14:26），safety-fixes-1 绑定 1897 项 `2cdf8e15…`（快照 15:08）；两份清单的 `apps/api`、`packages/contracts`、`apps/worker` 逐文件 sha256 完全一致，仅 4 个与 API 无关的文件不同（`OAuthAuthorizationScreen.tsx` 及其测试、`web.oauth-browser.spec.ts`、`web.workbench.spec.ts`）。因此两轮 API 输入等价；1197P/2F/24S 与 1221P/0F/2S 的差异（22 项被 `design-scheme-modify` beforeAll 120 秒阻塞、2 项 login-session-lifecycle 失败）属运行环境不稳定而非输入变化，且两轮均与其他重负载门禁并行——这正是 v2.5.1 P0-02 要求在最终提交上单独串行重跑完整 API 的依据。
- **paid-21（16:03，SSH 进程内转发路径）**：接续轮，165,338 raw（账单日志 1163＝Agent 105,338、1164＝图片 60,000）。报告自述 "Verified continuation; not a fresh complete paid flow"：复用已有的普通图、初版试跑/转正、第二模型结果，新做移动修改（含新版试跑出图）与导出。
- **paid-22（16:06，SSH 路径，从零）**：60,000 raw（账单日志 1165，16:07:24 图片）。服务端已成图，但前端图片 `data-loaded="false"` 5 秒不可见，`web.live-journey.spec.ts:29` 0P/1F（80,097ms）。
- **两次普通图诊断（16:11/16:14）**：只读，生图 POST 为 0，核对传输/解码/布局；可排除扣费。
- **工作台图片尺寸修复（16:16–16:17）**：成图后、图片字节到达前保留可见尺寸；就地单测 18P/4F→22P；E2E 只有修复前的红灯（修复后 `54a8362b…`），无绿灯——582 项完整回归在 `6fb42ed5…`/`54a8362b…` 上从未启动。
- **`6fb42ed5…` 上的其他未登记结果（16:08–16:18）**：根默认 3321P/77S、条件项 27P 与 43P（43P 含 13 项部署计划测试，不与其他计数相加）、完整 Worker 349P/2S。
- **全屏用例证据核对（2026-09-23 补查）**：当日 565 项轮（`e2e-final.json`）中 `electron.shell.spec.ts > macOS 原生全屏动态回收 brandInset` 为 passed，但 annotations 为空——证据不记录该轮实际走了原生切换还是 IPC 回退；真实原生切换验证由 v2.5.1 P0-09 补做并记录路径。
- **费用对账锚点**：16:11 只读账单（日志 1131–1165）合计 1,846,112 raw，与当日余额 91,366,682→89,520,570 差额精确相等。09-22 CLI 首轮之前另有 180,000 raw 无本地记录，归因与后续账单见 [v2.5.1 审计](../v2.5.1/README.md) §1.5 与 P0-11。
- **当日终态归纳**：09-21 没有 0F 的完整 E2E（5 次尝试仅 565 项轮跑完，551P/1F/13S）；最终源码 `54a8362b…` 未跑任何统一门禁；以上均由 v2.5.1 冻结候选提交后统一重跑。
